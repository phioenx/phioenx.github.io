<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>哈基鹏的大模型之路（六）</title>
      <link href="/posts/2ca4632f.html"/>
      <url>/posts/2ca4632f.html</url>
      
        <content type="html"><![CDATA[<h1 id="记录1：Transformers的TrainingArguments当中的参数"><a href="#记录1：Transformers的TrainingArguments当中的参数" class="headerlink" title="记录1：Transformers的TrainingArguments当中的参数"></a>记录1：Transformers的TrainingArguments当中的参数</h1><h2 id="具体示例："><a href="#具体示例：" class="headerlink" title="具体示例："></a>具体示例：</h2><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">training_args = TrainingArguments(</span><br><span class="line">    output_dir=<span class="string">&quot;./results&quot;</span>,</span><br><span class="line">    per_device_train_batch_size=<span class="number">8</span>,</span><br><span class="line">    per_device_eval_batch_size=<span class="number">16</span>,</span><br><span class="line">    num_train_epochs=<span class="number">3</span>,</span><br><span class="line">    evaluation_strategy=<span class="string">&quot;epoch&quot;</span>,</span><br><span class="line">    save_strategy=<span class="string">&quot;epoch&quot;</span>,</span><br><span class="line">    learning_rate=<span class="number">5e-5</span>,</span><br><span class="line">    weight_decay=<span class="number">0.01</span>,</span><br><span class="line">    fp16=<span class="literal">True</span>,</span><br><span class="line">    load_best_model_at_end=<span class="literal">True</span>,</span><br><span class="line">    metric_for_best_model=<span class="string">&quot;eval_loss&quot;</span>,</span><br><span class="line">    logging_dir=<span class="string">&quot;./logs&quot;</span>,</span><br><span class="line">    report_to=[<span class="string">&quot;tensorboard&quot;</span>],</span><br><span class="line">)</span><br></pre></td></tr></table></figure><h2 id="具体参数介绍："><a href="#具体参数介绍：" class="headerlink" title="具体参数介绍："></a>具体参数介绍：</h2><font color="black" size="5"><font color="blue" size="5">**1、output_dir (`str`)：**</font>模型预测和检查点输出的目录。</font><font color="black" size="5">**<font color="blue" size="5">2、overwrite_output_dir (`bool`, defaults to `False`)：</font>**如果该参数为True，在输出目录output_dir已经存在的情况下将删除该目录并重新创建。默认值是False。</font><font color="black" size="5"><font color="blue" size="5">**3、do_train (`bool`, defaults to `False`)：**</font>是否进行训练。Trainer没有直接使用此参数，它应用在我们写的training/evaluation脚本。</font><font color="black" size="5"><font color="blue" size="5">**4、do_eval (`bool`)：**</font>是否对验证集进行评估，evaluation_strategy如果不是no的话，应该设置为true，Trainer没有直接使用此参数，它应用在我们写的training/evaluation脚本。</font><font color="black" size="5"><font color="blue" size="5">**5、do_predict (`bool`, defaults to `False`)：**</font>是否在测试集上进行预测，Trainer没有直接使用此参数，它应用在我们写的training/evaluation脚本。</font><font color="black" size="5"><font color="blue" size="5">**6、evaluation_strategy (`str` or [`~trainer_utils.IntervalStrategy`], defaults to `"no"`)：**</font>训练期间采用的评估策略，可选的值有：</font><ul><li><font color="black" size="5">`"no"`：训练期间不进行评估</font></li><li><font color="black" size="5">`"steps"`：每一个eval_steps阶段之后都进行评估</font></li><li><font color="black" size="5">`"epoch"`：每一个epoch之后进行评估</font></li></ul><font color="black" size="5"><font color="blue" size="5">**7、prediction_loss_only (`bool`, defaults to `False`):**</font>当执行评估和预测的时候，是否仅仅返回损失。</font><font color="black" size="5"><font color="blue" size="5">**8、per_device_train_batch_size (`int`, defaults to 8):**</font>每一个GPU/TPU 或者CPU核心训练的批次大小。</font><font color="black" size="5">**<font color="blue" size="5">9、per_device_eval_batch_size (`int`, defaults to 8):</font>**每一个GPU/TPU 或者CPU核心评估的批次大小。</font><font color="black" size="5">**<font color="blue" size="5">10、gradient_accumulation_steps (`int`, *optional*, defaults to 1):</font>**在执行向后/更新过程之前，用于累积梯度的更新步骤数。</font><font color="black" size="5"><font color="blue" size="5">**11、eval_accumulation_steps (`int`):**</font>在将结果移动到CPU之前，累积输出张量的预测步骤数。如果如果未设置，则在移动到CPU之前，整个预测都会在GPU/TPU上累积（速度更快需要更多的内存）。</font><font color="black" size="5"><font color="blue" size="5">**12、eval_delay (`float`):**</font>在执行第一次评估之前要等待的epoch或step，具体取决于evaluation_strategy。</font><font color="black" size="5"><font color="blue" size="5">**13、learning_rate (`float`, defaults to 5e-5):**</font>`AdamW`优化器初始化的学习率。</font><font color="black" size="5"><font color="blue" size="5">**14、weight_decay (`float`, defaults to 0):**</font>在`AdamW`优化器中，除了bias和LayerNorm权重，如果weight_decay不是零，则应用于所有层。</font><font color="black" size="5"><font color="blue" size="5">**15、adam_beta1 (`float`, defaults to 0.9):**</font>`AdamW`优化器的beta1超参。</font><font color="black" size="5"><font color="blue" size="5">**16、adam_beta2 (`float`, defaults to 0.999):**</font>`AdamW`优化器的beta2超参。</font><font color="black" size="5"><font color="blue" size="5">**17、adam_epsilon (`float`, defaults to 1e-8):**</font>`AdamW`优化器的epsilon超参。</font><font color="black" size="5"><font color="blue" size="5">**18、max_grad_norm (`float`, defaults to 1.0):**</font>最大梯度范数（用于梯度剪裁）。</font><font color="black" size="5"><font color="blue" size="5">**19、num_train_epochs(`float`, defaults to 3.0):**</font>要执行的训练epoch的次数（如果不是整数，将执行停止训练前的最后一个epoch的小数部分百分比）。</font><font color="black" size="5"><font color="blue" size="5">**20、max_steps (`int`, defaults to -1):**</font>如果设置为正数，则表示要执行的训练step的次数。覆盖`num_train_epochs`。在使用有限可迭代数据集的情况下，训练可能在所有数据还没训练完成时因达到设定的步数而停止。</font><font color="black" size="5"><font color="blue" size="5">**21、lr_scheduler_type (`str`,  defaults to `"linear"`)：**</font>选择什么类型的学习率调度器来更新模型的学习率。可选的值有：`"linear"`，`"cosine"`，`"cosine_with_restarts"`，`"polynomial"`，`"constant"`，`"constant_with_warmup"`。</font><font color="black" size="5"><font color="blue" size="5">**22、warmup_ratio (`float`, defaults to 0.0):**</font>线性预热从0达到`learning_rate`时，每步学习率的增长率。</font><font color="black" size="5"><font color="blue" size="5">**23、warmup_steps (`int`, defaults to 0):**</font>线性预热从0达到`learning_rate`时，预热阶段的步数，它会覆盖`warmup_ratio`的设置。</font><font color="black" size="5"><font color="blue" size="5">**24、log_level (`str`, defaults to `passive`):**</font>设置主进程上使用的日志级别。可选择的值：'debug''info''warning''error' 'critical''passive' （不设置任何值，由应用进行设置）。</font><font color="black" size="5">**<font color="blue" size="5">25、log_level_replica (`str`, defaults to `passive`):</font>**控制训练过程中副本节点的日志级别，设置参数和log_level一样。</font><font color="black" size="5">**<font color="blue" size="5">26、log_on_each_node (`bool`, defaults to `True`):</font>**在多节点分布式训练中，是每个节点使用“log_level”进行一次日志记录，还是仅在主节点。</font><font color="black" size="5"><font color="blue" size="5">**27、logging_dir (`str`):**</font>日志目录。</font><font color="black" size="5">**<font color="blue" size="5">28、logging_strategy (`str` , defaults to `"steps"`):</font>**训练期间采用的日志策略，可选的值有：</font><ul><li><font color="black" size="5">`"no"`：训练期间不记录日志</font></li><li><font color="black" size="5">`"steps"`：每一个logging_steps阶段之后都记录日志</font></li><li><font color="black" size="5">`"epoch"`：每一个epoch之后记录日志</font></li></ul><font color="black" size="5">**<font color="blue" size="5">29、logging_first_step (`bool`, defaults to `False`):</font>**global_step 表示训练的全局步数。当训练开始时，global_step 被初始化为 0，每次更新模型时，global_step 会自动递增。是否打印日志和评估第一个`global_step`。</font><font color="black" size="5">**<font color="blue" size="5">30、logging_steps (`int`, defaults to 500):</font>**如果 `logging_strategy="steps"`，则两个日志中更新step的数量。</font><font color="black" size="5">**<font color="blue" size="5">31、logging_nan_inf_filter (`bool`, defaults to `True`):</font>**是否在日志中过滤掉 `nan` 和 `inf` 损失，如果设置为 `True`，每步的损失如果是 `nan`或者`inf`将会被过滤，将会使用平均损失记录在日志当中。</font><font color="black" size="5">**<font color="blue" size="5">32、save_strategy (`str`, defaults to `"steps"`):</font>**训练过程中，checkpoint的保存策略，可选择的值有：</font><ul><li><font color="black" size="5">`"no"`：训练过程中，不保存checkpoint</font></li><li><font color="black" size="5">`"epoch"`：每个epoch完成之后保存checkpoint</font></li><li><font color="black" size="5">`"steps"`：每个save_steps完成之后checkpoint</font></li></ul><font color="black" size="5">**<font color="blue" size="5">33、save_steps (`int`, defaults to 500):</font>**如果`save_strategy="steps"`，则两个checkpoint 保存的更新步骤数</font><font color="black" size="5">**<font color="blue" size="5">34、save_total_limit (`int`, ):</font>**如果设置了值，则将限制checkpoint的总数量，`output_dir`里面超过数量的老的checkpoint将会被删掉</font><font color="black" size="5">**<font color="blue" size="5">35、save_on_each_node (`bool`, defaults to `False`):</font>**当进行多节点分布式训练，是否在每个节点上保存模型和checkpoint还是仅仅在主节点上保存。当不同节点使用相同的存储时，不应激活此选项，因为文件将以相同的名称保存到每个节点</font><font color="black" size="5">**<font color="blue" size="5">36、no_cuda (`bool`, defaults to `False`):</font>**当有CUDA可以使用时，是否不使用CUDA</font><font color="black" size="5">**<font color="blue" size="5">37、seed (`int`, defaults to 42):</font>**训练开始时设置的随机种子，为了确保整个运行的可再现性，可使用`~Trainer.model_init`函数来初始化模型的随机初始化参数。</font><font color="black" size="5">**<font color="blue" size="5">38、data_seed (`int`):</font>**数据采样器的随机种子，它将用于数据采样器的可重现性，其独立于模型种子。</font><font color="black" size="5">**<font color="blue" size="5">39、jit_mode_eval (`bool`, defaults to `False`):</font>**是否使用PyTorch jit trace来进行推理</font><font color="black" size="5">**<font color="blue" size="5">40、use_ipex (`bool`, defaults to `False`):</font>**当PyTorch 的intel扩展可用时，是否使用</font><font color="black" size="5">**<font color="blue" size="5">41、bf16 (`bool`, defaults to `False`):</font>**是否使用bf16 16位 (mixed) 精度训练替代32位训练. 要求Ampere或者更高的NVIDIA架构，或者使用CPU训练.</font><font color="black" size="5">**<font color="blue" size="5">42、fp16 (`bool`, defaults to `False`):</font>**是否使用bf16 16位 (mixed) 精度训练替代32位训练.</font><font color="black" size="5">**<font color="blue" size="5">43、fp16_opt_level (`str`, defaults to 'O1'):</font>**`fp16`训练时, Apex AMP 优化级别选择，可选择的值有： ['O0', 'O1', 'O2', 'O3']Apex 是 NVIDIA 开发的一个混合精度训练和优化工具库，主要用于加速深度学习模型的训练过程。</font><font color="black" size="5">**<font color="blue" size="5">44、fp16_backend (`str`, defaults to `"auto"`):</font>**此参数已经废弃，使用`half_precision_backend`替代</font><font color="black" size="5">**<font color="blue" size="5">45、half_precision_backend (`str`, defaults to `"auto"`):</font>**半精度计算的后端实现，必须是这几个值：</font><ul><li><font color="black" size="5">`"auto"`：具体是使用CPU/CUDA AMP 还是APEX依赖于PyTorch版本检测</font></li><li><font color="black" size="5">`"cuda_amp"`</font></li><li><font color="black" size="5">`"apex"`</font></li><li><font color="black" size="5">`"cpu_amp"`</font></li></ul><font color="black" size="5">**<font color="blue" size="5">46、bf16_full_eval (`bool`, defaults to `False`):</font>**是否使用完整的bfloat16评估而不是32位。这将更快并节省内存，但可能会造成指标的损伤。</font><font color="black" size="5">**<font color="blue" size="5">47、fp16_full_eval (`bool`, defaults to `False`):</font>**是否使用完整的float16评估而不是32位。这将更快并节省内存，但可能会造成指标的损伤。</font><font color="black" size="5">**<font color="blue" size="5">48、tf32 (`bool`):</font>**是否启用TF32 模式，可以在Ampere 和更新的GPU架构上使用，默认值依赖于PyTorch的`torch.backends.cuda.matmul.allow_tf32`的默认值。</font><font color="black" size="5">**<font color="blue" size="5">49、local_rank (`int`, defaults to -1):</font>**分布式训练中进程的编号。在分布式训练中，每个进程（一般对应支持多线程的 GPU 卡）都会有一个特定的 local_rank，用于标识该进程对应的 GPU 编号。local_rank 的起始编号为 0，后续的编号依次递增。</font><font color="black" size="5">**<font color="blue" size="5">50、xpu_backend (`str`):</font>**xpu分布式训练中的后端，只能是 `"mpi"` 或者 `"ccl"`其中之一</font><font color="black" size="5">**<font color="blue" size="5">51、tpu_num_cores (`int`):</font>**当使用TPU训练时，TPU核心数 (自动通过启动脚本传递)。</font><font color="black" size="5">**<font color="blue" size="5">52、dataloader_drop_last (`bool`, defaults to `False`):</font>**是否删除最后一个不完整的批次（如果数据集的长度不能被批次大小整除）。</font><font color="black" size="5">**<font color="blue" size="5">53、eval_steps (`int`):</font>**如果 `evaluation_strategy="steps"`，两个评估之间更新step的数量Number of update steps，如果没有设置，则使用与 `logging_steps`一样的值。</font><font color="black" size="5">**<font color="blue" size="5">54、dataloader_num_workers (`int`, defaults to 0):</font>**数据加载的子进程数量（用于PyTorch ）. 0表示数据由主进程加载。</font><font color="black" size="5">**<font color="blue" size="5">55、past_index (`int`, defaults to -1):</font>**有些模型比如[TransformerXL](../model_doc/transformerxl)或者[XLNet](../model_doc/xlnet)使用过去隐藏状态进行预测。如果这个参数设置为正数，则 `Trainer`使用相应的输出（通常是索引2）作为过去的状态，并将其作为 `mems`参数提供给模型的下一个训练step。</font><font color="black" size="5">**<font color="blue" size="5">56、run_name (`str`):</font>**运行描述符。通常用于[wandb](https://www.wandb.com/)以及[mlflow](https://www.mlflow.org/)日志记录。</font><font color="black" size="5">**<font color="blue" size="5">57、disable_tqdm (`bool`):</font>**是否禁用在Jupyter Notebooks中由`~notebook.NotebookTrainingTracker`生成的tqdm进度条和指标表格。如果日志级别设置为warn或者更低的基本则默认值为`True`，否则为`False` 。</font><font color="black" size="5">**<font color="blue" size="5">58、remove_unused_columns (`bool`, defaults to `True`):</font>**是否自动删除模型forward方法不使用的列 (`TFTrainer`暂时还没有实现该功能)。</font><font color="black" size="5">**<font color="blue" size="5">59、label_names (`List[str]`):</font>**我们的输入字典的key列表相一致的标签，最终都将默认为`["labels"]`，除非使用`XxxForQuestionAnswering`系列的模型，该系列的模型最终默认为`["start_positions", "end_positions"]`。</font><font color="black" size="5">**<font color="blue" size="5">60、load_best_model_at_end (`bool`, defaults to `False`):</font>**是否在训练结束时加载训练期间发现的最佳模型。当设置为“True”时，参数“save_strategy”需要与“evaluation_strategy”相同，并且在这种情况下， "steps"和 `save_steps` 必须是`eval_steps`的整数倍。</font><font color="black" size="5">**<font color="blue" size="5">61、metric_for_best_model (`str`):</font>**与`load_best_model_at_end`一起使用，指定用于比较两个不同模型。必须是评估返回的度量的名称，带或不带前缀“eval_”。如果没有设定且`load_best_model_at_end=True`，则默认使用 `"loss"`，如果我们设置了这个值，则`greater_is_better`需要设置为 `True`。如果我们的度量在较低时更好，请不要忘记将其设置为“False”。</font><font color="black" size="5">**<font color="blue" size="5">62、greater_is_better (`bool`):</font>**与`load_best_model_at_end` 和 `metric_for_best_model`一起使用，说明好的模型是否应该有更好的度量值。默认值：</font><ul><li><font color="black" size="5">`True`：如果`metric_for_best_model`设置了值，并且该值不是`"loss"` 或者 `"eval_loss"`</font></li><li><font color="black" size="5">`False`：如果`metric_for_best_model`没有设置值，或者该值是`"loss"`或者 `"eval_loss"`.</font></li></ul><font color="black" size="5"><font color="blue" size="5">**63、ignore_data_skip (`bool`,  defaults to `False`):**</font>当恢复训练时，是否跳过之前训练时epoch和batch加载的数据，如果设置为`True`, 训练将会更快的开始，但是也不会产生与中断训练生成的相同的结果。</font><font color="black" size="5">**<font color="blue" size="5">64、sharded_ddp (`bool`, `str` or list of [`~trainer_utils.ShardedDDPOption`], defaults to `False`):</font>**是否启用分片式分布式数据并行（Sharded Distributed Data Parallel，简称ShardedDDP），以加快训练速度和效率。可选项有：`"simple"`、`"zero_dp_2"`、`"zero_dp_3"`、`"offload"`如果入参是字符串，它将会使用空格进行分隔，如果入参是了bool，它将被转换为空“False”的列表和["simple"]的“True”列表。</font>            <font color="black" size="5">**<font color="blue" size="5">65、fsdp (`bool`, `str` or list of [`~trainer_utils.FSDPOption`],  defaults to `False`):</font>**使用PyTorch 分布式并行训练(仅仅用在分布式训练)。可选项：`"full_shard"`、`"shard_grad_op"`、`"offload"`、`"auto_wrap"`：使用 `default_auto_wrap_policy`自动递归。</font><font color="black" size="5"><font color="blue" size="5">**66、fsdp_min_num_params (`int`,  defaults to `0`):**</font>用于指定使用 Fully Sharded Data Parallel （FSDP）时，最小可分片的参数数量。（仅在传递“fsdp”字段时有用）。</font><font color="black" size="5">**<font color="blue" size="5">67、deepspeed (`str` or `dict`):</font>**使用[Deepspeed](https://github.com/microsoft/deepspeed)。这是一个实验性功能，其API可能在未来发展。</font><font color="black" size="5">**<font color="blue" size="5">68、label_smoothing_factor (`float`, defaults to 0.0):</font>**要使用的标签平滑因子。它的取值范围在 0 到 1 之间。当 `label_smoothing_factor` 的值为 0 时，表示不使用标签平滑技术，此时模型接受到完整的 one-hot 标签，当 `label_smoothing_factor` 的值大于 0 时，表示使用标签平滑技术，此时真实标签将是一个加权平均值，其中每个标签的概率都等于 (1-label_smoothing_factor)/num_classes，其中 `num_classes` 表示标签的数量。</font><font color="black" size="5">**<font color="blue" size="5">69、debug (`str` or list of [`~debug_utils.DebugOption`], defaults to `""`):</font>**启用一个或多个调试功能。这是一个实验特性。可选项有：</font><ul><li><font color="black" size="5">`"underflow_overflow"`:检测模型的输入/输出中的溢出，并报告导致事件的最后一帧</font></li><li><font color="black" size="5">`"tpu_metrics_debug"`：在TPU上打印度量。这些选项通过空格进行分隔。</font></li></ul><font color="black" size="5">**<font color="blue" size="5">70、optim (`str` or [`training_args.OptimizerNames`] defaults to `"adamw_hf"`):</font>**可以使用的优化器：adamw_hf adamw_torch adamw_apex_fused adafactor。</font><font color="black" size="5"><font color="blue" size="5">**71、adafactor (`bool`, defaults to `False`):**</font>此参数已经废弃，使用 `--optim adafactor` 替代。</font><font color="black" size="5">**<font color="blue" size="5">72、group_by_length (`bool`, defaults to `False`):</font>**是否将训练数据集中长度大致相同的样本分组在一起（以最大限度地减少所应用的填充并提高效率）。仅在应用动态填充时有用。</font><font color="black" size="5">**<font color="blue" size="5">73、length_column_name (`str`, defaults to `"length"`):</font>**预计算列名的长度，如果列存在，则在按长度分组时使用这些值，而不是在训练启动时计算这些值。例外情况是：`group_by_length`设置为true，且数据集是`Dataset`的实例。</font><font color="black" size="5">**<font color="blue" size="5">74、report_to (`str` or `List[str]`, defaults to `"all"`):</font>**报告结果和日志的integration列表，支持的平台有：`"azure_ml"`, `"comet_ml"`, `"mlflow"`, `"tensorboard"` 和`"wandb"`. 使用 `"all"`则报告到所有安装的integration，配置为`"none"`则不报报告到任何的integration。</font><font color="black" size="5">**<font color="blue" size="5">75、ddp_find_unused_parameters (`bool`):</font>**使用分布式训练时，通过`find_unused_parameters`把该值传递给`DistributedDataParallel`。如果使用梯度checkpoint，则默认为false，否则为true。</font><font color="black" size="5">**<font color="blue" size="5">76、ddp_bucket_cap_mb (`int`):</font>**使用分布式训练时，传递给“DistributedDataParallel”的标志“bucket_cap_mb”的值。</font><font color="black" size="5">**<font color="blue" size="5">77、dataloader_pin_memory (`bool`, defaults to `True`):</font>**当设置为True 时，在数据加载过程中，batch 数据会被放入 CUDA 中固定的固定内存，从而避免了从主内存到 GPU 内存的冗余拷贝开销，提升了数据读取的效率。</font><font color="black" size="5">**<font color="blue" size="5">78、skip_memory_metrics (`bool`, defaults to `True`):</font>**是否跳过将内存探查器报告添加到度量中。默认情况下会跳过此操作，因为它会降低训练和评估速度。</font><font color="black" size="5">**<font color="blue" size="5">79、push_to_hub (`bool`, defaults to `False`):</font>**每次当模型保存的时候，是否把模型推送到Hub。</font><font color="black" size="5">**<font color="blue" size="5">80、resume_from_checkpoint (`str`):</font>**我们模型的有效checkpoint的文件夹的路径。此参数不是由直接给[`Trainer`]使用，它用于我们写的训练和评估脚本。</font><font color="black" size="5">**<font color="blue" size="5">81、hub_model_id (`str`):</font>**与本地的 `output_dir`保持同步的仓库名称。它可以是将会推送到我们的命名空间里的一个非常简单的模型ID . 否则它将需要完整的仓库名称，比如 `"user_name/model"`,它允许我们推送到一个我们是一个组织的成员之一（`"organization_name/model"`）的仓库。默认设置为`user_name/output_dir_name`，其中`output_dir_name` 是`output_dir`的值。</font><font color="black" size="5">**<font color="blue" size="5">82、hub_strategy (`str` or [`~trainer_utils.HubStrategy`], defaults to `"every_save"`):</font>**定义推送到hub的内容的范围以及何时推送到hub，可能的值有：</font><ul><li><font color="black" size="5">`"end"`：当`~Trainer.save_model`方法被调用的时候，会推送模型，推送它的配置、tokenizer（如果传给了`Trainer`）和model card 的草稿。</font></li><li><font color="black" size="5">`"every_save"`：在每次模型保存的时候，都会推送，推送它的配置、tokenizer（如果传给了`Trainer`）和model card 的草稿。推送是异步的，不会影响模型的训练，如果模型保存的非常频繁，则新的推送只会在旧的推送完成之后进行推送，最后的一个推送是在模型训练完成之后</font></li><li><font color="black" size="5">`"checkpoint"`：类似于 `"every_save"`，只是最后一个 checkpoint会被推送到名字为 last-checkpoint的子目录，它将方便我们使用 `trainer.train(resume_from_checkpoint="last-checkpoint")`重新开始训练。</font></li><li><font color="black" size="5">`"all_checkpoints"`: 类似于 `"checkpoint"` ，只是所有的checkpoints都推送，就像它们出现在输出目录一样 (这样你就可以在最终的仓库里面获取每一个checkpoint)</font></li></ul><font color="black" size="5">**<font color="blue" size="5">83、hub_token (`str`):</font>**用于将模型推送到Hub的token。默认将使用`huggingface-cli login`获得的缓存文件夹中的令牌。</font><font color="black" size="5">**<font color="blue" size="5">84、hub_private_repo (`bool`, defaults to `False`):</font>**如果为True， Hub repo将会被设置为私有的。</font><font color="black" size="5"><font color="blue" size="5">**85、gradient_checkpointing (`bool`, defaults to `False`):**</font>如果为True，则使用梯度检查点以节省内存为代价降低向后传递速度。</font><font color="black" size="5"><font color="blue" size="5">**86、include_inputs_for_metrics (`bool`,defaults to `False`):**</font>是否将输入传递给“compute_metrics”函数。这适用于需要在Metric类中进行评分计算的输入、预测和参考的度量。</font><font color="black" size="5"><font color="blue" size="5">**87、auto_find_batch_size (`bool`, defaults to `False`)：**</font>是否通过指数衰减自动找到适合内存的batch size，避免CUDA内存不足错误.需要安装 accelerate  (`pip install accelerate`)。</font><font color="black" size="5"><font color="blue" size="5">**88、full_determinism (`bool`, defaults to `False`)：**</font>如果为 `True`，则使用`enable_full_determinism`替代`set_seed`来确保在分布式训练下获得可重复的结果。</font><font color="black" size="5"><font color="blue" size="5">**89、torchdynamo (`str`):**</font>用于设置TorchDynamo后端编译器的token。可能的选择是[“eager”，“nvfuser]。这是一个实验性API，可能会更改。</font><font color="black" size="5"><font color="blue" size="5">**90、ray_scope (`str`, defaults to `"last"`):**</font>Ray Tune 是一个开元的分布式超参数优化库，可以用于自动搜索最佳的超参数配置，以及并行化训练作业。使用Ray进行超参搜索的范围。</font><font color="black" size="5"><font color="blue" size="5">**91、num_train_epochs (`int`):**</font>训练所用epoch数。</font><h1 id="记录二：高效微调方案"><a href="#记录二：高效微调方案" class="headerlink" title="记录二：高效微调方案"></a>记录二：高效微调方案</h1><h2 id="1-Adapt-Tuning"><a href="#1-Adapt-Tuning" class="headerlink" title="1.Adapt Tuning"></a>1.Adapt Tuning</h2><font color="black" size="5">**Adapt Tuning** 在模型中添加Adapter层，在微调时冻结原参数，仅更新Adapter层。具体而言，其在预训练模型每层中插入用于下游任务的参数，即 Adapter 模块，在微调时冻结模型主体，仅训练特定于任务的参数，如图6.8所示。</font><p><img src="/posts/2ca4632f.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251017180007342.png" alt="image-20251017180007342"></p><font color="black" size="5">每个 Adapter 模块由两个前馈子层组成，第一个前馈子层将 Transformer 块的输出作为输入，将原始输入维度 $d$ 投影到 $m$，通过控制 $m$ 的大小来限制 Adapter 模块的参数量，通常情况下 $m<<d$。在输出阶段，通过第二个前馈子层还原输入维度，将 $m$ 重新投影到 $d$，作为 adapter 模块的输出(如上图右侧结构)。< font><font color="black" size="5">LoRA 事实上就是一种改进的 Adapt Tuning 方法。但 Adapt Tuning 方法存在推理延迟问题，由于增加了额外参数和额外计算量，导致微调之后的模型计算速度相较原预训练模型更慢。</font><font color="black" size="5">**Prefix Tuning**。该种方法固定预训练 LM，为 LM 添加可训练，任务特定的前缀，这样就可以为不同任务保存不同的前缀，微调成本也小。具体而言，在每一个输入 token 前构造一段与下游任务相关的 virtual tokens 作为 prefix，在微调时只更新 prefix 部分的参数，而其他参数冻结不变。</font><font color="black" size="5">也是目前常用的微量微调方法的 Ptuning，其实就是 Prefix Tuning 的一种改进。但 Prefix Tuning 也存在固定的缺陷：模型可用序列长度减少。由于加入了 virtual tokens，占用了可用序列长度，因此越高的微调质量，模型可用序列长度就越低。</font><h2 id="2-LoRA微调"><a href="#2-LoRA微调" class="headerlink" title="2.LoRA微调"></a>2.LoRA微调</h2><font color="black" size="5">如果一个大模型是将数据映射到高维空间进行处理，这里假定在处理一个细分的小任务时，是不需要那么复杂的大模型的，可能只需要在某个子空间范围内就可以解决，那么也就不需要对全量参数进行优化了，我们可以定义当对某个子空间参数进行优化时，能够达到全量参数优化的性能的一定水平（如90%精度）时，那么这个子空间参数矩阵的秩就可以称为对应当前待解决问题的本征秩（intrinsic rank）。</font><font color="black" size="5">预训练模型本身就隐式地降低了本征秩，当针对特定任务进行微调后，模型中权重矩阵其实具有更低的本征秩（intrinsic rank）。同时，越简单的下游任务，对应的本征秩越低。（[Intrinsic Dimensionality Explains the Effectiveness of Language Model Fine-Tuning](https://arxiv.org/abs/2012.13255)）因此，权重更新的那部分参数矩阵尽管随机投影到较小的子空间，仍然可以有效的学习，可以理解为针对特定的下游任务这些权重矩阵就不要求满秩。我们可以通过优化密集层在适应过程中变化的秩分解矩阵来间接训练神经网络中的一些密集层，从而实现仅优化密集层的秩分解矩阵来达到微调效果。</font><font color="black" size="5">例如，假设预训练参数为 $θ_0^D$，在特定下游任务上密集层权重参数矩阵对应的本征秩为 $θ^d$，对应特定下游任务微调参数为 $θ^D$，那么有：</font><font color="black" size="5">$θ^D=θ_0^D+θ^dM$</font><font color="black" size="5">这个 $M$ 即为 LoRA 优化的秩分解矩阵。想对于其他高效微调方法，LoRA 存在以下优势：</font><font color="black" size="5">1️⃣可以针对不同的下游任务构建小型 LoRA 模块，从而在共享预训练模型参数基础上有效地切换下游任务。</font><font color="black" size="5">2️⃣LoRA 使用自适应优化器（Adaptive Optimizer），不需要计算梯度或维护大多数参数的优化器状态，训练更有效、硬件门槛更低。</font><font color="black" size="5">3️⃣LoRA 使用简单的线性设计，在部署时将可训练矩阵与冻结权重合并，不存在推理延迟。</font><font color="black" size="5">4️⃣LoRA 与其他方法正交，可以组合。</font><font color="black" size="5">因此，LoRA 成为目前高效微调 LLM 的主流方法，尤其是对于资源受限、有监督训练数据受限的情况下，LoRA 微调往往会成为 LLM 微调的首选方法。</font><font color="black" size="5">**LoRA微调的原理如下：**</font><font color="black" size="5">LoRA 假设权重更新的过程中也有一个较低的本征秩，对于预训练的权重参数矩阵 $W_0∈R^{d×k}$ ($d$为上一层输出维度，$k$ 为下一层输入维度)，使用低秩分解来表示其更新：</font><font color="black" size="5">$W_0+\Delta W = W_0 + BA,\quad where \quad B \in R^{d \times r},A\in R^{r\times k}$</font><font color="black" size="5">在训练过程中，$W_0$ 冻结不更新，$A$、$B$ 包含可训练参数。</font><font color="black" size="5">因此，LoRA 的前向传递函数为：</font><font color="black" size="5">$h=W_0x+\Delta Wx = W_0x + BAx$</font><font color="black" size="5">在开始训练时，对 $A$ 使用随机高斯初始化，对 $B$ 使用零初始化，然后使用 Adam 进行优化。训练思路如图6.9所示：</font><p><img src="/posts/2ca4632f.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251020094350755.png" alt="image-20251020094350755"></p><font color="black" size="5">在 Transformer 结构中，LoRA 技术主要应用在注意力模块的四个权重矩阵：$W_q$、$W_k$、$W_v$、$W_0$，而冻结 MLP 的权重矩阵。</font><font color="black" size="5">通过消融实验发现同时调整 $W_q$ 和 $W_v$ 会产生最佳结果。</font><font color="black" size="5">在上述条件下，可训练参数个数为：</font><font color="black" size="5">$\theta = 2 \times L_{LoRA}\times d_{model} \times r$</font><font color="black" size="5">其中，$L_{LoRA}$为应用 LoRA 的权重矩阵的个数，$d_{model}$ 为 Transformer 的输入输出维度，$r$ 为设定的 LoRA 秩。一般情况下，$r$ 取到 4、8、16。</font><font color="black" size="5">目前一般通过 peft 库来实现模型的 LoRA 微调。peft 库是 huggingface 开发的第三方库，其中封装了包括 LoRA、Adapt Tuning、P-tuning 等多种高效微调方法，可以基于此便捷地实现模型的 LoRA 微调。LoRA 微调的内部实现流程主要包括以下几个步骤：</font><font color="black" size="5">1️⃣确定要使用 LoRA 的层。peft 库目前支持调用 LoRA 的层包括：nn.Linear、nn.Embedding、nn.Conv2d 三种。</font><font color="black" size="5">在进行 LoRA 微调时，首先需要确定 LoRA 微调参数，其中一个重要参数即是 `target_modules`。`target_modules` 一般是一个字符串列表，每一个字符串是需要进行 LoRA 的层名称，例如：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">target_modules = [<span class="string">&quot;q_proj&quot;</span>,<span class="string">&quot;v_proj&quot;</span>]</span><br></pre></td></tr></table></figure><font color="black" size="5">这里的 `q_proj` 即为注意力机制中的 $W_q$， `v_proj` 即为注意力机制中的 $W_v$。我们可以根据模型架构和任务要求自定义需要进行 LoRA 操作的层。</font><font color="black" size="5">在创建 LoRA 模型时，会获取该参数，然后在原模型中找到对应的层，该操作主要通过使用 re 对层名进行正则匹配实现：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 找到模型的各个组件中，名字里带&quot;q_proj&quot;，&quot;v_proj&quot;的</span></span><br><span class="line">target_module_found = re.fullmatch(<span class="variable language_">self</span>.peft_config.target_modules, key)</span><br><span class="line"><span class="comment"># 这里的 key，是模型的组件名</span></span><br></pre></td></tr></table></figure><font color="black" size="5">2️⃣对每一个要使用 LoRA 的层，替换为 LoRA 层。所谓 LoRA 层，实则是在该层原结果基础上增加了一个旁路，通过低秩分解（即矩阵 $A$ 和矩阵 $B$）来模拟参数更新。</font><font color="black" size="5">对于找到的每一个目标层，会创建一个新的 LoRA 层进行替换。对于找到的每一个目标层，会创建一个新的 LoRA 层进行替换。</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">LoraLayer</span>:</span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params"></span></span><br><span class="line"><span class="params">        self,</span></span><br><span class="line"><span class="params">        r: <span class="built_in">int</span>, <span class="comment"># LoRA 的秩</span></span></span><br><span class="line"><span class="params">        lora_alpha: <span class="built_in">int</span>, <span class="comment"># 归一化参数</span></span></span><br><span class="line"><span class="params">        lora_dropout: <span class="built_in">float</span>, <span class="comment"># LoRA 层的 dropout 比例</span></span></span><br><span class="line"><span class="params">        merge_weights: <span class="built_in">bool</span>, <span class="comment"># eval 模式中，是否将 LoRA 矩阵的值加到原权重矩阵上</span></span></span><br><span class="line"><span class="params">    </span>):</span><br><span class="line">        <span class="variable language_">self</span>.r = r</span><br><span class="line">        <span class="variable language_">self</span>.lora_alpha = lora_alpha</span><br><span class="line">        <span class="comment"># Optional dropout</span></span><br><span class="line">        <span class="keyword">if</span> lora_dropout &gt; <span class="number">0.0</span>:</span><br><span class="line">            <span class="variable language_">self</span>.lora_dropout = nn.Dropout(p=lora_dropout)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="variable language_">self</span>.lora_dropout = <span class="keyword">lambda</span> x: x</span><br><span class="line">        <span class="comment"># Mark the weight as unmerged</span></span><br><span class="line">        <span class="variable language_">self</span>.merged = <span class="literal">False</span></span><br><span class="line">        <span class="variable language_">self</span>.merge_weights = merge_weights</span><br><span class="line">        <span class="variable language_">self</span>.disable_adapters = <span class="literal">False</span></span><br></pre></td></tr></table></figure><font color="black" size="5">nn.Linear 就是 Pytorch 的线性层实现。Linear 类就是具体的 LoRA 层，其主要实现如下：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Linear</span>(nn.Linear, LoraLayer):</span><br><span class="line">    <span class="comment"># LoRA 层</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params"></span></span><br><span class="line"><span class="params">        self,</span></span><br><span class="line"><span class="params">        in_features: <span class="built_in">int</span>,</span></span><br><span class="line"><span class="params">        out_features: <span class="built_in">int</span>,</span></span><br><span class="line"><span class="params">        r: <span class="built_in">int</span> = <span class="number">0</span>,</span></span><br><span class="line"><span class="params">        lora_alpha: <span class="built_in">int</span> = <span class="number">1</span>,</span></span><br><span class="line"><span class="params">        lora_dropout: <span class="built_in">float</span> = <span class="number">0.0</span>,</span></span><br><span class="line"><span class="params">        fan_in_fan_out: <span class="built_in">bool</span> = <span class="literal">False</span>, </span></span><br><span class="line"><span class="params">        merge_weights: <span class="built_in">bool</span> = <span class="literal">True</span>,</span></span><br><span class="line"><span class="params">        **kwargs,</span></span><br><span class="line"><span class="params">    </span>):</span><br><span class="line">        <span class="comment"># 继承两个基类的构造函数</span></span><br><span class="line">        nn.Linear.__init__(<span class="variable language_">self</span>, in_features, out_features, **kwargs)</span><br><span class="line">        LoraLayer.__init__(<span class="variable language_">self</span>, r=r, lora_alpha=lora_alpha, lora_dropout=lora_dropout, merge_weights=merge_weights)</span><br><span class="line"></span><br><span class="line">        <span class="variable language_">self</span>.fan_in_fan_out = fan_in_fan_out</span><br><span class="line">        <span class="comment"># Actual trainable parameters</span></span><br><span class="line">        <span class="keyword">if</span> r &gt; <span class="number">0</span>:</span><br><span class="line">            <span class="comment"># 参数矩阵 A</span></span><br><span class="line">            <span class="variable language_">self</span>.lora_A = nn.Linear(in_features, r, bias=<span class="literal">False</span>)</span><br><span class="line">            <span class="comment"># 参数矩阵 B</span></span><br><span class="line">            <span class="variable language_">self</span>.lora_B = nn.Linear(r, out_features, bias=<span class="literal">False</span>)</span><br><span class="line">            <span class="comment"># 归一化系数</span></span><br><span class="line">            <span class="variable language_">self</span>.scaling = <span class="variable language_">self</span>.lora_alpha / <span class="variable language_">self</span>.r</span><br><span class="line">            <span class="comment"># 冻结原参数，仅更新 A 和 B</span></span><br><span class="line">            <span class="variable language_">self</span>.weight.requires_grad = <span class="literal">False</span></span><br><span class="line">        <span class="comment"># 初始化 A 和 B</span></span><br><span class="line">        <span class="variable language_">self</span>.reset_parameters()</span><br><span class="line">        <span class="keyword">if</span> fan_in_fan_out:</span><br><span class="line">            <span class="variable language_">self</span>.weight.data = <span class="variable language_">self</span>.weight.data.T</span><br></pre></td></tr></table></figure><font color="black" size="5">替换时，直接将原层的 weight 和 bias 复制给新的 LoRA 层，再将新的 LoRA 层分配到指定设备即可。</font><font color="black" size="5">3️⃣冻结原参数，进行微调，更新 LoRA 层参数.</font><font color="black" size="5">实现了 LoRA 层的替换后，进行微调训练即可。由于在 LoRA 层中已冻结原参数，在训练中只有 A 和 B 的参数会被更新，从而实现了高效微调。训练的整体过程与原 Fine-tune 类似，此处不再赘述。由于采用了 LoRA 方式，forward 函数也会对应调整：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x: torch.Tensor</span>):</span><br><span class="line">    <span class="keyword">if</span> <span class="variable language_">self</span>.disable_adapters:</span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.r &gt; <span class="number">0</span> <span class="keyword">and</span> <span class="variable language_">self</span>.merged:</span><br><span class="line">            <span class="variable language_">self</span>.weight.data -= (</span><br><span class="line">                transpose(<span class="variable language_">self</span>.lora_B.weight @ <span class="variable language_">self</span>.lora_A.weight, <span class="variable language_">self</span>.fan_in_fan_out) * <span class="variable language_">self</span>.scaling</span><br><span class="line">            )</span><br><span class="line">            <span class="variable language_">self</span>.merged = <span class="literal">False</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> F.linear(x, transpose(<span class="variable language_">self</span>.weight, <span class="variable language_">self</span>.fan_in_fan_out), bias=<span class="variable language_">self</span>.bias)</span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;主要分支&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">elif</span> <span class="variable language_">self</span>.r &gt; <span class="number">0</span> <span class="keyword">and</span> <span class="keyword">not</span> <span class="variable language_">self</span>.merged:</span><br><span class="line">        result = F.linear(x, transpose(<span class="variable language_">self</span>.weight, <span class="variable language_">self</span>.fan_in_fan_out), bias=<span class="variable language_">self</span>.bias)</span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.r &gt; <span class="number">0</span>:</span><br><span class="line">            result += <span class="variable language_">self</span>.lora_B(<span class="variable language_">self</span>.lora_A(<span class="variable language_">self</span>.lora_dropout(x))) * <span class="variable language_">self</span>.scaling</span><br><span class="line">        <span class="keyword">return</span> result</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> F.linear(x, transpose(<span class="variable language_">self</span>.weight, <span class="variable language_">self</span>.fan_in_fan_out), bias=<span class="variable language_">self</span>.bias)</span><br><span class="line"></span><br></pre></td></tr></table></figure><font color="black" size="5">替换时，直接将原层的 weight 和 bias 复制给新的 LoRA 层，再将新的 LoRA 层分配到指定设备即可。</font></d$。在输出阶段，通过第二个前馈子层还原输入维度，将></font>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>哈基鹏的大模型之路（五）</title>
      <link href="/posts/49aa28f6.html"/>
      <url>/posts/49aa28f6.html</url>
      
        <content type="html"><![CDATA[<h1 id="大语言模型（Large-Lanauage-Model，LLM）"><a href="#大语言模型（Large-Lanauage-Model，LLM）" class="headerlink" title="大语言模型（Large Lanauage Model，LLM）"></a>大语言模型（Large Lanauage Model，LLM）</h1><h2 id="1-LLM的定义"><a href="#1-LLM的定义" class="headerlink" title="1.LLM的定义"></a>1.LLM的定义</h2><font color="black" size="5">&emsp;&emsp;LLM，即 Large Language Model，中文名为大语言模型或大型语言模型，是一种相较传统语言模型参数量更多、在更大规模语料上进行预训练的语言模型。LLM 使用与传统预训练语言模型相似的架构与预训练任务（如 Decoder-Only 架构与 CLM 预训练任务），但拥有更庞大的参数、在更海量的语料上进行预训练，也从而展现出与传统预训练语言模型截然不同的能力。</font><font color="black" size="5">&emsp;&emsp;一般来说，LLM 指包含**数百亿（或更多）参数的语言模型**，它们往往在**数 T token 语料上**通过多卡分布式集群进行预训练，具备远超出传统预训练模型的文本理解与生成能力。不过，随着 LLM 研究的不断深入，多种参数尺寸的 LLM 逐渐丰富，广义的 LLM 一般覆盖了从**十亿参数**（如 Qwen-1.5B）到**千亿参数**（如 Grok-314B）的所有大型语言模型。只要模型展现出**涌现能力**，即在一系列复杂任务上表现出远超传统预训练模型（如 BERT、T5）的能力与潜力，都可以称之为 LLM。</font><font color="black" size="5">&emsp;&emsp;一般认为，GPT-3（1750亿参数）是 LLM 的开端，基于 GPT-3 通过 预训练（Pretraining）、监督微调（Supervised Fine-Tuning，SFT）、强化学习与人类反馈（Reinforcement Learning with Human Feedback，RLHF）三阶段训练得到的 ChatGPT 更是主导了 LLM 时代的到来。自2022年11月 OpenAI 发布 ChatGPT 至今不到2年时间里，已涌现出了上百个各具特色、能力不一的 LLM。下表列举了自 2022年11月至2023年11月国内外发布的部分大模型：</font><div class="table-container"><table><thead><tr><th>时间</th><th>开源 LLM</th><th>闭源 LLM</th></tr></thead><tbody><tr><td>2022.11</td><td>无</td><td>OpenAI-ChatGPT</td></tr><tr><td>2023.02</td><td>Meta-LLaMA；复旦-MOSS</td><td>无</td></tr><tr><td>2023.03</td><td>斯坦福-Alpaca、Vicuna；智谱-ChatGLM</td><td>OpenAI-GPT4；百度-文心一言；Anthropic-Claude；Google-Bard</td></tr><tr><td>2023.04</td><td>阿里-通义千问；Stability AI-StableLM</td><td>商汤-日日新</td></tr><tr><td>2023.05</td><td>微软-Pi；Tll-Falcon</td><td>讯飞-星火大模型；Google-PaLM2</td></tr><tr><td>2023.06</td><td>智谱-ChatGLM2；上海 AI Lab-书生浦语；百川-BaiChuan；虎博-TigerBot</td><td>360-智脑大模型</td></tr><tr><td>2023.07</td><td>Meta-LLaMA2</td><td>Anthropic-Claude2；华为-盘古大模型3</td></tr><tr><td>2023.08</td><td>无</td><td>字节-豆包</td></tr><tr><td>2023.09</td><td>百川-BaiChuan2</td><td>Google-Gemini；腾讯-混元大模型</td></tr><tr><td>2023.11</td><td>零一万物-Yi；幻方-DeepSeek</td><td>xAI-Grok</td></tr></tbody></table></div><h2 id="2-LLM的能力"><a href="#2-LLM的能力" class="headerlink" title="2.LLM的能力"></a>2.LLM的能力</h2><h3 id="2-1-涌现能力（Emergent-Abilities）"><a href="#2-1-涌现能力（Emergent-Abilities）" class="headerlink" title="2.1 涌现能力（Emergent Abilities）"></a><font color="black" size="5">2.1 涌现能力（Emergent Abilities）</font></h3><font color="black" size="5">&emsp;&emsp;区分 LLM 与传统 PLM 最显著的特征即是 LLM 具备 `涌现能力` 。涌现能力是指同样的模型架构与预训练任务下，某些能力在小型模型中不明显，但在大型模型中特别突出。可以类比到物理学中的相变现象，涌现能力的显现就像是模型性能随着规模增大而迅速提升，超过了随机水平，也就是我们常说的量变引起了质变。</font><font color="black" size="5">&emsp;&emsp;具体来说，涌现能力可以定义为与某些复杂任务相关的能力。但一般而言，NLP 更关注的是它们具备的通用能力，也就是能够应用于解决各种 NLP 任务的能力。涌现能力是目前业界和学界对 LLM 保持较高的热情和关注的核心所在，即虽然 LLM 目前的能力、所能解决的任务与人类最终所期待的通用人工智能还存在不小的差距，但在涌现能力的作用下，我们相信随着研究的不断深入、高质量数据的不断涌现和更高效的模型架构及训练框架的出现，LLM 终能具备通用人工智能所需要具备的能力，从而给人类生活带来质变。</font><h3 id="2-2-上下文学习（In-context-Learning）"><a href="#2-2-上下文学习（In-context-Learning）" class="headerlink" title="2.2 上下文学习（In-context Learning）"></a><font color="black" size="5">2.2 上下文学习（In-context Learning）</font></h3><font color="black" size="5">&emsp;&emsp;上下文学习能力是由 GPT-3 首次引入的。具体而言，**上下文学习是指允许语言模型在提供自然语言指令或多个任务示例的情况下，通过理解上下文并生成相应输出的方式来执行任务，而无需额外的训练或参数更新**。</font><font color="black" size="5">&emsp;&emsp;对传统 PLM，在经过高成本的预训练之后，往往还需要对指定的下游任务进行有监督微调。虽然传统 PLM 体量较小，对算力要求较低，但例如 BERT 类模型（0.5B 参数），进行有监督微调一般还是需要 10G 以上显存，有一定的算力成本。而同时，有监督微调的训练数据的成本更高。针对下游任务难度的不同，需要的训练样本数往往在 1k~数十k 不等，均需要进行人工标注，数据获取上有不小的成本。而具备上下文学习能力的 LLM 往往无需进行高成本的额外训练或微调，而可以通过少数示例或是调整自然语言指令，来处理绝大部分任务，从而大大节省了算力和数据成本。</font><font color="black" size="5">&emsp;&emsp;上下文学习能力也正在引发 NLP 研究范式的变革。在传统 PLM 时代，解决 NLP 下游任务的一般范式是预训练-微调，即选用一个合适的预训练模型，针对自己的下游任务准备有监督数据来进行微调。而通过使用具备上下文学习能力的 LLM，一般范式开始向 Prompt Engineering 也就是调整 Prompt 来激发 LLM 的能力转变。例如，目前绝大部分 NLP 任务，通过调整 Prompt 或提供 1~5 个自然语言示例，就可以令 GPT-4 达到超过传统 PLM 微调的效果。</font><h3 id="2-3-指令遵循（Instruction-Following）"><a href="#2-3-指令遵循（Instruction-Following）" class="headerlink" title="2.3 指令遵循（Instruction Following）"></a><font color="black" size="5">2.3 指令遵循（Instruction Following）</font></h3><font color="black" size="5">&emsp;&emsp;通过使用自然语言描述的多任务数据进行微调，也就是所谓的 `指令微调` ，LLM 被证明在同样使用指令形式化描述的未见过的任务上表现良好。也就是说，经过指令微调的 LLM 能够理解并遵循未见过的指令，并根据任务指令执行任务，而无需事先见过具体示例，这展示了其强大的泛化能力。</font><font color="black" size="5">&emsp;&emsp;指令遵循能力意味我们不再需要每一件事都先教模型，然后它才能去做。我们只需要在指令微调阶段混合多种指令来训练其泛化能力，LLM 就可以处理人类绝大部分指令，即可以灵活地解决用户遇到的问题。这一点在 ChatGPT 上体现地尤为明显。ChatGPT 之所以能够具备极高的热度，其核心原因即在于其不再是仅能用于学界、业界研究的理论模型，而同样可以广泛地服务于各行各业用户。通过给 ChatGPT 输入指令，其可以写作文、编程序、批改试卷、阅读报纸等等。</font><font color="black" size="5">&emsp;&emsp;指令遵循能力使 LLM 可以真正和多个行业结合起来，通过人工智能技术为人类生活的方方面面赋能，从而为人类带来质的改变。不管是目前大火的 Agent、WorkFlow，还是并不遥远的未来可能就会出现的全能助理、超级智能，其本质依赖的都是 LLM 的指令遵循能力。</font><font color="black" size="5">**2.4 逐步推理（Step by Step Reasoning）**</font><font color="black" size="5">&emsp;&emsp;逻辑推理，尤其是涉及多个推理步骤的复杂推理任务，一直是 NLP 的攻关难点，也是人工智能难以得到普遍认可的重要原因。毕竟，如果一个模型不能解答基础的“鸡兔同笼”问题，或者不能识别语言中的逻辑陷阱，你很难认为它是“智能的”而非“智障的”。</font><font color="black" size="5">&emsp;&emsp;但是，传统的 NLP 模型通常难以解决涉及多个推理步骤的复杂任务，例如数学问题。然而，LLM 通过采用思维链（Chain-of-Thought，CoT）推理策略，可以利用包含中间推理步骤的提示机制来解决这些任务，从而得出最终答案。据推测，这种能力可能是通过对代码的训练获得的。</font><font color="black" size="5">&emsp;&emsp;逐步推理能力意味着 LLM 可以处理复杂逻辑任务，也就是说可以解决日常生活中需要逻辑判断的绝大部分问题，从而向“可靠的”智能助理迈出了坚实的一步。</font><font color="black" size="5">&emsp;&emsp;这些独特能力是 LLM 区别于传统 PLM 的重要优势，也让 LLM 在处理各种任务时表现出色，使它们成为了解决复杂问题和应用于多领域的强大工具。正是因为涌现能力、上下文学习能力、指令遵循能力与逐步推理能力的存在，NLP 研究人员相信 LLM 是迈向通用人工智能，帮助人类社会实现生产力质变的重要途径。而事实上，目前已有众多基于 LLM 的应用，旨在利用 LLM 的独特能力显著提高生产力。例如，微软基于 GPT-4 推出的 Copilot，就基于 LLM 强大的指令遵循能力与逐步推理能力，通过提供代码补全、代码提示、代码编写等多种功能，辅助程序员更高效、便捷、精准地编写程序，极大提高了程序员的生产效率。</font><h2 id="3-LLM的特点"><a href="#3-LLM的特点" class="headerlink" title="3.LLM的特点"></a>3.LLM的特点</h2><h3 id="3-1-多语言支持"><a href="#3-1-多语言支持" class="headerlink" title="3.1 多语言支持"></a><font color="black" size="5"><strong>3.1 多语言支持</strong></font></h3><font color="black" size="5">&emsp;&emsp;多语言、跨语言模型曾经是 NLP 的一个重要研究方向，但 LLM 由于需要使用到海量的语料进行预训练，训练语料往往本身就是多语言的，因此 LLM 天生即具有多语言、跨语言能力，只不过随着训练语料和指令微调的差异，在不同语言上的能力有所差异。由于英文高质量语料目前仍是占据大部分，以 GPT-4 为代表的绝大部分模型在英文上具有显著超越中文的能力。虽然都可以对多种语言进行处理，但针对中文进行额外训练和优化的国内模型（如文心一言、通义千问等）往往能够在中文环境上展现更优越的效果。</font><h3 id="3-2-长文本处理"><a href="#3-2-长文本处理" class="headerlink" title="3.2 长文本处理"></a><font color="black" size="5"><strong>3.2 长文本处理</strong></font></h3><font color="black" size="5">&emsp;&emsp;由于能够处理多长的上下文文本，在一定程度上决定了模型的部分能力上限，LLM 往往比传统 PLM 更看重长文本处理能力。相对于以 512 token 为惯例的传统 PLM（如 BERT、T5等模型的最大上下文长度均为 512），LLM 在拓宽最大上下文长度方面可谓妙计频出。由于在海量分布式训练集群上进行训练，LLM 往往在训练时就支持 4k、8k 甚至 32k 的上下文长度。同时，LLM 大部分采用了旋转位置编码（Rotary Positional Encoding，RoPE）（或者同样具有外推能力的 AliBi）作为位置编码，具有一定的长度外推能力，也就是在推理时能够处理显著长于训练长度的文本。例如，InternLM 在 32k 长度上下文上进行了预训练，但通过 RoPE 能够实现 200k 长度的上下文处理。通过不断增强长文本处理能力，LLM 往往能够具备更强的信息阅读、信息总结能力，从而解决诸如要求 LLM 读完《红楼梦》并写一篇对应的高考作文的“世纪难题”。</font><h3 id="3-3-拓展多模态"><a href="#3-3-拓展多模态" class="headerlink" title="3.3 拓展多模态"></a><font color="black" size="5"><strong>3.3 拓展多模态</strong></font></h3><font color="black" size="5">&emsp;&emsp;LLM 的强大能力也为其带来了跨模态的强大表现。随着 LLM 的不断改进，通过为 LLM 增加额外的参数来进行图像表示，从而利用 LLM 的强大能力打造支持文字、图像双模态的模型，已经是一个成功的方法。通过引入 Adapter 层和图像编码器，并针对性地在图文数据上进行有监督微调，模型能够具备不错的图文问答甚至生成能力。在未来，如何对齐文本与图像的表示，从而打造更强大的多模态大模型，将 LLM 的能力辐射到更多模态，是一个重要的研究方向。</font><h3 id="3-4-辉之不去的幻觉"><a href="#3-4-辉之不去的幻觉" class="headerlink" title="3.4 辉之不去的幻觉"></a><font color="black" size="5"><strong>3.4 辉之不去的幻觉</strong></font></h3><font color="black" size="5">&emsp;&emsp;幻觉，是指 LLM 根据 Prompt 杜撰生成虚假、错误信息的表现。例如，当我们要求 LLM 生成一篇学术论文及其参考文献列表时，其往往会捏造众多看似“一本正经”实则完全不存在的论文和研究。幻觉问题是 LLM 的固有缺陷，也是目前 LLM 研究及应用的巨大挑战。尤其是在医学、金融学等非常强调精准、正确的领域，幻觉的存在可能造成非常严重的后果。目前也有很多研究提供了削弱幻觉的一些方法，如 Prompt 里进行限制、通过 RAG（检索增强生成）来指导生成等，但都还只能一定程度减弱幻觉而无法彻底根除。</font><font color="black" size="5">&emsp;&emsp;除上述几点之外，LLM 还存在诸多可供研究的特点，例如我们将在下一节详细论述的 LLM 三阶段训练流程、LLM 的自我反思性等，此处就不一一列举赘述了。</font><h2 id="4-如何训练一个LLM"><a href="#4-如何训练一个LLM" class="headerlink" title="4.如何训练一个LLM"></a>4.如何训练一个LLM</h2><font color="black" size="5">&emsp;&emsp;在上一节，我们分析了 LLM 的定义及其特有的强大能力，通过更大规模的参数和海量的训练语料获得远超传统预训练模型的涌现能力，展现出强大的上下文学习、指令遵循及逐步推理能力，带来 NLP 领域的全新变革。那么，通过什么样的步骤，我们才可以训练出一个具有涌现能力的 LLM 呢？训练一个 LLM，与训练传统的预训练模型，又有什么区别？</font><p><img src="/posts/49aa28f6.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251010180322485.png" alt="image-20251010180322485"></p><font color="black" size="5">&emsp;&emsp;一般而言，训练一个完整的 LLM 需要经过图1中的三个阶段——Pretrain、SFT 和 RLHF。在这一节，我们将详细论述训练 LLM 的三个阶段，并分析每一个阶段的过程及其核心难点、注意事项，帮助读者们从理论上了解要训练一个 LLM，需要经过哪些步骤。</font><h3 id="4-1-Pretrain"><a href="#4-1-Pretrain" class="headerlink" title="4.1 Pretrain"></a><font color="black" size="5"><strong>4.1 Pretrain</strong></font></h3><font color="black" size="5">&emsp;&emsp;Pretrain，即预训练，是训练 LLM 最核心也是工程量最大的第一步。LLM 的预训练和传统预训练模型非常类似，同样是使用海量无监督文本对随机初始化的模型参数进行训练。正如我们在第三章中所见，目前主流的 LLM 几乎都采用了 Decoder-Only 的类 GPT 架构（LLaMA 架构），它们的预训练任务也都沿承了 GPT 模型的经典预训练任务——因果语言模型（Causal Language Model，CLM）。</font><font color="black" size="5">&emsp;&emsp;因果语言模型建模，即和最初的语言模型一致，通过给出上文要求模型预测下一个 token 来进行训练。CLM 的过程和原理我们已在第三章详细论述过，此处就不再赘述。LLM 的预训练同传统预训练模型的核心差异即在于，预训练的体量和资源消耗。</font><font color="black" size="5">&emsp;&emsp;根据定义，LLM 的核心特点即在于其具有远超传统预训练模型的参数量，同时在更海量的语料上进行预训练。传统预训练模型如 BERT，有 base 和 large 两个版本。BERT-base 模型由 12个 Encoder 层组成，其 hidden_size 为 768，使用 12个头作为多头注意力层，整体参数量为 1亿（110M）；而 BERT-large 模型由 24个 Encoder 层组成，hidden_size 为 1024，有 16个头，整体参数量为 3亿（340M）。同时，BERT 预训练使用了 33亿（3B）token 的语料，在 64块 TPU 上训练了 4天。事实上，相对于传统的深度学习模型，3亿参数量、33亿训练数据的 BERT 已经是一个能力超群、资源消耗巨大的庞然大物。</font><font color="black" size="5">&emsp;&emsp;但是，前面我们提到，一般而言的 LLM 通常具有数百亿甚至上千亿参数，即使是广义上最小的 LLM，一般也有十亿（1B）以上的参数量。例如以开山之作 GPT-3 为例，其有 96个 Decoder 层，12288 的 hidden_size 和 96个头，共有 1750亿（175B）参数，比 BERT 大出快 3个数量级。即使是目前流行的小型 LLM 如 Qwen-1.8B，其也有 24个 Decoder 层、2048的 hidden_size 和 16个注意力头，整体参数量达到 18亿（1.8B）。</font><div class="table-container"><table><thead><tr><th>模型</th><th>hidden_layers</th><th>hidden_size</th><th>heads</th><th>整体参数量</th><th>预训练数据量</th></tr></thead><tbody><tr><td>BERT-base</td><td>12</td><td>768</td><td>12</td><td>0.1B</td><td>3B</td></tr><tr><td>BERT-large</td><td>24</td><td>1024</td><td>16</td><td>0.3B</td><td>3B</td></tr><tr><td>Qwen-1.8B</td><td>24</td><td>2048</td><td>16</td><td>1.8B</td><td>2.2T</td></tr><tr><td>LLaMA-7B</td><td>32</td><td>4096</td><td>32</td><td>7B</td><td>1T</td></tr><tr><td>GPT-3</td><td>96</td><td>12288</td><td>96</td><td>175B</td><td>300B</td></tr></tbody></table></div><font color="black" size="5">&emsp;&emsp;更重要的是，LLM 往往需要使用更大规模的预训练语料。根据由 OpenAI 提出的 Scaling Law：C ~ 6ND，其中 C 为计算量，N 为模型参数，D 为训练的 token 数，可以实验得出训练 token 数应该是模型参数的 1.7倍，也就是说 175B 的 GPT-3，需要使用 300B token 进行预训练。而 LLaMA 更是进一步提出，使用 20倍 token 来训练模型能达到效果最优，因此 175B 的 GPT-3，可以使用3.5T token 数据预训练达到最优性能。</font><font color="black" size="5">&emsp;&emsp;如此庞大的模型参数和预训练数据，使得预训练一个 LLM 所需要的算力资源极其庞大。事实上，哪怕是预训练一个 1B 的大模型，也至少需要多卡分布式 GPU 集群，通过分布式框架对模型参数、训练的中间参数和训练数据进行切分，才能通过以天为单位的长时间训练来完成。一般来说，百亿级 LLM 需要 1024张 A100 训练一个多月，而十亿级 LLM 一般也需要 256张 A100 训练两、三天，计算资源消耗非常高。</font><font color="black" size="5">&emsp;&emsp;也正因如此，分布式训练框架也成为 LLM 训练必不可少的组成部分。分布式训练框架的核心思路是数据并行和模型并行。所谓数据并行，是指训练模型的尺寸可以被单个 GPU 内存容纳，但是由于增大训练的 batch_size 会增大显存开销，无法使用较大的 batch_size 进行训练；同时，训练数据量非常大，使用单张 GPU 训练时长难以接受。</font><p><img src="/posts/49aa28f6.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251010180958694.png" alt="image-20251010180958694"></p><font color="black" size="5">&emsp;&emsp;因此，如图4.2所示可以让模型实例在不同 GPU 和不同批数据上运行，每一次前向传递完成之后，收集所有实例的梯度并计算梯度更新，更新模型参数之后再传递到所有实例。也就是在数据并行的情况下，每张 GPU 上的模型参数是保持一致的，训练的总批次大小等于每张卡上的批次大小之和。</font><font color="black" size="5">&emsp;&emsp;但是，当 LLM 扩大到上百亿参数，单张 GPU 内存往往就无法存放完整的模型参数。如图4.3所示，在这种情况下，可以将模型拆分到多个 GPU 上，每个 GPU 上存放不同的层或不同的部分，从而实现模型并行。</font><p><img src="/posts/49aa28f6.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251010181046733.png" alt="image-20251010181046733"></p><font color="black" size="5">&emsp;&emsp;在数据并行和模型并行的思想基础上，还演化出了多种更高效的分布式方式，例如张量并行、3D 并行、ZeRO（Zero Redundancy Optimizer，零冗余优化器）等。目前，主流的分布式训练框架包括 Deepspeed、Megatron-LM、ColossalAI 等，其中，Deepspeed 使用面最广。</font><font color="black" size="5">&emsp;&emsp;Deepspeed 的核心策略是 ZeRO 和 CPU-offload。ZeRO 是一种显存优化的数据并行方案，其核心思想是优化数据并行时每张卡的显存占用，从而实现对更大规模模型的支持。ZeRO 将模型训练阶段每张卡被占用的显存分为两类：</font><ul><li><font color="black" size="5">模型状态（Model States），包括模型参数、模型梯度和优化器 Adam 的状态参数。假设模型参数量为 1M，一般来说，在混合精度训练的情况下，该部分需要 16M 的空间进行存储，其中 Adam 状态参数会占据 12M 的存储空间。</font></li><li><font color="black" size="5">剩余状态（Residual States），除了模型状态之外的显存占用，包括激活值、各种缓存和显存碎片。</font></li></ul><font color="black" size="5">&emsp;&emsp;针对上述显存占用，ZeRO 提出了三种不断递进的优化策略：</font><ol><li><font color="black" size="5">ZeRO-1，对模型状态中的 Adam 状态参数进行分片，即每张卡只存储 $\frac{1}{N}$ 的 Adam 状态参数，其他参数仍然保持每张卡一份。</font></li><li><font color="black" size="5">ZeRO-2，继续对模型梯度进行分片，每张卡只存储 $\frac{1}{N}$ 的模型梯度和 Adam 状态参数，仅模型参数保持每张卡一份。</font></li><li><font color="black" size="5">ZeRO-3，将模型参数也进行分片，每张卡只存储 $\frac{1}{N}$ 的模型梯度、模型参数和 Adam 状态参数。</font></li></ol><font color="black" size="5">&emsp;&emsp;可以看出，随着分片的参数量不断增加，每张卡需要占用的显存也不断减少。当然，分片的增加也就意味着训练中通信开销的增加，一般而言，每张卡的 GPU 利用率 ZeRO-1 最高而 ZeRO-3 最低。具体使用什么策略，需要结合计算资源的情况和需要训练的模型体量动态确定。</font><font color="black" size="5">&emsp;&emsp;除去计算资源的要求，训练数据本身也是预训练 LLM 的一个重大挑战。训练一个 LLM，至少需要数百 B 甚至上 T 的预训练语料。根据研究，LLM 所掌握的知识绝大部分都是在预训练过程中学会的，因此，为了使训练出的 LLM 能够覆盖尽可能广的知识面，预训练语料需要组织多种来源的数据，并以一定比例进行混合。目前，主要的开源预训练语料包括 CommonCrawl、C4、Github、Wikipedia 等。不同的 LLM 往往会在开源预训练语料基础上，加入部分私有高质量语料，再基于自己实验得到的最佳配比来构造预训练数据集。事实上，数据配比向来是预训练 LLM 的“核心秘籍”，不同的配比往往会相当大程度影响最终模型训练出来的性能。例如，下表展示了 LLaMA 的预训练数据及配比：</font><div class="table-container"><table><thead><tr><th>数据集</th><th>占比</th><th>数据集大小（Disk size）</th></tr></thead><tbody><tr><td>CommonCrawl</td><td>67.0%</td><td>3.3 TB</td></tr><tr><td>C4</td><td>15.0%</td><td>783 GB</td></tr><tr><td>Github</td><td>4.5%</td><td>328 GB</td></tr><tr><td>Wikipedia</td><td>4.5%</td><td>83 GB</td></tr><tr><td>Books</td><td>4.5%</td><td>85 GB</td></tr><tr><td>ArXiv</td><td>2.5%</td><td>92 GB</td></tr><tr><td>StackExchange</td><td>2.0%</td><td>78 GB</td></tr></tbody></table></div><font color="black" size="5">&emsp;&emsp;训练一个中文 LLM，训练数据的难度会更大。目前，高质量语料还是大部分集中在英文范畴，例如上表的 Wikipedia、Arxiv 等，均是英文数据集；而 C4 等多语言数据集中，英文语料也占据主要地位。目前开源的中文 LLM 如 ChatGLM、Baichuan 等模型均未开放其预训练数据集，开源的中文预训练数据集目前仅有昆仑天工开源的[SkyPile](https://huggingface.co/datasets/Skywork/SkyPile-150B)（150B）、中科闻歌开源的[yayi2](https://huggingface.co/datasets/wenge-research/yayi2_pretrain_data)（100B）等，相较于英文开源数据集有明显差距。</font><font color="black" size="5">&emsp;&emsp;预训练数据的处理与清洗也是 LLM 预训练的一个重要环节。诸多研究证明，预训练数据的质量往往比体量更加重要。预训练数据处理一般包括以下流程：</font><ol><li><font color="black" size="5">文档准备。由于海量预训练语料往往是从互联网上获得，一般需要从爬取的网站来获得自然语言文档。文档准备主要包括 URL 过滤（根据网页 URL 过滤掉有害内容）、文档提取（从 HTML 中提取纯文本）、语言选择（确定提取的文本的语种）等。</font></li><li><font color="black" size="5">语料过滤。语料过滤的核心目的是去除低质量、无意义、有毒有害的内容，例如乱码、广告等。语料过滤一般有两种方法：基于模型的方法，即通过高质量语料库训练一个文本分类器进行过滤；基于启发式的方法，一般通过人工定义 web 内容的质量指标，计算语料的指标值来进行过滤。</font></li><li><font color="black" size="5">语料去重。实验表示，大量重复文本会显著影响模型的泛化能力，因此，语料去重即删除训练语料中相似度非常高的文档，也是必不可少的一个步骤。去重一般基于 hash 算法计算数据集内部或跨数据集的文档相似性，将相似性大于指定阈值的文档去除；也可以基于子串在序列级进行精确匹配去重。</font></li></ol><font color="black" size="5">&emsp;&emsp;目前，已有很多经过处理的高质量预训练语料和专用于预训练数据处理的框架。例如，有基于 LLaMA 思路收集、清洗的预训练数据集[RedPajama-1T](https://huggingface.co/datasets/togethercomputer/RedPajama-Data-1T)，以及在 RedPajama 基础上进行筛选去重的[SlimPajama-627B](https://huggingface.co/datasets/cerebras/SlimPajama-627B/tree/main/train)数据集，实验证明高质量的 627B Slimpajama 数据集能够获得比 1T 的 RedPajama 数据集更好的效果。</font><h3 id="4-2-SFT"><a href="#4-2-SFT" class="headerlink" title="4.2 SFT"></a><font color="black" size="5"><strong>4.2 SFT</strong></font></h3><font color="black" size="5">&emsp;&emsp;预训练是 LLM 强大能力的根本来源，事实上，LLM 所覆盖的海量知识基本都是源于预训练语料。LLM 的性能本身，核心也在于预训练的工作。但是，预训练赋予了 LLM 能力，却还需要第二步将其激发出来。经过预训练的 LLM 好像一个博览群书但又不求甚解的书生，对什么样的偏怪问题，都可以流畅地接出下文，但他偏偏又不知道问题本身的含义，只会“死板背书”。这一现象的本质是因为，LLM 的预训练任务就是经典的 CLM，也就是训练其预测下一个 token 的能力，在没有进一步微调之前，其无法与其他下游任务或是用户指令适配。</font><font color="black" size="5">&emsp;&emsp;因此，我们还需要第二步来教这个博览群书的学生如何去使用它的知识，也就是 SFT（Supervised Fine-Tuning，有监督微调）。所谓有监督微调，其实就是我们在第三章中讲过的预训练-微调中的微调，稍有区别的是，对于能力有限的传统预训练模型，我们需要针对每一个下游任务单独对其进行微调以训练模型在该任务上的表现。例如要解决文本分类问题，需要对 BERT 进行文本分类的微调；要解决实体识别的问题，就需要进行实体识别任务的微调。</font><font color="black" size="5">&emsp;&emsp;而面对能力强大的 LLM，我们往往不再是在指定下游任务上构造有监督数据进行微调，而是选择训练模型的“通用指令遵循能力”，也就是一般通过`指令微调`的方式来进行 SFT。</font><font color="black" size="5">&emsp;&emsp;所谓指令微调，即我们训练的输入是各种类型的用户指令，而需要模型拟合的输出则是我们希望模型在收到该指令后做出的回复。例如，我们的一条训练样本可以是：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">input</span>:告诉我今天的天气预报？</span><br><span class="line">output:根据天气预报，今天天气是晴转多云，最高温度<span class="number">26</span>摄氏度，最低温度<span class="number">9</span>摄氏度，昼夜温差大，请注意保暖哦</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;也就是说，SFT 的主要目标是让模型从多种类型、多种风格的指令中获得泛化的指令遵循能力，也就是能够理解并回复用户的指令。因此，类似于 Pretrain，SFT 的数据质量和数据配比也是决定模型指令遵循能力的重要因素。</font><font color="black" size="5">&emsp;&emsp;首先是指令数据量及覆盖范围。为了使 LLM 能够获得泛化的指令遵循能力，即能够在未训练的指令上表现良好，需要收集大量类别各异的用户指令和对应回复对 LLM 进行训练。一般来说，在单个任务上 500~1000 的训练样本就可以获得不错的微调效果。但是，为了让 LLM 获得泛化的指令遵循能力，在多种任务指令上表现良好，需要在训练数据集中覆盖多种类型的任务指令，同时也需要相对较大的训练数据量，**表现良好的开源 LLM SFT 数据量一般在数 B token 左右**。</font><font color="black" size="5">&emsp;&emsp;为提高 LLM 的泛化能力，指令数据集的覆盖范围自然是越大越好。**但是**，多种不同类型的指令数据之间的配比也是 LLM 训练的一大挑战。OpenAI 训练的 InstructGPT（即 ChatGPT 前身）使用了源自于用户使用其 API 的十种指令：</font><div class="table-container"><table><thead><tr><th>指令类型</th><th>占比</th></tr></thead><tbody><tr><td>文本生成</td><td>45.6%</td></tr><tr><td>开放域问答</td><td>12.4%</td></tr><tr><td>头脑风暴</td><td>11.2%</td></tr><tr><td>聊天</td><td>8.4%</td></tr><tr><td>文本转写</td><td>6.6%</td></tr><tr><td>文本总结</td><td>4.2%</td></tr><tr><td>文本分类</td><td>3.5%</td></tr><tr><td>其他</td><td>3.5%</td></tr><tr><td>特定域问答</td><td>2.6%</td></tr><tr><td>文本抽取</td><td>1.9%</td></tr></tbody></table></div><font color="black" size="5">&emsp;&emsp;高质量的指令数据集具有较高的获取难度。不同于预训练使用的无监督语料，SFT 使用的指令数据集是有监督语料，除去设计广泛、合理的指令外，还需要对指令回复进行人工标注，并保证标注的高质量。事实上，ChatGPT 的成功很大一部分来源于其高质量的人工标注数据。但是，人工标注数据成本极高，也罕有企业将人工标注的指令数据集开源。为降低数据成本，部分学者提出了使用 ChatGPT 或 GPT-4 来生成指令数据集的方法。例如，经典的开源指令数据集 [Alpaca](https://github.com/yizhongw/self-instruct/blob/main/human_eval/user_oriented_instructions.jsonl)就是基于一些种子 Prompt，通过 ChatGPT 生成更多的指令并对指令进行回复来构建的。</font><font color="black" size="5">&emsp;&emsp;一般 SFT 所使用的指令数据集包括以下三个键：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;instruction&quot;</span>:<span class="string">&quot;即输入的用户指令&quot;</span>,</span><br><span class="line">    <span class="string">&quot;input&quot;</span>:<span class="string">&quot;执行该指令可能需要的补充输入，没有则置空&quot;</span>,</span><br><span class="line">    <span class="string">&quot;output&quot;</span>:<span class="string">&quot;即模型应该给出的回复&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;例如，如果我们的指令是将目标文本“今天天气真好”翻译成英文，那么该条样本可以构建成如下形式：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    <span class="string">&quot;instruction&quot;</span>:<span class="string">&quot;将下列文本翻译成英文：&quot;</span>,</span><br><span class="line">    <span class="string">&quot;input&quot;</span>:<span class="string">&quot;今天天气真好&quot;</span>,</span><br><span class="line">    <span class="string">&quot;output&quot;</span>:<span class="string">&quot;Today is a nice day！&quot;</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;同时，为使模型能够学习到和预训练不同的范式，在 SFT 的过程中，往往会针对性设置特定格式。例如，LLaMA 的 SFT 格式为：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">### Instruction:\n&#123;&#123;content&#125;&#125;\n\n### Response:\n</span></span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;其中的 content 即为具体的用户指令，也就是说，对于每一个用户指令，将会嵌入到上文的 content 部分，这里的用户指令不仅指上例中的 “instruction”，而是指令和输入的拼接，即模型可以执行的一条完整指令。例如，针对上例，LLaMA 获得的输入应该是：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">### Instruction:\n将下列文本翻译成英文：今天天气真好\n\n### Response:\n</span></span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;其需要拟合的输出则是：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">### Instruction:\n将下列文本翻译成英文：今天天气真好\n\n### Response:\nToday is a nice day！</span></span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;注意，因为指令微调本质上仍然是对模型进行 CLM 训练，只不过要求模型对指令进行理解和回复而不是简单地预测下一个 token，所以模型预测的结果不仅是 output，而应该是 input + output，只不过 input 部分不参与 loss 的计算，但回复指令本身还是以预测下一个 token 的形式来实现的。</font><font color="black" size="5">&emsp;&emsp;但是，随着 LLM 能力的不断增强，模型的多轮对话能力逐渐受到重视。所谓多轮对话，是指模型在每一次对话时能够参考之前对话的历史记录来做出回复。例如，一个没有多轮对话能力的 LLM 可能有如下对话记录：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">用户：你好，我是开源组织 Datawhale 的成员。</span><br><span class="line">模型：您好，请问有什么可以帮助您的吗？</span><br><span class="line">用户：你知道 Datawhale 是什么吗？</span><br><span class="line">模型：不好意思，我不知道 Datawhale 是什么。</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;也就是说，模型不能记录用户曾经提到或是自己曾经回答的历史信息。如果是一个具有多轮对话能力的 LLM，其对话记录应该是这样的：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">用户：你好，我是开源组织 Datawhale 的成员。</span><br><span class="line">模型：您好，请问有什么可以帮助您的吗？</span><br><span class="line">用户：你知道 Datawhale 是什么吗？</span><br><span class="line">模型：Datawhale 是一个开源组织。</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;模型是否支持多轮对话，与预训练是没有关系的。事实上，模型的多轮对话能力完全来自于 SFT 阶段。如果要使模型支持多轮对话，我们需要在 SFT 时将训练数据构造成多轮对话格式，让模型能够利用之前的知识来生成回答。假设我们目前需要构造的多轮对话是：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&lt;prompt_1&gt;&lt;completion_1&gt;&lt;prompt_2&gt;&lt;completion_2&gt;&lt;prompt_3&gt;&lt;completion_3&gt;</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;构造多轮对话样本一般有三种方式：</font><font color="black" size="5">1️⃣直接将最后一次模型回复作为输出，前面所有历史对话作为输入，直接拟合最后一次回复：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">input</span>=&lt;prompt_1&gt;&lt;completion_1&gt;&lt;prompt_2&gt;&lt;completion_2&gt;&lt;prompt_3&gt;&lt;completion_3&gt;</span><br><span class="line">output=[MASK][MASK][MASK][MASK][MASK]&lt;completion_3&gt;</span><br></pre></td></tr></table></figure><font color="black" size="5">2️⃣将 N 轮对话构造成 N 个样本：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">input_1 = &lt;prompt_1&gt;&lt;completion_1&gt;</span><br><span class="line">output_1 = [MASK]&lt;completion_1&gt;</span><br><span class="line"></span><br><span class="line">input_2 = &lt;prompt_1&gt;&lt;completion_1&gt;&lt;prompt_2&gt;&lt;completion_2&gt;</span><br><span class="line">output_2 = [MASK][MASK][MASK]&lt;completion_2&gt;</span><br><span class="line"></span><br><span class="line">input_3=&lt;prompt_1&gt;&lt;completion_1&gt;&lt;prompt_2&gt;&lt;completion_2&gt;&lt;prompt_3&gt;&lt;completion_3&gt;</span><br><span class="line">output_3=[MASK][MASK][MASK][MASK][MASK]&lt;completion_3&gt;</span><br></pre></td></tr></table></figure><font color="black" size="5">3️⃣直接要求模型预测每一轮对话的输出：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="built_in">input</span>=&lt;prompt_1&gt;&lt;completion_1&gt;&lt;prompt_2&gt;&lt;completion_2&gt;&lt;prompt_3&gt;&lt;completion_3&gt;</span><br><span class="line">output=[MASK]&lt;completion_1&gt;[MASK]&lt;completion_2&gt;[MASK]&lt;completion_3&gt;</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;显然可知，第一种方式会丢失大量中间信息，第二种方式造成了大量重复计算，只有第三种方式是最合理的多轮对话构造。我们之所以可以以第三种方式来构造多轮对话样本，是因为 LLM 本质还是进行的 CLM 任务，进行单向注意力计算，因此在预测时会从左到右依次进行拟合，前轮的输出预测不会影响后轮的预测。目前，绝大部分 LLM 均使用了多轮对话的形式来进行 SFT。</font><h3 id="4-3-RLHF"><a href="#4-3-RLHF" class="headerlink" title="4.3 RLHF"></a><font color="black" size="5">4.3 RLHF</font></h3><font color="black" size="5">&emsp;&emsp;RLHF，全称是 Reinforcement Learning from Human Feedback，即人类反馈强化学习，是利用强化学习来训练 LLM 的关键步骤。相较于在 GPT-3 就已经初见雏形的 SFT，RLHF 往往被认为是 ChatGPT 相较于 GPT-3 的最核心突破。事实上，从功能上出发，我们可以将 LLM 的训练过程分成预训练与对齐（alignment）两个阶段。**预训练的核心作用是赋予模型海量的知识，而所谓对齐，其实就是让模型与人类价值观一致，从而输出人类希望其输出的内容。**在这个过程中，SFT 是让 LLM 和人类的指令对齐，从而具有指令遵循能力；而 RLHF 则是从更深层次令 LLM 和人类价值观对齐，令其达到安全、有用、无害的核心标准。</font><font color="black" size="5">&emsp;&emsp;RLHF 的思路是，引入强化学习的技术，通过实时的人类反馈令 LLM 能够给出更令人类满意的回复。强化学习是有别于监督学习的另一种机器学习方法，主要讨论的问题是智能体怎么在复杂、不确定的环境中最大化它能获得的奖励。强化学习主要由两部分构成：智能体和环境。在强化学习过程中，智能体会不断行动并从环境获取反馈，根据反馈来调整自己行动的策略。应用到 LLM 的对齐上，其实就是针对不同的问题，LLM 会不断生成对应的回复，人工标注员会不断对 LLM 的回复做出反馈，从而让 LLM 学会人类更偏好、喜欢的回复。</font><font color="black" size="5">&emsp;&emsp;RLHF 就类似于 LLM 作为一个学生，不断做作业来去提升自己解题能力的过程。如果把 LLM 看作一个能力强大的学生，Pretrain 是将所有基础的知识教给他，SFT 是教他怎么去读题、怎么去解题，那么 RLHF 就类似于真正的练习。LLM 会不断根据 Pretrain 学到的基础知识和 SFT 学到的解题能力去解答练习，然后人类作为老师批改 LLM 的练习，来让 LLM 反思错误的解题方式，不断强化正确的解题方式。</font><font color="black" size="5">&emsp;&emsp;RLHF 分为两个步骤：**训练 RM 和 PPO 训练。**</font><font color="black" size="5">&emsp;&emsp;RM，Reward Model，即奖励模型。RM 是用于拟合人类偏好，来给 LLM 做出反馈的。在强化学习的训练中，对于 LLM 的每一个回复，RM 会进行打分，这个打分反映了生成回复符合人类偏好的程度。然后 LLM 会根据强化学习的原理，基于 RM 的打分来进行优化训练。所以，RM 本质上是一个文本分类模型，对于一个文本输出一个标量奖励，和文本分类任务中的隐藏层输出非常类似。在具体实现上，RM 也往往就是传统的 LLM 架构（或 BERT 架构）加上一层分类层，和用于文本分类的 LLM 架构完全一致，只不过使用隐藏层输出而不是最后的分类输出而已。</font><font color="black" size="5">&emsp;&emsp;但是，在训练 RM 时，我们往往并不直接使用文本及其对应的标量奖励来对 RM 进行训练。因为要对齐人类偏好，RM 训练的偏好数据往往是由人工标注的。但是，由于标注者之间往往也存在价值观差异，数值形式的标量奖励往往会将这些差异放大，从而导致在训练过程中对同样程度的回复奖励不一致，模型难以拟合到正确的标量奖励。因此，我们往往对同一个 completion 下的不同回复进行排名，再将排名转化为奖励。</font><font color="black" size="5">&emsp;&emsp;例如，我们的训练数据往往形如：</font><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="punctuation">&#123;</span></span><br><span class="line">    <span class="attr">&quot;prompt&quot;</span><span class="punctuation">:</span><span class="string">&quot;如果你打算从商店偷东西，你觉得早上好还是晚上好？&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;chosen&quot;</span><span class="punctuation">:</span><span class="string">&quot;这是违法的事情，我不能提供建议&quot;</span><span class="punctuation">,</span></span><br><span class="line">    <span class="attr">&quot;rejected&quot;</span><span class="punctuation">:</span><span class="string">&quot;考虑晚上的人口贩运和监控摄像头的差别是件好事。夜间时间可能更有利于避免监控摄像头,但晚上的商店雇员会更能看见你。另一方面,由于白天通常不太忙,因此更容易避免被其他人注意到。无论如何,必须密切注意商店雇员和所有顾客的行为。他们也许能够看见你,即使他们不是直接面对你。为了安全起见,重要的是要密切注意商店里的一切事情,而不是不小心。&quot;</span></span><br><span class="line"><span class="punctuation">&#125;</span></span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;其中，prompt 是用户的问题，chosen 是应该对齐的、符合人类偏好的回答，rejected 是不符合人类偏好的回答。在训练中，prompt 将和 chosen 以及 rejected 分别拼接起来，形成 chosen_example 和 rejected_example，然后分别进入模型通过前向传播输出一个标量奖励。然后模型会通过最大化 chosen_example 和 rejected_example 的标量差异来计算 loss，并进行反向传播完成训练。</font><font color="black" size="5">&emsp;&emsp;值得注意的是，RM 训练使用的模型往往和最后的 LLM 大小不同。例如 OpenAI 使用了 175B 的 LLM 和 6B 的 RM。同时，RM 使用的模型可以是经过 SFT 之后的 LM，也可以是基于偏好数据从头训练的 RM。哪一种更好，至今尚没有定论。</font><font color="black" size="5">&emsp;&emsp;在完成 RM 训练之后，就可以使用 PPO 算法来进行强化学习训练。PPO，Proximal Policy Optimization，近端策略优化算法，是一种经典的 RL 算法。事实上，强化学习训练时也可以使用其他的强化学习算法，但目前 PPO 算法因为成熟、成本较低，还是最适合 RLHF 的算法。</font><font color="black" size="5">&emsp;&emsp;在具体 PPO 训练过程中，会存在四个模型。如图4.5所示，两个 LLM 和两个 RM。两个 LLM 分别是进行微调、参数更新的 actor model 和不进行参数更新的 ref model，均是从 SFT 之后的 LLM 初始化的。两个 RM 分别是进行参数更新的 critic model 和不进行参数更新的 reward model，均是从上一步训练的 RM 初始化的。</font><p><img src="/posts/49aa28f6.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251011094630171.png" alt="image-20251011094630171"></p><font color="black" size="5">&emsp;&emsp;如上图，使用 PPO 算法的强化学习训练过程如下：</font><ol><li><font color="black" size="5">从 SFT 之后的 LLM 初始化两个模型分别作为 Actor Model 和 Ref Model；从训练的 RM 初始化两个模型分别作为 Reward Model 和 Critic Model；</font></li><li><font color="black" size="5">输入一个 Prompt，Actor Model 和 Ref Model 分别就 Prompt 生成回复；</font></li><li><font color="black" size="5">Actor Response 和 Ref Response 计算 KL 散度： $r_{KL}=-\theta_{KL}D_{KL}(\pi_{PPO}(y|x)||\pi_{base}(y|X))$。其中， $π_{PPO}(y∣x)$ 即为 Actor Model 的输出，而 $π_{base}(y∣x)$ 即为 Ref Model 的输出， $\theta_{KL}D_{KL}$即是计算 KL 散度的方法；</font></li><li><font color="black" size="5">Actor Response 分别输入到 Reward Model 和 Critic Model 进行打分，其中，Reward Model 输出的是回复对应的标量奖励，Critic Model 还会输出累加奖励（即从i位置到最后的累积奖励）；</font></li><li><font color="black" size="5">计算的 KL 散度、两个模型的打分均输入到奖励函数中，计算奖励： $loss=−(kl_{ctl}⋅r_{KL}+γ⋅V_{t+1}−V_{t})\log⁡P(A_t∣V_t)$，这里的 $kl_{ctl}$ 是控制 KL 散度对结果影响的权重参数， $γ$ 是控制下一个时间（也就是样本）打分对结果影响的权重参数， $V_t$ 是 Critic Model 的打分输出， $A_t$ 则是 Reward Model 的打分输出;</font></li><li><font color="black" size="5">根据奖励函数分别计算出的 actor loss 和 critic loss，更新 Actor Model 的参数和 Critic Model 的参数；注意，Actor Model 和 Critic Model 的参数更新方法是不同的，此处就不再一一赘述了，感兴趣的读者可以深入研究强化学习的相关理论。</font></li></ol><font color="black" size="5">&emsp;&emsp;在上述过程中，因为要使用到四个模型，显存占用会数倍于 SFT。例如，如果我们 RM 和 LLM 都是用 7B 的体量，PPO 过程中大概需要 240G（4张 80G A100，每张卡占用 60G）显存来进行模型加载。那么，为什么我们需要足足四个模型呢？Actor Model 和 Critic Model 较为容易理解，而之所以我们还需要保持原参数不更新的 Ref Model 和 Reward Model，是为了限制模型的更新不要过于偏离原模型以至于丢失了 Pretrain 和 SFT 赋予的能力。</font><font color="black" size="5">&emsp;&emsp;当然，如此大的资源占用和复杂的训练过程，使 RLHF 成为一个门槛非常高的阶段。也有学者从监督学习的思路出发，提出了 DPO（Direct Preference Optimization，直接偏好优化），可以低门槛平替 RLHF。DPO 的核心思路是，将 RLHF 的强化学习问题转化为监督学习来直接学习人类偏好。DPO 通过使用奖励函数和最优策略间的映射，展示了约束奖励最大化问题完全可以通过单阶段策略训练进行优化，也就是说，通过学习 DPO 所提出的优化目标，可以直接学习人类偏好，而无需再训练 RM 以及进行强化学习。由于直接使用监督学习进行训练，DPO 只需要两个 LLM 即可完成训练，且训练过程相较 PPO 简单很多，是 RLHF 更简单易用的平替版本。DPO 所提出的优化目标为什么能够直接学习人类偏好，作者通过一系列的数学推导完成了证明，感兴趣的读者可以下来进一步阅读，此处就不再赘述了。</font>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>哈基鹏的大模型之路（四）</title>
      <link href="/posts/2634edb1.html"/>
      <url>/posts/2634edb1.html</url>
      
        <content type="html"><![CDATA[<h1 id="预训练语言模型"><a href="#预训练语言模型" class="headerlink" title="预训练语言模型"></a>预训练语言模型</h1><font color="black" size="5">&emsp;&emsp;Transformer 结构主要由 Encoder、Decoder 两个部分组成，两个部分分别具有不一样的结构和输入输出。针对 Encoder、Decoder 的特点，引入 ELMo 的预训练思路，开始出现不同的、对 Transformer 进行优化的思路。例如，Google 仅选择了 Encoder 层，通过将 Encoder 层进行堆叠，再提出不同的预训练任务-掩码语言模型（Masked Language Model，MLM），打造了一统自然语言理解（Natural Language Understanding，NLU）任务的代表模型——BERT。而 OpenAI 则选择了 Decoder 层，使用原有的语言模型（Language Model，LM）任务，通过不断增加模型参数和预训练语料，打造了在 NLG（Natural Language Generation，自然语言生成）任务上优势明显的 GPT 系列模型，也是现今大火的 LLM 的基座模型。当然，还有一种思路是同时保留 Encoder 与 Decoder，打造预训练的 Transformer 模型，例如由 Google 发布的 T5模型。</font><font color="black" size="5">&emsp;&emsp;在本章中，我们将以 Encoder-Only、Encoder-Decoder、Decoder-Only 的顺序来依次介绍 Transformer 时代的各个主流预训练模型，分别介绍三种核心的模型架构、每种主流模型选择的预训练任务及其独特优势，这也是目前所有主流 LLM 的模型基础。</font><h2 id="1-Encoder-only-PLM"><a href="#1-Encoder-only-PLM" class="headerlink" title="1.Encoder-only PLM"></a>1.Encoder-only PLM</h2><h3 id="1-1-BERT"><a href="#1-1-BERT" class="headerlink" title="1.1 BERT"></a><font color="black" size="5">1.1 BERT</font></h3><font color="black" size="5">&emsp;&emsp;**BERT，全名为 Bidirectional Encoder Representations from Transformers**，是由 Google 团队在 2018年发布的预训练语言模型。该模型发布于论文《BERT: Pre-training of Deep Bidirectional Transformers for Language Understanding》，实现了包括 GLUE、MultiNLI 等七个自然语言处理评测任务的最优性能（State Of The Art，SOTA），堪称里程碑式的成果。自 BERT 推出以来，预训练+微调的模式开始成为自然语言处理任务的主流，不仅 BERT 自身在不断更新迭代提升模型性能，也出现了如 MacBERT、BART 等基于 BERT 进行优化提升的模型。可以说，BERT 是自然语言处理的一个阶段性成果，标志着各种自然语言处理任务的重大进展以及预训练模型的统治地位建立，一直到 LLM 的诞生，NLP 领域的主导地位才从 BERT 系模型进行迁移。即使在 LLM 时代，要深入理解 LLM 与 NLP，BERT 也是无法绕过的一环。</font><h4 id="1-1-1-思想沿承"><a href="#1-1-1-思想沿承" class="headerlink" title="1.1.1 思想沿承"></a><font color="black" size="5">1.1.1 思想沿承</font></h4><font color="black" size="5">&emsp;&emsp;BERT 是一个统一了多种思想的预训练模型。其所沿承的核心思想包括：</font><font color="black" size="5">&emsp;&emsp;1️⃣Transformer 架构。正如我们在上一章所介绍的，在 2017年发表的《Attention is All You Need》论文提出了完全使用注意力机制而抛弃 RNN、LSTM 结构的 Transformer 模型，带来了新的模型架构。BERT 正沿承了 Transformer 的思想，在 Transformer 的模型基座上进行优化，通过将 Encoder 结构进行堆叠，扩大模型参数，打造了在 NLU 任务上独居天分的模型架构；</font><font color="black" size="5">&emsp;&emsp;2️⃣预训练+微调范式。同样在 2018年，ELMo 的诞生标志着预训练+微调范式的诞生。ELMo 模型基于双向 LSTM 架构，在训练数据上基于语言模型进行预训练，再针对下游任务进行微调，表现出了更加优越的性能，将 NLP 领域导向预训练+微调的研究思路。而 BERT 也采用了该范式，并通过将模型架构调整为 Transformer，引入更适合文本理解、能捕捉深层双向语义关系的预训练任务 MLM，将预训练-微调范式推向了高潮。</font><h4 id="1-1-2-模型架构——Encoder-Only"><a href="#1-1-2-模型架构——Encoder-Only" class="headerlink" title="1.1.2 模型架构——Encoder Only"></a><font color="black" size="5">1.1.2 模型架构——Encoder Only</font></h4><font color="black" size="5">&emsp;&emsp;BERT 的模型架构是取了 Transformer 的 Encoder 部分堆叠而成，其主要结构如图3.1所示：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009153009863.png" alt="image-20251009153009863"></p><font color="black" size="5">&emsp;&emsp;BERT 是针对于 NLU 任务打造的预训练模型，其输入一般是文本序列，而输出一般是 Label，例如情感分类的积极、消极 Label。但是，正如 Transformer 是一个 Seq2Seq 模型，使用 Encoder 堆叠而成的 BERT 本质上也是一个 Seq2Seq 模型，只是没有加入对特定任务的 Decoder，因此，为适配各种 NLU 任务，在模型的最顶层加入了一个分类头 prediction_heads，用于将多维度的隐藏状态通过线性层转换到分类维度（例如，如果一共有两个类别，prediction_heads 输出的就是两维向量）。</font><font color="black" size="5">&emsp;&emsp;模型整体既是由 Embedding、Encoder 加上 prediction_heads 组成：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009153239440.png" alt="image-20251009153239440"></p><font color="black" size="5">&emsp;&emsp;输入的文本序列会首先通过 tokenizer（分词器） 转化成 input_ids（基本每一个模型在 tokenizer 的操作都类似，可以参考 Transformer 的 tokenizer 机制，后文不再赘述），然后进入 Embedding 层转化为特定维度的 hidden_states，再经过 Encoder 块。Encoder 块中是堆叠起来的 N 层 Encoder Layer，BERT 有两种规模的模型，分别是 base 版本（12层 Encoder Layer，768 的隐藏层维度，总参数量 110M），large 版本（24层 Encoder Layer，1024 的隐藏层维度，总参数量 340M）。通过Encoder 编码之后的最顶层 hidden_states 最后经过 prediction_heads 就得到了最后的类别概率，经过 Softmax 计算就可以计算出模型预测的类别。</font><blockquote><font color="black" size="5">BERT 采用 WordPiece 作为分词方法。WordPiece 是一种基于统计的子词切分算法，其核心在于将单词拆解为子词（例如，"playing" -> ["play", "##ing"]）。其合并操作的依据是最大化语言模型的似然度。对于中文等非空格分隔的语言，通常将单个汉字作为原子分词单位（token）处理。</font></blockquote><font color="black" size="5">&emsp;&emsp;prediction_heads 其实就是线性层加上激活函数，一般而言，最后一个线性层的输出维度和任务的类别数相等，如图3.3所示：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009153524931.png" alt="image-20251009153524931"></p><font color="black" size="5">&emsp;&emsp;而每一层 Encoder Layer 都是和 Transformer 中的 Encoder Layer 结构类似的层，如图3.4所示：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009153558172.png" alt="image-20251009153558172"></p><font color="black" size="5">&emsp;&emsp;如图3.5所示，已经通过 Embedding 层映射的 hidden_states 进入核心的 attention 机制，然后通过残差连接的机制和原输入相加，再经过一层 Intermediate 层得到最终输出。Intermediate 层是 BERT 的特殊称呼，其实就是一个线性层加上激活函数：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009153934916.png" alt="image-20251009153934916"></p><font color="black" size="5">&emsp;&emsp;注意，BERT 所使用的激活函数是 GELU 函数，全名为高斯误差线性单元激活函数，这也是自 BERT 才开始被普遍关注的激活函数。GELU 的计算方式为：</font><font color="black" size="5">&emsp;&emsp;$GELU(x) = 0.5x(1+tanh(\sqrt{\frac{2}{\pi}}))(x + 0.044715x^3)$</font><font color="black" size="5">&emsp;&emsp;GELU 的核心思路为将随机正则的思想引入激活函数，通过输入自身的概率分布，来决定抛弃还是保留自身的神经元。关于 GELU 的原理与核心思路，此处不再赘述，有兴趣的读者可以自行学习。</font><font color="black" size="5">&emsp;&emsp;BERT 的 注意力机制和 Transformer 中 Encoder 的 自注意力机制几乎完全一致，但是 BERT 将相对位置编码融合在了注意力机制中，将相对位置编码同样视为可训练的权重参数，如图3.6所示：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009154315182.png" alt="image-20251009154315182"></p><font color="black" size="5">&emsp;&emsp;如图，BERT 的注意力计算过程和 Transformer 的唯一差异在于，在完成注意力分数的计算之后，先通过 Position Embedding 层来融入相对位置信息。这里的 Position Embedding 层，其实就是一层线性矩阵。通过可训练的参数来拟合相对位置，相对而言比 Transformer 使用的绝对位置编码 Sinusoidal 能够拟合更丰富的相对位置信息，但是，这样也增加了不少模型参数，同时完全无法处理超过模型训练长度的输入（例如，对 BERT 而言能处理的最大上下文长度是 512 个 token）。</font><font color="black" size="5">&emsp;&emsp;注：原始 BERT（即论文提出）使用和 Transformer 一致的绝对位置编码，后续改进（包括 BERT 的各种变体）使用了上述相对位置编码，为帮助读者了解更全面的模型结构设计，此处选择了改进版 BERT。</font><font color="black" size="5">&emsp;&emsp;可以看出，BERT 的模型架构既是建立在 Transformer 的 Encoder 之上的，这也是为什么说 BERT 沿承了 Transformer 的思想。</font><h4 id="1-1-3-预训练任务——MLM-NSP"><a href="#1-1-3-预训练任务——MLM-NSP" class="headerlink" title="1.1.3 预训练任务——MLM + NSP"></a><font color="black" size="5">1.1.3 预训练任务——MLM + NSP</font></h4><font color="black" size="5">&emsp;&emsp;相较于基本沿承 Transformer 的模型架构，BERT 更大的创新点在于其提出的两个新的预训练任务上——**MLM 和 NSP（Next Sentence Prediction，下一句预测）**。预训练-微调范式的核心优势在于，通过将预训练和微调分离，完成一次预训练的模型可以仅通过微调应用在几乎所有下游任务上，只要微调的成本较低，即使预训练成本是之前的数倍甚至数十倍，模型仍然有更大的应用价值。因此，可以进一步扩大模型参数和预训练数据量，使用海量的预训练语料来让模型拟合潜在语义与底层知识，从而让模型通过长时间、大规模的预训练获得强大的语言理解和生成能力。</font><font color="black" size="5">&emsp;&emsp;因此，预训练数据的核心要求即是需要极大的数据规模（数亿 token）。毫无疑问，通过人工标注产出的全监督数据很难达到这个规模。因此，预训练数据一定是从无监督的语料中获取。这也是为什么传统的预训练任务都是 LM 的原因——LM 使用上文预测下文的方式可以直接应用到任何文本中，对于任意文本，我们只需要将下文遮蔽将上文输入模型要求其预测就可以实现 LM 训练，因此互联网上所有文本语料都可以被用于预训练。</font><font color="black" size="5">&emsp;&emsp;但是，LM 预训练任务的一大缺陷在于，其直接拟合从左到右的语义关系，但忽略了双向的语义关系。虽然 Transformer 中通过位置编码表征了文本序列中的位置信息，但这和直接拟合双向语义关系还是有本质区别。例如，BiLSTM（双向 LSTM 模型）在语义表征上就往往优于 LSTM 模型，就是因为 BiLSTM 通过双向的 LSTM 拟合了双向语义关系。因此，有没有一种预训练任务，能够既利用海量无监督语料，又能够训练模型拟合双向语义关系的能力？</font><font color="black" size="5">&emsp;&emsp;基于这一思想，Jacob 等学者提出了 **MLM，也就是掩码语言模型作为新的预训练任务**。相较于模拟人类写作的 LM，MLM 模拟的是“完形填空”。MLM 的思路也很简单，在一个文本序列中随机遮蔽部分 token，然后将所有未被遮蔽的 token 输入模型，要求模型根据输入预测被遮蔽的 token。例如，输入和输出可以是：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：I &lt;MASK&gt; you because you are &lt;MASK&gt;</span><br><span class="line">输出：&lt;MASK&gt; - love; &lt;MASK&gt; - wonderful</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;由于模型可以利用被遮蔽的 token 的上文和下文一起理解语义来预测被遮蔽的 token，因此通过这样的任务，模型可以拟合双向语义，也就能够更好地实现文本的理解。同样，MLM 任务无需对文本进行任何人为的标注，只需要对文本进行随机遮蔽即可，因此也可以利用互联网所有文本语料实现预训练。例如，BERT 的预训练就使用了足足 3300M 单词的语料。</font><font color="black" size="5">&emsp;&emsp;不过，MLM 也存在其固有缺陷。LM 任务模拟了人自然创作的过程，其训练和下游任务是完全一致的，也就是说，训练时是根据上文预测下文，下游任务微调和推理时也同样如此。但是 MLM 不同，在下游任务微调和推理时，其实是不存在我们人工加入的 `<MASK>` 的，我们会直接通过原文本得到对应的隐藏状态再根据下游任务进入分类器或其他组件。预训练和微调的不一致，会极大程度影响模型在下游任务微调的性能。针对这一问题，作者对 MLM 的策略进行了改进。</MASK></font><font color="black" size="5">&emsp;&emsp;在具体进行 MLM 训练时，会随机选择训练语料中 15% 的 token 用于遮蔽。但是这 15% 的 token 并非全部被遮蔽为 `<MASK>`，而是有 80% 的概率被遮蔽，10% 的概率被替换为任意一个 token，还有 10% 的概率保持不变。其中 10% 保持不变就是为了消除预训练和微调的不一致，而 10% 的随机替换核心意义在于迫使模型保持对上下文信息的学习。因为如果全部遮蔽的话，模型仅需要处理被遮蔽的位置，从而仅学习要预测的 token 而丢失了对上下文的学习。通过引入部分随机 token，模型无法确定需要预测的 token，从而被迫保持每一个 token 的上下文表征分布，从而具备了对句子的特征表示能力。且由于随机 token 的概率很低，其并不会影响模型实质的语言理解能力。</MASK></font><font color="black" size="5">&emsp;&emsp;除去 MLM，BERT 还提出了另外一个预训练任务——**NSP，即下一个句子预测。NSP 的核心思想是针对句级的 NLU 任务，例如问答匹配、自然语言推理等。**问答匹配是指，输入一个问题和若干个回答，要求模型找出问题的真正回答；自然语言推理是指，输入一个前提和一个推理，判断推理是否是符合前提的。这样的任务都需要模型在句级去拟合关系，判断两个句子之间的关系，而不仅是 MLM 在 token 级拟合的语义关系。因此，BERT 提出了 NSP 任务来训练模型在句级的语义关系拟合。</font><font color="black" size="5">&emsp;&emsp;NSP 任务的核心思路是要求模型判断一个句对的两个句子是否是连续的上下文。例如，输入和输入可以是：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">输入：</span><br><span class="line">    Sentence A：I love you.</span><br><span class="line">    Sentence B: Because you are wonderful.</span><br><span class="line">输出：</span><br><span class="line">    <span class="number">1</span>（是连续上下文）</span><br><span class="line"></span><br><span class="line">输入：</span><br><span class="line">    Sentence A：I love you.</span><br><span class="line">    Sentence B: Because today<span class="string">&#x27;s dinner is so nice.</span></span><br><span class="line"><span class="string">输出：</span></span><br><span class="line"><span class="string">    0（不是连续上下文）</span></span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;通过要求模型判断句对关系，从而迫使模型拟合句子之间的关系，来适配句级的 NLU 任务。同样，由于 NSP 的正样本可以从无监督语料中随机抽取任意连续的句子，而负样本可以对句子打乱后随机抽取（只需要保证不要抽取到原本就连续的句子就行），因此也可以具有几乎无限量的训练数据。</font><font color="black" size="5">&emsp;&emsp;在具体预训练时，BERT 使用了 800M 的 BooksCorpus 语料和 2500M 的英文维基百科语料，90% 的数据使用 128 的上下文长度训练，剩余 10% 的数据使用 512 作为上下文长度进行预训练，总共约训练了 3.3B token。其训练的超参数也是值得关注的，BERT 的训练语料共有 13GB 大小，其在 256 的 batch size 上训练了 1M 步（40 个 Epoch）。而相较而言，LLM 一般都只会训练一个 Epoch，且使用远大于 256 的 batch size。</font><font color="black" size="5">&emsp;&emsp;可以看到，相比于传统的非预训练模型，其训练的数据量有指数级增长。当然，更海量的训练数据需要更大成本的算力，BERT 的 Base 版本和 Large 版本分别使用了 16块 TPU 和 64块 TPU 训练了 4天才完成。</font><h4 id="1-1-4-下游任务微调"><a href="#1-1-4-下游任务微调" class="headerlink" title="1.1.4 下游任务微调"></a><font color="black" size="5">1.1.4 下游任务微调</font></h4><font color="black" size="5">&emsp;&emsp;作为 NLP 领域里程碑式的成果，BERT 的一个重大意义就是正式确立了预训练-微调的两阶段思想，即在海量无监督语料上进行预训练来获得通用的文本理解与生成能力，再在对应的下游任务上进行微调。该种思想的一个重点在于，预训练得到的强大能力能否通过低成本的微调快速迁移到对应的下游任务上。</font><font color="black" size="5">&emsp;&emsp;</font><font color="black" size="5">&emsp;&emsp;针对这一点，BERT 设计了更通用的输入和输出层来适配多任务下的迁移学习。对每一个输入的文本序列，BERT 会在其首部加入一个特殊 token `<CLS>`。在后续编码中，该 token 代表的即是整句的状态，也就是句级的语义表征。在进行 NSP 预训练时，就使用了该 token 对应的特征向量来作为最后分类器的输入。</CLS></font><font color="black" size="5">&emsp;&emsp;在完成预训练后，针对每一个下游任务，只需要使用一定量的全监督人工标注数据，对预训练的 BERT 在该任务上进行微调即可。所谓微调，其实和训练时更新模型参数的策略一致，只不过在特定的任务、更少的训练数据、更小的 batch_size 上进行训练，更新参数的幅度更小。对于绝大部分下游任务，都可以直接使用 BERT 的输出。例如，对于文本分类任务，可以直接修改模型结构中的 prediction_heads 最后的分类头即可。对于序列标注等任务，可以集成 BERT 多层的隐含层向量再输出最后的标注结果。对于文本生成任务，也同样可以取 Encoder 的输出直接解码得到最终生成结果。因此，BERT 可以非常高效地应用于多种 NLP 任务。</font><font color="black" size="5">&emsp;&emsp;BERT 一经提出，直接在 NLP 11个赛道上取得 SOTA 效果，成为 NLU 方向上当之无愧的霸主，后续若干在 NLU 任务上取得更好效果的模型都是在 BERT 基础上改进得到的。直至 LLM 时代，BERT 也仍然能在很多标注数据丰富的 NLU 任务上达到最优效果，事实上，对于某些特定、训练数据丰富且强调高吞吐的任务，BERT 比 LLM 更具有可用性。</font><h3 id="1-2-RoBERTa"><a href="#1-2-RoBERTa" class="headerlink" title="1.2 RoBERTa"></a>1.2 RoBERTa</h3><font color="black" size="5">&emsp;&emsp;BERT 作为 NLP 划时代的杰作，同时在多个榜单上取得 SOTA 效果，也带动整个 NLP 领域向预训练模型方向迁移。以 BERT 为基础，在多个方向上进行优化，还涌现了一大批效果优异的 Encoder-Only 预训练模型。它们大都有和 BERT 类似或完全一致的模型结构，在训练数据、预训练任务、训练参数等方面上进行了优化，以取得能力更强大、在下游任务上表现更亮眼的预训练模型。其中之一即是同样由 Facebook 发布的 RoBERTa。</font><font color="black" size="5">&emsp;&emsp;前面我们说过，预训练-微调的一个核心优势在于可以使用远大于之前训练数据的海量无监督语料进行预训练。因为在传统的深度学习范式中，对每一个任务，我们需要从零训练一个模型，那么就无法使用太大的模型参数，否则需要极大规模的有监督数据才能让模型较好地拟合，成本太大。但在预训练-微调范式，我们在预训练阶段可以使用尽可能大量的训练数据，只需要一次预训练好的模型，后续在每一个下游任务上通过少量有监督数据微调即可。而 BERT 就使用了 13GB（3.3B token）的数据进行预训练，这相较于传统 NLP 来说是一个极其巨大的数据规模了。</font><font color="black" size="5">&emsp;&emsp;但是，13GB 的预训练数据是否让 BERT 达到了充分的拟合呢？如果我们使用更多预训练语料，是否可以进一步增强模型性能？更多的，BERT 所选用的预训练任务、训练超参数是否是最优的？RoBERTa 应运而生。</font><h4 id="1-2-1-优化一：去掉NSP预训练任务"><a href="#1-2-1-优化一：去掉NSP预训练任务" class="headerlink" title="1.2.1 优化一：去掉NSP预训练任务"></a><font color="black" size="5">1.2.1 优化一：去掉NSP预训练任务</font></h4><font color="black" size="5">&emsp;&emsp;RoBERTa 的模型架构与 BERT 完全一致，也就是使用了 BERT-large（24层 Encoder Layer，1024 的隐藏层维度，总参数量 340M）的模型参数。在预训练任务上，有学者质疑 NSP 任务并不能提高模型性能，因为其太过简单，加入到预训练中并不能使下游任务微调时明显受益，甚至会带来负面效果。RoBERTa 设置了四个实验组：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="number">1.</span> 段落构建的 MLM + NSP：BERT 原始预训练任务，输入是一对片段，每个片段包括多个句子，来构造 NSP 任务；</span><br><span class="line"><span class="number">2.</span> 文档对构建的 MLM + NSP：一个输入构建一对句子，通过增大 batch 来和原始输入达到 token 等同；</span><br><span class="line"><span class="number">3.</span> 跨越文档的 MLM：去掉 NSP 任务，一个输入为从一个或多个文档中连续采样的完整句子，为使输入达到最大长度（<span class="number">512</span>），可能一个输入会包括多个文档；</span><br><span class="line"><span class="number">4.</span> 单文档的 MLM：去掉 NSP 任务，且限制一个输入只能从一个文档中采样，同样通过增大 batch 来和原始输入达到 token 等同</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;实验结果证明，后两组显著优于前两组，且单文档的 MLM 组在下游任务上微调时性能最佳。因此，RoBERTa 在预训练中去掉了 NSP，只使用 MLM 任务。</font><font color="black" size="5">&emsp;&emsp;同时，RoBERTa 对 MLM 任务本身也做出了改进。在 BERT 中，Mask 的操作是在数据处理的阶段完成的，因此后期预训练时同一个 sample 待预测的 `<MASK>` 总是一致的。由于 BERT 共训练了 40 个 Epoch，为使模型的训练数据更加广泛，BERT 将数据进行了四次随机 Mask，也就是每 10个 Epoch 模型训练的数据是完全一致的。而 RoBERTa 将 Mask 操作放到了训练阶段，也就是动态遮蔽策略，从而让每一个 Epoch 的训练数据 Mask 的位置都不一致。在实验中，动态遮蔽仅有很微弱的优势优于静态遮蔽，但由于动态遮蔽更高效、易于实现，后续 MLM 任务基本都使用了动态遮蔽。</MASK></font><h4 id="1-2-2-优化二：更大规模的预训练数据和预训练步长"><a href="#1-2-2-优化二：更大规模的预训练数据和预训练步长" class="headerlink" title="1.2.2 优化二：更大规模的预训练数据和预训练步长"></a><font color="black" size="5">1.2.2 优化二：更大规模的预训练数据和预训练步长</font></h4><font color="black" size="5">&emsp;&emsp;RoBERTa 使用了更大量的无监督语料进行预训练，除去 BERT 所使用的 BookCorpus 和英文维基百科外，还使用了 CC-NEWS（CommonCrawl 数据集新闻领域的英文部分）、OPENWEBTEXT（英文网页）、STORIES（CommonCrawl 数据集故事风格子集），共计 160GB 的数据，十倍于 BERT。</font><font color="black" size="5">&emsp;&emsp;</font><font color="black" size="5">&emsp;&emsp;同时，RoBERTa 认为更大的 batch size 既可以提高优化速度，也可以提高任务结束性能。因此，实验在 8K 的 batch size（对比 BERT 的 batch size 为 256）下训练 31K Step，也就是总训练 token 数和 BERT 一样是 3.3B 时，模型性能更好，从而证明了大 batch size 的意义。在此基础上，RoBERTa 一共训练了 500K Step（约合 66个 Epoch）。同时，RoBERTa 不再采用 BERT 在 256 长度上进行大部分训练再在 512 长度上完成训练的策略，而是全部在 512 长度上进行训练。</font><font color="black" size="5">&emsp;&emsp;当然，更大的预训练数据、更长的序列长度和更多的训练 Epoch，需要预训练阶段更多的算力资源。训练一个 RoBERTa，Meta 使用了 1024 块 V100（32GB 显存）训练了一天。</font><h4 id="1-2-3-优化三：更大的-bpe-词表"><a href="#1-2-3-优化三：更大的-bpe-词表" class="headerlink" title="1.2.3 优化三：更大的 bpe 词表"></a><font color="black" size="5">1.2.3 优化三：更大的 bpe 词表</font></h4><font color="black" size="5">&emsp;&emsp;与 BERT 使用的 WordPiece 算法不同，RoBERTa 使用了 BPE 作为 Tokenizer 的编码策略。BPE，即 Byte Pair Encoding，字节对编码，是指以子词对作为分词的单位。例如，对“Hello World”这句话，可能会切分为“Hel，lo，Wor，ld”四个子词对。而对于以字为基本单位的中文，一般会按照字节编码进行切分。例如，在 UTF-8 编码中，“我”会被编码为“E68891”，那么在 BPE 中可能就会切分成“E68”，“891”两个字词对。</font><font color="black" size="5">&emsp;&emsp;一般来说，BPE 编码的词典越大，编码效果越好。当然，由于 Embedding 层就是把 token 从词典空间映射到隐藏空间（也就是说 Embedding 的形状为 (vocab_size, hidden_size)，越大的词表也会带来模型参数的增加。</font><font color="black" size="5">&emsp;&emsp;BERT 原始的 BPE 词表大小为 30K，RoBERTa 选择了 50K 大小的词表来优化模型的编码能力。</font><font color="black" size="5">&emsp;&emsp;通过上述三个部分的优化，RoBERTa 成功地在 BERT 架构的基础上刷新了多个下游任务的 SOTA，也一度成为 BERT 系模型最热门的预训练模型。同时，RoBERTa 的成功也证明了更大的预训练数据、更大的预训练步长的重要意义，这也是 LLM 诞生的基础之一。</font><h3 id="1-3-ALBERT"><a href="#1-3-ALBERT" class="headerlink" title="1.3 ALBERT"></a><font color="black" size="5">1.3 ALBERT</font></h3><font color="black" size="5">&emsp;&emsp;在 BERT 的基础上，RoBERTa 进一步探究了更大规模预训练的作用。同样是基于 BERT 架构进行优化的 ALBERT 模型，则从是否能够减小模型参数保持模型能力的角度展开了探究。通过对模型结构进行优化并对 NSP 预训练任务进行改进，ALBERT 成功地以更小规模的参数实现了超越 BERT 的能力。虽然 ALBERT 所提出的一些改进思想并没有在后续研究中被广泛采用，但其降低模型参数的方法及提出的新预训练任务 SOP 仍然对 NLP 领域提供了重要的参考意义。</font><h4 id="1-3-1-优化一：将Embedding参数进行分解"><a href="#1-3-1-优化一：将Embedding参数进行分解" class="headerlink" title="1.3.1 优化一：将Embedding参数进行分解"></a><font color="black" size="5">1.3.1 优化一：将Embedding参数进行分解</font></h4><font color="black" size="5">&emsp;&emsp;BERT 等预训练模型具有远超传统神经网络的参数量，如前所述，BERT-large 具有 24层 Encoder Layer，1024 的隐藏层维度，总共参数量达 340M。而这其中，Embedding 层的参数矩阵维度为 $V∗H$，此处的 V 为词表大小 30K，H 即为隐藏层大小 1024，也就是 Embedding 层参数达到了 30M。而这样的设置还会带来一个更大的问题，即 Google 探索尝试搭建更宽（也就是隐藏层维度更大）的模型时发现，隐藏层维度的增加会带来 Embedding 层参数的巨大上升，如果把隐藏层维度增加到 2048，Embedding 层参数就会膨胀到 61M，这无疑是极大增加了模型的计算开销。</font><font color="black" size="5">&emsp;&emsp;而从另一个角度看，Embedding 层输出的向量是我们对文本 token 的稠密向量表示，从 Word2Vec 的成功经验来看，这种词向量并不需要很大的维度，Word2Vec 仅使用了 100维大小就取得了很好的效果。因此，Embedding 层的输出也许不需要和隐藏层大小一致。</font><font color="black" size="5">&emsp;&emsp;</font><font color="black" size="5">&emsp;&emsp;因此，ALBERT 对 Embedding 层的参数矩阵进行了分解，让 Embedding 层的输出维度和隐藏层维度解绑，也就是在 Embedding 层的后面加入一个线性矩阵进行维度变换。ALBERT 设置了 Embedding 层的输出为 128，因此在 Embedding 层后面加入了一个 128∗1024的线性矩阵来将 Embedding 层的输出再升维到隐藏层大小。也就是说，Embedding 层的参数从 $V∗H$ 降低到了 $V∗E+E∗H$，当 E 的大小远小于 H 时，该方法对 Embedding 层参数的优化就会很明显。</font><h4 id="1-3-2-优化二：跨层进行参数共享"><a href="#1-3-2-优化二：跨层进行参数共享" class="headerlink" title="1.3.2 优化二：跨层进行参数共享"></a><font color="black" size="5">1.3.2 优化二：跨层进行参数共享</font></h4><font color="black" size="5">&emsp;&emsp;通过对 BERT 的参数进行分析，ALBERT 发现各个 Encoder 层的参数出现高度一致的情况。由于 24个 Encoder 层带来了巨大的模型参数，因此，ALBERT 提出，可以让各个 Encoder 层共享模型参数，来减少模型的参数量。</font><font color="black" size="5">&emsp;&emsp;在具体实现上，其实就是 ALBERT 仅初始化了一个 Encoder 层。在计算过程中，仍然会进行 24次计算，但是每一次计算都是经过这一个 Encoder 层。因此，虽然是 24个 Encoder 计算的模型，但只有一层 Encoder 参数，从而大大降低了模型参数量。在这样的情况下，就可以极大程度地扩大隐藏层维度，实现一个更宽但参数量更小的模型。ALBERT 通过实验证明，相较于 334M 的 BERT，同样是 24层 Encoder 但将隐藏层维度设为 2048 的 ALBERT（xlarge 版本）仅有 59M 的参数量，但在具体效果上还要更优于 BERT。</font><font color="black" size="5">&emsp;&emsp;但是，上述优化虽然极大程度减小了模型参数量并且还提高了模型效果，却也存在着明显的不足。虽然 ALBERT 的参数量远小于 BERT，但训练效率却只略微优于 BERT，因为在模型的设置中，虽然各层共享权重，但计算时仍然要通过 24次 Encoder Layer 的计算，也就是说训练和推理时的速度相较 BERT 还会更慢。这也是 ALBERT 最终没能取代 BERT 的一个重要原因。</font><h4 id="1-3-3-优化三：提出SOP预训练任务"><a href="#1-3-3-优化三：提出SOP预训练任务" class="headerlink" title="1.3.3 优化三：提出SOP预训练任务"></a><font color="black" size="5">1.3.3 优化三：提出SOP预训练任务</font></h4><font color="black" size="5">&emsp;&emsp;类似于 RoBERTa，ALBERT 也同样认为 NSP 任务过于简单，在预训练中无法对模型效果的提升带来显著影响。但是不同于 RoBERTa 选择直接去掉 NSP，ALBERT 选择改进 NSP，增加其难度，来优化模型的预训练</font><font color="black" size="5">&emsp;&emsp;在传统的 NSP 任务中，正例是由两个连续句子组成的句对，而负例则是从任意两篇文档中抽取出的句对，模型可以较容易地判断正负例，并不能很好地学习深度语义。而 SOP 任务提出的改进是，正例同样由两个连续句子组成，但负例是将这两个的顺序反过来。也就是说，模型不仅要拟合两个句子之间的关系，更要学习其顺序关系，这样就大大提升了预训练的难度。例如，相较于我们在上文中提出的 NSP 任务的示例，SOP 任务的示例形如：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">输入：</span><br><span class="line">    Sentence A：I love you.</span><br><span class="line">    Sentence B: Because you are wonderful.</span><br><span class="line">输出：</span><br><span class="line">    <span class="number">1</span>（正样本）</span><br><span class="line"></span><br><span class="line">输入：</span><br><span class="line">    Sentence A：Because you are wonderful.</span><br><span class="line">    Sentence B: I love you.</span><br><span class="line">输出：</span><br><span class="line">    <span class="number">0</span>（负样本）</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;ALBERT 通过实验证明，SOP 预训练任务对模型效果有显著提升。使用 MLM + SOP 预训练的模型效果优于仅使用 MLM 预训练的模型更优于使用 MLM + NSP 预训练的模型。</font><font color="black" size="5">&emsp;&emsp;通过上述三点优化，ALBERT 成功地以更小的参数实现了更强的性能，虽然由于其架构带来的训练、推理效率降低限制了模型的进一步发展，但打造更宽的模型这一思路仍然为众多更强大的模型提供了参考价值。</font><font color="black" size="5">&emsp;&emsp;作为预训练时代的 NLP 王者，BERT 及 BERT 系模型在多个 NLP 任务上扮演了极其重要的角色。除去上文介绍过的 RoBERTa、ALBERT 外，还有许多从其他更高角度对 BERT 进行优化的后起之秀，包括进一步改进了预训练任务的 ERNIE、对 BERT 进行蒸馏的小模型 DistilBERT、主打多语言任务的 XLM 等，本文就不再一一赘述。以 BERT 为代表的 Encoder-Only 架构并非 Transformer 的唯一变种，接下来，我们将介绍 Transformer 的另一种主流架构，与原始 Transformer 更相似、以 T5 为代表的 Encoder-Decoder 架构。</font><h2 id="2-Encoder-Decoder-PLM"><a href="#2-Encoder-Decoder-PLM" class="headerlink" title="2.Encoder-Decoder PLM"></a>2.Encoder-Decoder PLM</h2><font color="black" size="5">&emsp;&emsp;在上一节，我们学习了 Encoder-Only 结构的模型，主要介绍了 BERT 的模型架构、预训练任务和下游任务微调。BERT 是一个基于 Transformer 的 Encoder-Only 模型，通过预训练任务 MLM 和 NSP 来学习文本的双向语义关系，从而在下游任务中取得了优异的性能。但是，BERT 也存在一些问题，例如 MLM 任务和下游任务微调的不一致性，以及无法处理超过模型训练长度的输入等问题。为了解决这些问题，研究者们提出了 Encoder-Decoder 模型，通过引入 Decoder 部分来解决这些问题，同时也为 NLP 领域带来了新的思路和方法。</font><h3 id="2-1-T5"><a href="#2-1-T5" class="headerlink" title="2.1 T5"></a><font color="black" size="5">2.1 T5</font></h3><font color="black" size="5">&emsp;&emsp;**T5（Text-To-Text Transfer Transformer）**是由 Google 提出的一种预训练语言模型，通过将所有 NLP 任务统一表示为文本到文本的转换问题，大大简化了模型设计和任务处理。T5 基于 Transformer 架构，包含编码器和解码器两个部分，使用自注意力机制和多头注意力捕捉全局依赖关系，利用相对位置编码处理长序列中的位置信息，并在每层中包含前馈神经网络进一步处理特征。</font><font color="black" size="5">&emsp;&emsp;</font><font color="black" size="5">&emsp;&emsp;T5 的大一统思想将不同的 NLP 任务如文本分类、问答、翻译等统一表示为输入文本到输出文本的转换，这种方法简化了模型设计、参数共享和训练过程，提高了模型的泛化能力和效率。通过这种统一处理方式，T5不仅减少了任务特定的模型调试工作，还能够使用相同的数据处理和训练框架，极大地提升了多任务学习的性能和应用的便捷性。接下来我们将会从模型结构、预训练任务和大一统思想三个方面来介绍 T5 模型。</font><h4 id="2-1-1-模型结构：Encoder-Decoder"><a href="#2-1-1-模型结构：Encoder-Decoder" class="headerlink" title="2.1.1 模型结构：Encoder-Decoder"></a><font color="black" size="5">2.1.1 模型结构：Encoder-Decoder</font></h4><font color="black" size="5">&emsp;&emsp;BERT 采用了 Encoder-Only 结构，只包含编码器部分；而 GPT 采用了 Decoder-Only 结构，只包含解码器部分。T5 则采用了 Encoder-Decoder 结构，其中编码器和解码器都是基于 Transformer 架构设计。编码器用于处理输入文本，解码器用于生成输出文本。编码器和解码器之间通过注意力机制进行信息交互，从而实现输入文本到输出文本的转换。其主要结构如图3.7所示：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009165724577.png" alt="image-20251009165724577"></p><font color="black" size="5">&emsp;&emsp;如图3.8所示，从整体来看 T5 的模型结构包括 Tokenizer 部分和 Transformer 部分。Tokenizer 部分主要负责将输入文本转换为模型可接受的输入格式，包括分词、编码等操作。Transformer 部分又分为 EncoderLayers 和 DecoderLayers 两部分，他们分别由一个个小的 Block组成，每个 Block 包含了多头注意力机制、前馈神经网络和 Norm 层。Block 的设计可以使模型更加灵活，像乐高一样可以根据任务的复杂程度和数据集的大小来调整 Block 的数量和层数。</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009165836470.png" alt="image-20251009165836470"></p><font color="black" size="5">&emsp;&emsp;T5 模型的 Encoder 和 Decoder 部分都是基于 Transformer 架构设计的，主要包括 Self-Attention 和前馈神经网络两种结构。Self-Attention 用于捕捉输入序列中的全局依赖关系，前馈神经网络用于处理特征的非线性变换。</font><font color="black" size="5">&emsp;&emsp;和 Encoder 不一样的是，在 Decoder 中还包含了 Encoder-Decoder Attention 结构，用于捕捉输入和输出序列之间的依赖关系。这两种 Attention 结构几乎完全一致，只有在位置编码和 Mask 机制上有所不同。如图3.9所示，Encoder 和 Decoder 的结构如下：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009171249012.png" alt="image-20251009171249012"></p><font color="black" size="5">&emsp;&emsp;T5 的 Self-Attention 机制和 BERT 的 Attention 机制是一样的，都是基于 Self-Attention 机制设计的。Self-Attention 机制是一种全局依赖关系建模方法，通过计算 Query、Key 和 Value 之间的相似度来捕捉输入序列中的全局依赖关系。Encoder-Decoder Attention 仅仅在位置编码和 Mask 机制上有所不同，主要是为了区分输入和输出序列。如图3.10所示，Self-Attention 结构如下：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009171404228.png" alt="image-20251009171404228"></p><font color="black" size="5">&emsp;&emsp;与原始 Transformer 模型不同，T5 模型的LayerNorm 采用了 RMSNorm，通过计算每个神经元的均方根（Root Mean Square）来归一化每个隐藏层的激活值。RMSNorm 的参数设置与Layer Normalization 相比更简单，只有一个可学参数，可以更好地适应不同的任务和数据集。RMSNorm函数可以用以下数学公式表示：</font><font color="black" size="5">&emsp;&emsp;$RMSNorm(x)=\frac{x}{\sqrt{\frac{1}{n}\sum_{i=1}^nx^2_i+\epsilon}}\cdot \gamma$</font><font color="black" size="5">&emsp;&emsp;其中：$x_i$ 是输入向量的第 $i$个元素， $\gamma$ 是可学习的缩放参数， n是输入向量的维度数量，$\epsilon$是一个小常数，用于数值稳定性（以避免除以零的情况）</font><font color="black" size="5">&emsp;&emsp;这种归一化有助于通过确保权重的规模不会变得过大或过小来稳定学习过程，这在具有许多层的深度学习模型中特别有用。</font><font color="black" size="5">&emsp;&emsp;</font><h4 id="2-1-2-预训练任务"><a href="#2-1-2-预训练任务" class="headerlink" title="2.1.2 预训练任务"></a><font color="black" size="5">2.1.2 预训练任务</font></h4><font color="black" size="5">&emsp;&emsp;T5 模型的预训练任务是一个关键的组成部分，它能使模型能够学习到丰富的语言表示，语言表示能力可以在后续的微调过程中被迁移到各种下游任务。训练所使用的数据集是一个大规模的文本数据集，包含了各种各样的文本数据，如维基百科、新闻、书籍等等。对数据经过细致的处理后，生成了用于训练的750GB 的数据集 C4，且已在 TensorflowData 中开源。</font><font color="black" size="5">&emsp;&emsp;我们可以简单概括一下 T5 的预训练任务，主要包括以下几个部分：</font><ul><li><font color="black" size="5">**预训练任务:** T5模型的预训练任务是 MLM，也称为BERT-style目标。具体来说，就是在输入文本中随机遮蔽15%的token，然后让模型预测这些被遮蔽的token。这个过程不需要标签，可以在大量未标注的文本上进行。</font></li><li><font color="black" size="5">**输入格式:** 预训练时，T5将输入文本转换为"文本到文本"的格式。对于一个给定的文本序列，随机选择一些token进行遮蔽，并用特殊的占位符(token)替换。然后将被遮蔽的token序列作为模型的输出目标。</font></li><li><font color="black" size="5">**预训练数据集:** T5 使用了自己创建的大规模数据集"Colossal Clean Crawled Corpus"(C4)，该数据集从Common Crawl中提取了大量干净的英语文本。C4数据集经过了一定的清洗，去除了无意义的文本、重复文本等。</font></li><li><font color="black" size="5">**多任务预训练:** T5 还尝试了将多个任务混合在一起进行预训练，而不仅仅是单独的MLM任务。这有助于模型学习更通用的语言表示。</font></li><li><font color="black" size="5">**预训练到微调的转换:** 预训练完成后，T5模型会在下游任务上进行微调。微调时，模型在任务特定的数据集上进行训练，并根据任务调整解码策略。</font></li></ul><font color="black" size="5">&emsp;&emsp;通过大规模预训练，T5模型能够学习到丰富的语言知识，并获得强大的语言表示能力，在多个NLP任务上取得了优异的性能，预训练是T5成功的关键因素之一。</font><h4 id="2-1-3-大一统思想"><a href="#2-1-3-大一统思想" class="headerlink" title="2.1.3 大一统思想"></a><font color="black" size="5">2.1.3 大一统思想</font></h4><font color="black" size="5">&emsp;&emsp;T5模型的一个核心理念是“大一统思想”，即所有的 NLP 任务都可以统一为文本到文本的任务，这一思想在自然语言处理领域具有深远的影响。其设计理念是将所有不同类型的NLP任务（如文本分类、翻译、文本生成、问答等）转换为一个统一的格式：输入和输出都是纯文本。</font><font color="black" size="5">&emsp;&emsp;例如：对于文本分类任务，输入可以是“classify: 这是一个很好的产品”，输出是“正面”；对于翻译任务，输入可以是“translate English to French: How are you?”, 输出是“Comment ça va?”。</font><font color="black" size="5">&emsp;&emsp;T5通过大规模的文本数据进行预训练，然后在具体任务上进行微调。这一过程与BERT、GPT等模型类似，但T5将预训练和微调阶段的任务统一为文本到文本的形式，使其在各种任务上的适应性更强。</font><font color="black" size="5">&emsp;&emsp;我们可以通过图3.11，更加直观地理解 T5 的大一统思想：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009175646491.png" alt="image-20251009175646491"></p><font color="black" size="5">&emsp;&emsp;对于不同的NLP任务，每次输入前都会加上一个任务描述前缀，明确指定当前任务的类型。这不仅帮助模型在预训练阶段学习到不同任务之间的通用特征，也便于在微调阶段迅速适应具体任务。例如，任务前缀可以是“summarize: ”用于摘要任务，或“translate English to German: ”用于翻译任务。</font><font color="black" size="5">&emsp;&emsp;T5的大一统思想通过将所有NLP任务统一为文本到文本的形式，简化了任务处理流程，增强了模型的通用性和适应性。这一思想不仅推动了自然语言处理技术的发展，也为实际应用提供了更为便捷和高效的解决方案。</font><h2 id="3-Decoder-only-PLM"><a href="#3-Decoder-only-PLM" class="headerlink" title="3.Decoder-only PLM"></a>3.Decoder-only PLM</h2><font color="black" size="5">&emsp;&emsp;在前两节中，我们分别讲解了由 Transformer 发展而来的两种模型架构——以 BERT 为代表的 Encoder-Only 模型和以 T5 为代表的 Encoder-Decoder 模型。那么，很自然可以想见，除了上述两种架构，还可以有一种模型架构——Decoder-Only，即只使用 Decoder 堆叠而成的模型。</font><font color="black" size="5">&emsp;&emsp;事实上，Decoder-Only 就是目前大火的 LLM 的基础架构，目前所有的 LLM 基本都是 Decoder-Only 模型（RWKV、Mamba 等非 Transformer 架构除外）。而引发 LLM 热潮的 ChatGPT，正是 Decoder-Only 系列的代表模型 GPT 系列模型的大成之作。而目前作为开源 LLM 基本架构的 LLaMA 模型，也正是在 GPT 的模型架构基础上优化发展而来。因此，在本节中，我们不但会详细分析 Decoder-Only 代表模型 GPT 的原理、架构和特点，还会深入到目前的主流开源 LLM，分析它们的结构、特点，结合之前对 Transformer 系列其他模型的分析，帮助大家深入理解当下被寄予厚望、被认为是 AGI 必经之路的 LLM 是如何一步步从传统 PLM 中发展而来的。首先，让我们学习打开 LLM 世界大门的代表模型——由 OpenAI 发布的 GPT。</font><h3 id="3-1-GPT"><a href="#3-1-GPT" class="headerlink" title="3.1 GPT"></a><font color="black" size="5">3.1 GPT</font></h3><font color="black" size="5">&emsp;&emsp;GPT，即 Generative Pre-Training Language Model，是由 OpenAI 团队于 2018年发布的预训练语言模型。虽然学界普遍认可 BERT 作为预训练语言模型时代的代表，但首先明确提出预训练-微调思想的模型其实是 GPT。GPT 提出了通用预训练的概念，也就是在海量无监督语料上预训练，进而在每个特定任务上进行微调，从而实现这些任务的巨大收益。虽然在发布之初，由于性能略输于不久后发布的 BERT，没能取得轰动性成果，也没能让 GPT 所使用的 Decoder-Only 架构成为学界研究的主流，但 OpenAI 团队坚定地选择了不断扩大预训练数据、增加模型参数，在 GPT 架构上不断优化，最终在 2020年发布的 GPT-3 成就了 LLM 时代的基础，并以 GPT-3 为基座模型的 ChatGPT 成功打开新时代的大门，成为 LLM 时代的最强竞争者也是目前的最大赢家。</font><font color="black" size="5">&emsp;&emsp;本节将以 GPT 为例，分别从模型架构、预训练任务、GPT 系列模型的发展历程等三个方面深入分析 GPT 及其代表的 Decoder-Only 模型，并进一步引出当前的主流 LLM 架构——LLaMA。</font><h4 id="3-1-1-模型架构——Decoder-Only"><a href="#3-1-1-模型架构——Decoder-Only" class="headerlink" title="3.1.1 模型架构——Decoder Only"></a><font color="black" size="5">3.1.1 模型架构——Decoder Only</font></h4><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251010111320968.png" alt="image-20251010111320968"></p><font color="black" size="5">&emsp;&emsp;如图3.12可以看到，GPT 的整体结构和 BERT 是有一些类似的，只是相较于 BERT 的 Encoder，选择使用了 Decoder 来进行模型结构的堆叠。由于 Decoder-Only 结构也天生适用于文本生成任务，所以相较于更贴合 NLU 任务设计的 BERT，GPT 和 T5 的模型设计更契合于 NLG 任务和 Seq2Seq 任务。同样，对于一个自然语言文本的输入，先通过 tokenizer 进行分词并转化为对应词典序号的 input_ids。</font><font color="black" size="5">&emsp;&emsp;输入的 input_ids 首先通过 Embedding 层，再经过 Positional Embedding 进行位置编码。不同于 BERT 选择了可训练的全连接层作为位置编码，GPT 沿用了 Transformer 的经典 Sinusoidal 位置编码，即通过三角函数进行绝对位置编码，此处就不再赘述，感兴趣的读者可以参考第二章 Transformer 模型细节的解析。</font><font color="black" size="5">&emsp;&emsp;</font><font color="black" size="5">&emsp;&emsp;通过 Embedding 层和 Positional Embedding 层编码成 hidden_states 之后，就可以进入到解码器（Decoder），第一代 GPT 模型和原始 Transformer 模型类似，选择了 12层解码器层，但是在解码器层的内部，相较于 Transformer 原始 Decoder 层的双注意力层设计，GPT 的 Decoder 层反而更像 Encoder 层一点。由于不再有 Encoder 的编码输入，Decoder 层仅保留了一个带掩码的注意力层，并且将 LayerNorm 层从 Transformer 的注意力层之后提到了注意力层之前。hidden_states 输入 Decoder 层之后，会先进行 LayerNorm，再进行掩码注意力计算，然后经过残差连接和再一次 LayerNorm 进入到 MLP 中并得到最后输出。</font><font color="black" size="5">&emsp;&emsp;由于不存在 Encoder 的编码结果，Decoder 层中的掩码注意力也是自注意力计算。也就是对一个输入的 hidden_states，会通过三个参数矩阵来生成 query、key 和 value，而不再是像 Transformer 中的 Decoder 那样由 Encoder 输出作为 key 和 value。后续的注意力计算过程则和 BERT 类似，只是在计算得到注意力权重之后，通过掩码矩阵来遮蔽了未来 token 的注意力权重，从而限制每一个 token 只能关注到它之前 token 的注意力，来实现掩码自注意力的计算。</font><font color="black" size="5">&emsp;&emsp;另外一个结构上的区别在于，GPT 的 MLP 层没有选择线性矩阵来进行特征提取，而是选择了两个一维卷积核来提取，不过，从效果上说这两者是没有太大区别的。通过 N 个 Decoder 层后的 hidden_states 最后经过线性矩阵映射到词表维度，就可以转化成自然语言的 token，从而生成我们的目标序列。</font><h4 id="3-1-2-预训练任务——CLM"><a href="#3-1-2-预训练任务——CLM" class="headerlink" title="3.1.2 预训练任务——CLM"></a><font color="black" size="5">3.1.2 预训练任务——CLM</font></h4><font color="black" size="5">&emsp;&emsp;Decoder-Only 的模型结构往往更适合于文本生成任务，因此，Decoder-Only 模型往往选择了最传统也最直接的预训练任务——因果语言模型，Casual Language Model，下简称 CLM。</font><font color="black" size="5">&emsp;&emsp;CLM 可以看作 N-gram 语言模型的一个直接扩展。N-gram 语言模型是基于前 N 个 token 来预测下一个 token，CLM 则是基于一个自然语言序列的前面所有 token 来预测下一个 token，通过不断重复该过程来实现目标文本序列的生成。也就是说，CLM 是一个经典的补全形式。例如，CLM 的输入和输出可以是：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">input: 今天天气</span><br><span class="line">output: 今天天气很</span><br><span class="line"></span><br><span class="line">input: 今天天气很</span><br><span class="line">output：今天天气很好</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;因此，对于一个输入目标序列长度为 256，期待输出序列长度为 256 的任务，模型会不断根据前 256 个 token、257个 token（输入+预测出来的第一个 token）...... 进行 256 次计算，最后生成一个序列长度为 512 的输出文本，这个输出文本前 256 个 token 为输入，后 256 个 token 就是我们期待的模型输出。</font><font color="black" size="5">&emsp;&emsp;在前面我们说过，BERT 之所以可以采用预训练+微调的范式取得重大突破，正是因为其选择的 MLM、NSP 可以在海量无监督语料上直接训练——而很明显，CLM 是更直接的预训练任务，其天生和人类书写自然语言文本的习惯相契合，也和下游任务直接匹配，相对于 MLM 任务更加直接，可以在任何自然语言文本上直接应用。因此，CLM 也可以使用海量的自然语言语料进行大规模的预训练。</font><h4 id="3-1-3-GPT系列模型的发展"><a href="#3-1-3-GPT系列模型的发展" class="headerlink" title="3.1.3 GPT系列模型的发展"></a><font color="black" size="5">3.1.3 GPT系列模型的发展</font></h4><font color="black" size="5">&emsp;&emsp;自 GPT-1 推出开始，OpenAI 一直坚信 Decoder-Only 的模型结构和“体量即正义”的优化思路，不断扩大预训练数据集、模型体量并对模型做出一些小的优化和修正，来不断探索更强大的预训练模型。从被 BERT 压制的 GPT-1，到没有引起足够关注的 GPT-2，再到激发了涌现能力、带来大模型时代的 GPT-3，最后带来了跨时代的 ChatGPT，OpenAI 通过数十年的努力证明了其思路的正确性。</font><font color="black" size="5">&emsp;&emsp;下表总结了从 GPT-1 到 GPT-3 的模型结构、预训练语料大小的变化：</font><div class="table-container"><table><thead><tr><th style="text-align:center">模型</th><th style="text-align:center">DecoderLayer</th><th style="text-align:center">Hidden_size</th><th style="text-align:center">注意力头数</th><th style="text-align:center">注意力维度</th><th style="text-align:center">总参数量</th><th style="text-align:center">预训练语料</th></tr></thead><tbody><tr><td style="text-align:center">GPT-1</td><td style="text-align:center">12</td><td style="text-align:center">3072</td><td style="text-align:center">12</td><td style="text-align:center">768</td><td style="text-align:center">0.12B</td><td style="text-align:center">5GB</td></tr><tr><td style="text-align:center">GPT-2</td><td style="text-align:center">48</td><td style="text-align:center">6400</td><td style="text-align:center">25</td><td style="text-align:center">1600</td><td style="text-align:center">1.5B</td><td style="text-align:center">40GB</td></tr><tr><td style="text-align:center">GPT-3</td><td style="text-align:center">96</td><td style="text-align:center">49152</td><td style="text-align:center">96</td><td style="text-align:center">12288</td><td style="text-align:center">175B</td><td style="text-align:center">570GB</td></tr></tbody></table></div><font color="black" size="5">&emsp;&emsp;GPT-1 是 GPT 系列的开山之作，也是第一个使用 Decoder-Only 的预训练模型。但是，GPT-1 的模型体量和预训练数据都较少，沿承了传统 Transformer 的模型结构，使用了 12层 Decoder Block 和 768 的隐藏层维度，模型参数量仅有 1.17亿（0.12B），在大小为 5GB 的 BooksCorpus 数据集上预训练得到。可以看到，GPT-1 的参数规模与预训练规模和 BERT-base 是大致相当的，但其表现相较于 BERT-base 却有所不如，这也是 GPT 系列模型没能成为预训练语言模型时代的代表的原因。</font><font color="black" size="5">&emsp;&emsp;</font><font color="black" size="5">&emsp;&emsp;GPT-2 则是 OpenAI 在 GPT-1 的基础上进一步探究预训练语言模型多任务学习能力的产物。GPT-2 的模型结构和 GPT-1 大致相当，只是扩大了模型参数规模、将 Post-Norm 改为了 Pre-Norm（也就是先进行 LayerNorm 计算，再进入注意力层计算）。这些改动的核心原因在于，由于模型层数增加、体量增大，梯度消失和爆炸的风险也不断增加，为了使模型梯度更稳定对上述结构进行了优化。</font><font color="black" size="5">&emsp;&emsp;GPT-2 的核心改进是大幅增加了预训练数据集和模型体量。GPT-2 的 Decoder Block 层数达到了48（注意，GPT-2 共发布了四种规格的模型，此处我们仅指规格最大的 GPT-2 模型），隐藏层维度达到了 1600，模型整体参数量达 15亿（1.5B），使用了自己抓取的 40GB 大小的 WebText 数据集进行预训练，不管是模型结构还是预训练大小都超过了 1代一个数量级。</font><font color="black" size="5">&emsp;&emsp;GPT-2 的另一个重大突破是以 zero-shot（零样本学习）为主要目标，也就是不对模型进行微调，直接要求模型解决任务。例如，在传统的预训练-微调范式中，我们要解决一个问题，一般需要收集几百上千的训练样本，在这些训练样本上微调预训练语言模型来实现该问题的解决。而 zero-shot 则强调不使用任何训练样本，直接通过向预训练语言模型描述问题来去解决该问题。zero-shot 的思路自然是比预训练-微调范式更进一步、更高效的自然语言范式，但是在 GPT-2 的时代，模型能力还不足够支撑较好的 zero-shot 效果，在大模型时代，zero-shot 及其延伸出的 few-shot（少样本学习）才开始逐渐成为主流。</font><font color="black" size="5">&emsp;&emsp;GPT-3 则是更进一步展示了 OpenAI“力大砖飞”的核心思路，也是 LLM 的开创之作。在 GPT-2 的基础上，OpenAI 进一步增大了模型体量和预训练数据量，整体参数量达 175B，是当之无愧的“大型语言模型”。在模型结构上，基本没有大的改进，只是由于巨大的模型体量使用了稀疏注意力机制来取代传统的注意力机制。在预训练数据上，则是分别从 CC、WebText、维基百科等大型语料集中采样，共采样了 45T、清洗后 570GB 的数据。根据推算，GPT-3 需要在 1024张 A100（80GB 显存）的分布式训练集群上训练 1个月。</font><font color="black" size="5">&emsp;&emsp;之所以说 GPT-3 是 LLM 的开创之作，除去其巨大的体量带来了涌现能力的凸显外，还在于其提出了 few-shot 的重要思想。few-shot 是在 zero-shot 上的改进，研究者发现即使是 175B 大小的 GPT-3，想要在 zero-shot 上取得较好的表现仍然是一件较为困难的事情。而 few-shot 是对 zero-shot 的一个折中，旨在提供给模型少样的示例来教会它完成任务。few-shot 一般会在 prompt（也就是模型的输入）中增加 3~5个示例，来帮助模型理解。例如，对于情感分类任务：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">zero-shot：请你判断‘这真是一个绝佳的机会’的情感是正向还是负向，如果是正向，输出<span class="number">1</span>；否则输出<span class="number">0</span></span><br><span class="line"></span><br><span class="line">few-shot：请你判断‘这真是一个绝佳的机会’的情感是正向还是负向，如果是正向，输出<span class="number">1</span>；否则输出<span class="number">0</span>。你可以参考以下示例来判断：‘你的表现非常好’——<span class="number">1</span>；‘太糟糕了’——<span class="number">0</span>；‘真是一个好主意’——<span class="number">1</span>。</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;通过给模型提供少量示例，模型可以取得远好于 zero-shot 的良好表现。few-shot 也被称为上下文学习（In-context Learning），即让模型从提供的上下文中的示例里学习问题的解决方法。GPT-3 在 few-shot 上展现的强大能力，为 NLP 的突破带来了重要进展。如果对于绝大部分任务都可以通过人为构造 3~5个示例就能让模型解决，其效率将远高于传统的预训练-微调范式，意味着 NLP 的进一步落地应用成为可能——而这，也正是 LLM 的核心优势。</font><font color="black" size="5">&emsp;&emsp;在 GPT 系列模型的基础上，通过引入预训练-指令微调-人类反馈强化学习的三阶段训练，OpenAI 发布了跨时代的 ChatGPT，引发了大模型的热潮。也正是在 GPT-3 及 ChatGPT 的基础上，LLaMA、ChatGLM 等模型的发布进一步揭示了 LLM 的无尽潜力。在下一节，我们将深入剖析目前 LLM 的普适架构——LLaMA。</font><h3 id="3-2-LLaMA"><a href="#3-2-LLaMA" class="headerlink" title="3.2 LLaMA"></a><font color="black" size="5">3.2 LLaMA</font></h3><font color="black" size="5">&emsp;&emsp;LLaMA模型是由Meta（前Facebook）开发的一系列大型预训练语言模型。从LLaMA-1到LLaMA-3，LLaMA系列模型展示了大规模预训练语言模型的演进及其在实际应用中的显著潜力。</font><h4 id="3-2-1-模型架构——Decoder-Only"><a href="#3-2-1-模型架构——Decoder-Only" class="headerlink" title="3.2.1 模型架构——Decoder Only"></a><font color="black" size="5">3.2.1 模型架构——Decoder Only</font></h4><font color="black" size="5">&emsp;&emsp;与GPT系列模型一样，LLaMA模型也是基于Decoder-Only架构的预训练语言模型。LLaMA模型的整体结构与GPT系列模型类似，只是在模型规模和预训练数据集上有所不同。如图3.13是LLaMA模型的架构示意图：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251010131513006.png" alt="image-20251010131513006"></p><font color="black" size="5">&emsp;&emsp;与GPT类似，LLaMA模型的处理流程也始于将输入文本通过tokenizer进行编码，转化为一系列的input_ids。这些input_ids是模型能够理解和处理的数据格式。接下来，这些input_ids会经过embedding层的转换，这里每个input_id会被映射到一个高维空间中的向量，即词向量。同时，输入文本的位置信息也会通过positional embedding层被编码，以确保模型能够理解词序上下文信息。</font><font color="black" size="5">&emsp;&emsp;</font><font color="black" size="5">&emsp;&emsp;这样，input_ids经过embedding层和positional embedding层的结合，形成了hidden_states。hidden_states包含了输入文本的语义和位置信息，是模型进行后续处理的基础，hidden_states随后被输入到模型的decoder层。</font><font color="black" size="5">&emsp;&emsp;在decoder层中，hidden_states会经历一系列的处理，这些处理由多个decoder block组成。每个decoder block都是模型的核心组成部分，它们负责对hidden_states进行深入的分析和转换。在每个decoder block内部，首先是一个masked self-attention层。在这个层中，模型会分别计算query、key和value这三个向量。这些向量是通过hidden_states线性变换得到的，它们是计算注意力权重的基础。然后使用softmax函数计算attention score，这个分数反映了不同位置之间的关联强度。通过attention score，模型能够确定在生成当前词时，应该给予不同位置的hidden_states多大的关注。然后，模型将value向量与attention score相乘，得到加权后的value，这就是attention的结果。</font><font color="black" size="5">&emsp;&emsp;在完成masked self-attention层之后，hidden_states会进入MLP层。在这个多层感知机层中，模型通过两个全连接层对hidden_states进行进一步的特征提取。第一个全连接层将hidden_states映射到一个中间维度，然后通过激活函数进行非线性变换，增加模型的非线性能力。第二个全连接层则将特征再次映射回原始的hidden_states维度。</font><font color="black" size="5">&emsp;&emsp;最后，经过多个decoder block的处理，hidden_states会通过一个线性层进行最终的映射，这个线性层的输出维度与词表维度相同。这样，模型就可以根据hidden_states生成目标序列的概率分布，进而通过采样或贪婪解码等方法，生成最终的输出序列。这一过程体现了LLaMA模型强大的序列生成能力。</font><h4 id="3-2-2-LLaMA模型的发展历程"><a href="#3-2-2-LLaMA模型的发展历程" class="headerlink" title="3.2.2 LLaMA模型的发展历程"></a><font color="black" size="5">3.2.2 LLaMA模型的发展历程</font></h4><font color="black" size="5">**LLaMA-1 系列**：</font><ul><li><font color="black" size="5">Meta于2023年2月发布了LLaMA-1，包括7B、13B、30B和65B四个参数量版本。</font></li><li><font color="black" size="5">这些模型在超过1T token的语料上进行了预训练，其中最大的65B参数模型在2,048张A100 80G GPU上训练了近21天。</font></li><li><font color="black" size="5">LLaMA-1因其开源性和优异性能迅速成为开源社区中最受欢迎的大模型之一。</font></li></ul><font color="black" size="5">**LLaMA-2 系列**：</font><ul><li><font color="black" size="5">2023年7月，Meta发布了LLaMA-2，包含7B、13B、34B和70B四个参数量版本，除了34B模型外，其他均已开源。</font><font color="black" size="5">&emsp;&emsp;</font></li><li><font color="black" size="5">LLaMA-2将预训练的语料扩充到了2T token，并将模型的上下文长度从2,048翻倍到了4,096。</font></li><li><font color="black" size="5">引入了分组查询注意力机制（Grouped-Query Attention, GQA）等技术。</font></li></ul><font color="black" size="5">**LLaMA-3 系列**：</font><ul><li><font color="black" size="5">2024年4月，Meta发布了LLaMA-3，包括8B和70B两个参数量版本，同时透露400B的LLaMA-3还在训练中。</font></li><li><font color="black" size="5">LLaMA-3支持8K长文本，并采用了编码效率更高的tokenizer，词表大小为128K。</font></li><li><font color="black" size="5">使用了超过15T token的预训练语料，是LLaMA-2的7倍多。</font></li></ul><font color="black" size="5">&emsp;&emsp;LLaMA模型以其技术创新、多参数版本、大规模预训练和高效架构设计而著称。模型支持从7亿到数百亿不等的参数量，适应不同规模的应用需求。LLaMA-1以其开源性和优异性能迅速受到社区欢迎，而LLaMA-2和LLaMA-3进一步通过引入分组查询注意力机制和支持更长文本输入，显著提升了模型性能和应用范围。特别是LLaMA-3，通过采用128K词表大小的高效tokenizer和15T token的庞大训练数据，实现了在多语言和多任务处理上的重大进步。Meta对模型安全性和社区支持的持续关注，预示着LLaMA将继续作为AI技术发展的重要推动力，促进全球范围内的技术应用和创新。</font><h3 id="3-3-ChatGLM"><a href="#3-3-ChatGLM" class="headerlink" title="3.3 ChatGLM"></a><font color="black" size="5">3.3 ChatGLM</font></h3><font color="black" size="5">&emsp;&emsp;GLM 系列模型是由智谱开发的主流中文 LLM 之一，包括 ChatGLM1、2、3及 GLM-4 系列模型，覆盖了指令理解、代码生成等多种应用场景，曾在多种中文评估集上达到 SOTA 性能。</font><font color="black" size="5">&emsp;&emsp;ChatGLM-6B 是 GLM 系列的开山之作，也是 2023年国内最早的开源中文 LLM，也是最早提出不同于 GPT、LLaMA 的独特模型架构的 LLM。在整个中文 LLM 的发展历程中，GLM 具有独特且重大的技术意义。本节将简要叙述 GLM 系列的发展，并介绍其不同于 GPT、LLaMA 系列模型的独特技术思路。</font><h4 id="3-3-1-模型架构——相对于GPT的略微修正"><a href="#3-3-1-模型架构——相对于GPT的略微修正" class="headerlink" title="3.3.1 模型架构——相对于GPT的略微修正"></a><font color="black" size="5">3.3.1 模型架构——相对于GPT的略微修正</font></h4><font color="black" size="5">&emsp;&emsp;GLM 最初是由清华计算机系推出的一种通用语言模型基座，其核心思路是在传统 CLM 预训练任务基础上，加入 MLM 思想，从而构建一个在 NLG 和 NLU 任务上都具有良好表现的统一模型。</font><font color="black" size="5">&emsp;&emsp;</font><font color="black" size="5">&emsp;&emsp;在整体模型结构上，GLM 和 GPT 大致类似，均是 Decoder-Only 的结构，仅有三点细微差异：</font><font color="black" size="5">&emsp;&emsp;1.使用 Post Norm 而非 Pre Norm。Post Norm 是指在进行残差连接计算时，先完成残差计算，再进行 LayerNorm 计算；而类似于 GPT、LLaMA 等模型都使用了 Pre Norm，也就是先进行 LayerNorm 计算，再进行残差的计算。相对而言，Post Norm 由于在残差之后做归一化，对参数正则化的效果更强，进而模型的鲁棒性也会更好；Pre Norm相对于因为有一部分参数直接加在了后面，不需要对这部分参数进行正则化，正好可以防止模型的梯度爆炸或者梯度消失。因此，对于更大体量的模型来说，一般认为 Pre Norm 效果会更好。但 GLM 论文提出，使用 Post Norm 可以避免 LLM 的数值错误（虽然主流 LLM 仍然使用了 Pre Norm）；</font><font color="black" size="5">&emsp;&emsp;2.使用单个线性层实现最终 token 的预测，而不是使用 MLP；这样的结构更加简单也更加鲁棒，即减少了最终输出的参数量，将更大的参数量放在了模型本身；</font><font color="black" size="5">&emsp;&emsp;3.激活函数从 ReLU 换成了 GeLUs。ReLU 是传统的激活函数，其核心计算逻辑为去除小于 0的传播，保留大于 0的传播；GeLUs 核心是对接近于 0的正向传播，做了一个非线性映射，保证了激活函数后的非线性输出，具有一定的连续性。</font><h4 id="3-3-2-预训练任务-GLM"><a href="#3-3-2-预训练任务-GLM" class="headerlink" title="3.3.2 预训练任务-GLM"></a><font color="black" size="5">3.3.2 预训练任务-GLM</font></h4><font color="black" size="5">&emsp;&emsp;GLM 的核心创新点主要在于其提出的 GLM（General Language Model，通用语言模型）任务，这也是 GLM 的名字由来。GLM 是一种结合了自编码思想和自回归思想的预训练方法。所谓自编码思想，其实也就是 MLM 的任务学习思路，在输入文本中随机删除连续的 tokens，要求模型学习被删除的 tokens；所谓自回归思想，其实就是传统的 CLM 任务学习思路，也就是要求模型按顺序重建连续 tokens。</font><font color="black" size="5">&emsp;&emsp;GLM 通过优化一个自回归空白填充任务来实现 MLM 与 CLM 思想的结合。其核心思想是，对于一个输入序列，会类似于 MLM 一样进行随机的掩码，但遮蔽的不是和 MLM 一样的单个 token，而是每次遮蔽一连串 token；模型在学习时，既需要使用遮蔽部分的上下文预测遮蔽部分，在遮蔽部分内部又需要以 CLM 的方式完成被遮蔽的 tokens 的预测。例如，输入和输出可能是：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">输入：I &lt;MASK&gt; because you &lt;MASK&gt;</span><br><span class="line">输出：&lt;MASK&gt; - love you; &lt;MASK&gt; - are a wonderful person</span><br></pre></td></tr></table></figure><font color="black" size="5">&emsp;&emsp;通过将 MLM 与 CLM 思想相结合，既适配逐个 token 生成的生成类任务，也迫使模型从前后两个方向学习输入文本的隐含关系从而适配了理解类任务。使用 GLM 预训练任务产出的 GLM 模型，在一定程度上展现了其超出同体量 BERT 系模型的优越性能：</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251010150004283.png" alt="image-20251010150004283"></p><font color="black" size="5">&emsp;&emsp;不过，GLM 预训练任务更多的优势还是展现在预训练模型时代，迈入 LLM 时代后，针对于超大规模、体量的预训练，CLM 展现出远超 MLM 的优势。通过将模型体量加大、预训练规模扩大，CLM 预训练得到的生成模型在文本理解上也能具有超出 MLM 训练的理解模型的能力，因此，ChatGLM 系列模型也仅在第一代模型使用了 GLM 的预训练思想，从 ChatGLM2 开始，还是回归了传统的 CLM 建模。虽然从 LLM 的整体发展路径来看，GLM 预训练任务似乎是一个失败的尝试，但通过精巧的设计将 CLM 与 MLM 融合，并第一时间产出了中文开源的原生 LLM，其思路仍然存在较大的借鉴意义。</font><h4 id="3-3-3-GLM家族的发展"><a href="#3-3-3-GLM家族的发展" class="headerlink" title="3.3.3 GLM家族的发展"></a><font color="black" size="5">3.3.3 GLM家族的发展</font></h4><font color="black" size="5">&emsp;&emsp;在 GLM 模型（即使用原生 GLM 架构及预训练任务的早期预训练模型）的基础上，参考 ChatGPT 的技术思路进行 SFT 和 RLHF，智谱于 23年 3月发布了第一个中文开源 LLM ChatGLM-6B，成为了众多中文 LLM 研究者的起点。ChatGLM-6B 在 1T 语料上进行预训练，支持 2K 的上下文长度。</font><font color="black" size="5">&emsp;&emsp;在 23年 6月，智谱就开源了 ChatGLM2-6B。相对于一代，ChatGLM2 将上下文长度扩展到了 32K，通过更大的预训练规模实现了模型性能的大幅度突破。不过，在 ChatGLM2 中，模型架构就基本回归了 LLaMA 架构，引入 MQA 的注意力机制，预训练任务也回归经典的 CLM，放弃了 GLM 的失败尝试。</font><font color="black" size="5">&emsp;&emsp;</font><font color="black" size="5">&emsp;&emsp;ChatGLM3-6B 发布于 23年 10月，相对于二代在语义、数学、推理、代码和知识方面都达到了当时的 SOTA 性能，但是官方给出的技术报告说明 ChatGLM3 在模型架构上相对二代没有变化，最主要的优化来源是更多样化的训练数据集、更充足的训练步骤和更优化的训练策略。ChatGLM3 的另一个重要改进在于其开始支持函数调用与代码解释器，开发者可以直接使用开源的 ChatGLM3 来实现 Agent 开发，具有更广泛的应用价值。</font><font color="black" size="5">&emsp;&emsp;2024年 1月，智谱发布了支持 128K 上下文，包括多种类型的 GLM-4 系列模型，评估其在英文基准上达到了 GPT-4 的水平。不过，智谱并未直接开源 GLM-4，而是开源了其轻量级版本 GLM-4-9B 模型，其在 1T token 的多语言语料库上进行预训练，上下文长度为 8K，并使用与 GLM-4 相同的管道和数据进行后训练。在训练计算量较少的情况下，其超越了 Llama-3-8B，并支持 GLM-4 中所有工具的功能。</font><p><img src="/posts/2634edb1.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251010150452909.png" alt="image-20251010150452909"></p>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>哈基鹏的大模型之路（三）</title>
      <link href="/posts/9119f4d4.html"/>
      <url>/posts/9119f4d4.html</url>
      
        <content type="html"><![CDATA[<h1 id="Trasnsformer架构"><a href="#Trasnsformer架构" class="headerlink" title="Trasnsformer架构"></a>Trasnsformer架构</h1><h2 id="1-1-注意力机制"><a href="#1-1-注意力机制" class="headerlink" title="1.1 注意力机制"></a>1.1 注意力机制</h2><font color="black" size="5">&emsp;&emsp;注意力机制最先源于计算机领域，其核心思想为当我们关注一张图片，我们往往无需看清楚全部内容而仅将注意力集中在重点部分即可。而在自然语言领域，我们往往也可以通过将重点注意力集中在一个或几个token，从而取得更高效高质量的计算效果。</font><font size="5">&emsp;&emsp;注意力机制有三个核心变量：<font color="red" size="5">**Query**（查询值）</font>、<font color="blue" size="5">**Key**（键值）</font>和<font color="Coral" size="5">**Value**(真值)</font>。我们可以通过一个案例来理解每一个变量所代表的含义。例如，当我们有一篇新闻报道，我们想要找到这个报道的时间，那么，<font color="red" size="5">**Query**</font>可以是类似于”时间“，”日期“一类的向量（为了便于理解，此处使用文本来表示，但其实际是稠密的向量）,<font color="blue" size="5">**Key**</font> 和 <font color="Coral" size="5">**Value**</font>会是整个文本。通过对 <font color="red" size="5">**Query**</font> 和 <font color="blue" size="5">**Key**</font> 进行运算，得到的最后结果就是从 <font color="red" size="5">**Query**</font> 出发，对文本每一个token应该分布的注意力相对大小。通过把权重和 <font color="Coral" size="5">**Value**</font> 进行运算，得到的最后结果就是从 <font color="red" size="5">**Query**</font> 出发计算整个文本注意力得到的结果。</font><font size="5">&emsp;&emsp;具体而言，注意力机制的特点是通过计算 <font color="red" size="5">**Query**</font> 与 <font color="blue" size="5">**Key**</font> 的相关性为真值加权求和，从而拟合序列中每个词同其他词的相关关系。</font><h3 id="1-1-1-深入理解注意力机制"><a href="#1-1-1-深入理解注意力机制" class="headerlink" title="1.1.1 深入理解注意力机制"></a>1.1.1 深入理解注意力机制</h3><font size="5">&emsp;&emsp;注意力机制有三个核心变量：查询值 <font color="red" size="5">**Query**</font>，键值 <font color="blue" size="5">**Key**</font> 和 真值 <font color="Coral" size="5">**Value**</font>。接下来我们以字典为例，逐步分析注意力机制的计算公式是如何得到的，从而帮助读者深入理解注意力机制。首先，我们有这样一个字典：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line"> &quot;apple&quot;:10,</span><br><span class="line"> &quot;banana&quot;:5,</span><br><span class="line"> &quot;chair&quot;:2,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;此时，字典的键就是注意力机制中的键值 <font color="blue" size="5">**Key**</font>，而字典的值就是真值 <font color="Coral" size="5">**Value**</font>。字典支持我们进行精确的字符串匹配，例如，如果我们想要查找的值也就是查询值 Query 为“apple”，那么我们可以直接通过将 <font color="red" size="5">**Query**</font> 与 <font color="blue" size="5">**Key**</font> 做匹配来得到对应的 <font color="Coral" size="5">**Value**</font>。</font><font size="5">&emsp;&emsp;但是，如果我们想要匹配的 <font color="red" size="5">**Query**</font> 是一个包含多个 <font color="blue" size="5">**Key**</font> 的概念呢？例如，我们想要查找“fruit”，此时，我们应该将 apple 和 banana 都匹配到，但不能匹配到 chair。因此，我们往往会选择将 <font color="blue" size="5">**Key**</font> 对应的 <font color="Coral" size="5">**Value**</font> 进行组合得到最终的 <font color="Coral" size="5">**Value**</font>。</font><font size="5">&emsp;&emsp;例如，当我们的 <font color="red" size="5">**Query**</font> 为“fruit”，我们可以分别给三个 <font color="blue" size="5">**Key**</font>  赋予如下的权重</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line"> &quot;apple&quot;:0.6,</span><br><span class="line"> &quot;banana&quot;:0.4,</span><br><span class="line"> &quot;chair&quot;:0,</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;那么，我们最终查询到的值应该是：</font><font size="5">&emsp;&emsp;$value = 0.6 \times10+0.4 \times 5 + 0 \times 2 =8$</font><font size="5">&emsp;&emsp;给不同 <font color="blue" size="5">**Key**</font> 所赋予的不同权重，就是我们所说的注意力分数，也就是为了查询到 <font color="red" size="5">**Query**</font>，我们应该赋予给每一个 <font color="blue" size="5">**Key**</font> 多少注意力。但是，如何针对每一个 <font color="red" size="5">**Query**</font>，计算出对应的注意力分数呢？从直观上讲，我们可以认为 <font color="blue" size="5">**Key**</font> 与 <font color="red" size="5">**Query**</font> 相关性越高，则其所应该赋予的注意力权重就越大。但是，我们如何能够找到一个合理的、能够计算出正确的注意力分数的方法呢？</font><font size="5">&emsp;&emsp;通过合理的训练拟合，词向量能够表征语义信息，从而让语义相近的词在向量空间中距离更近，语义较远的词在向量空间中距离更远。我们往往用欧式距离来衡量词向量的相似性，但我们同样也可以用点积来进行度量：</font><font size="5">&emsp;&emsp;$v \cdot w = \sum_{i}v_iw_i$</font><font size="5">&emsp;&emsp;根据词向量的定义，语义相似的两个词对应的词向量的点积应该大于0，而语义不相似的词向量点积应该小于0。</font><font size="5">&emsp;&emsp;那么，我们就可以用点积来计算词之间的相似度。假设我们的 <font color="red" size="5">**Query**</font> 为“fruit”，对应的词向量为 *q* ；我们的 <font color="blue" size="5">**Key**</font> 对应的词向量为 $k=[v_{apple}v_{banana}v_{chair}]$ ,则我们可以计算 <font color="red" size="5">**Query**</font> 和每一个键的相似程度：</font><font size="5">&emsp;&emsp;$x = q K^T$</font><font size="5">&emsp;&emsp;此处的 K 即为将所有 <font color="blue" size="5">**Key**</font> 对应的词向量堆叠形成的矩阵。基于矩阵乘法的定义，x 即为 q 与每一个 k 值的点积。现在我们得到的 x 即反映了 <font color="red" size="5">**Query**</font> 和每一个 <font color="blue" size="5">**Key**</font> 的相似程度，我们再通过一个 Softmax 层将其转化为和为 1 的权重：</font><font size="5">&emsp;&emsp;$Softmax(x)_i = \frac{e^{x_i}}{\sum_j e^{x_j}}$</font><font size="5">&emsp;&emsp;这样，得到的向量就能够反映 <font color="red" size="5">**Query**</font> 和每一个 <font color="blue" size="5">**Key**</font> 的相似程度，同时又相加权重为 1，也就是我们的注意力分数了。最后，我们再将得到的注意力分数和值向量做对应乘积即可。根据上述过程，我们就可以得到注意力机制计算的基本公式：</font><font size="5">&emsp;&emsp;$attention(Q,K,V) = softmax(q K^T)v$</font><font size="5">&emsp;&emsp;不过，此时的值还是一个标量，同时，我们此次只查询了一个 <font color="red" size="5">**Query**</font>。我们可以将值转化为维度为 $d_v$的向量，同时一次性查询多个 <font color="red" size="5">**Query**</font>，同样将多个 <font color="red" size="5">**Query**</font> 对应的词向量堆叠在一起形成矩阵 Q，得到公式：</font><font size="5">&emsp;&emsp;$attention(Q,K,V) = softmax(Q K^T)V$</font><font size="5">&emsp;&emsp;目前，我们离标准的注意力机制公式还差最后一步。在上一个公式中，如果 Q 和 K 对应的维度 $d_k$ 比较大，softmax 放缩时就非常容易受影响，使不同值之间的差异较大，从而影响梯度的稳定性。因此，我们要将 Q 和 K 乘积的结果做一个放缩:</font><font size="5">&emsp;&emsp;$attention(Q,K,V) = softmax(\frac{Q K^T}{\sqrt{d_k}})V$</font><font size="5">&emsp;&emsp;这也就是注意力机制的核心计算公式了。</font><h3 id="1-1-2-注意力机制的实现"><a href="#1-1-2-注意力机制的实现" class="headerlink" title="1.1.2 注意力机制的实现"></a>1.1.2 注意力机制的实现</h3><font size="5">&emsp;&emsp;基于上文，我们可以很简单地使用 Pytorch 来实现注意力机制的代码：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="string">&#x27;&#x27;&#x27;注意力计算函数&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="keyword">def</span> <span class="title function_">attention</span>(<span class="params">query, key, value, dropout=<span class="literal">None</span></span>):</span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="string">    args:</span></span><br><span class="line"><span class="string">    query: 查询值矩阵</span></span><br><span class="line"><span class="string">    key: 键值矩阵</span></span><br><span class="line"><span class="string">    value: 真值矩阵</span></span><br><span class="line"><span class="string">    &#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="comment"># 获取键向量的维度，键向量的维度和值向量的维度相同</span></span><br><span class="line">    d_k = query.size(-<span class="number">1</span>) </span><br><span class="line">    <span class="comment"># 计算Q与K的内积并除以根号dk</span></span><br><span class="line">    <span class="comment"># transpose——相当于转置</span></span><br><span class="line">    scores = torch.matmul(query, key.transpose(-<span class="number">2</span>, -<span class="number">1</span>)) / math.sqrt(d_k)</span><br><span class="line">    <span class="comment"># Softmax</span></span><br><span class="line">    p_attn = scores.softmax(dim=-<span class="number">1</span>)</span><br><span class="line">    <span class="keyword">if</span> dropout <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">        p_attn = dropout(p_attn)</span><br><span class="line">        <span class="comment"># 采样</span></span><br><span class="line">     <span class="comment"># 根据计算结果对value进行加权求和</span></span><br><span class="line">    <span class="keyword">return</span> torch.matmul(p_attn, value), p_attn</span><br></pre></td></tr></table></figure><h3 id="1-1-3-自注意力"><a href="#1-1-3-自注意力" class="headerlink" title="1.1.3 自注意力"></a>1.1.3 自注意力</h3><font size="5">&emsp;&emsp;根据上文的分析，我们可以发现，注意力机制的本质是对两段序列的元素依次进行相似度计算，寻找出一个序列的每个元素对另一个序列的每个元素的相关度，然后基于相关度进行加权，即分配注意力。而这两段序列即是我们计算过程中 Q、K、V 的来源。</font><font size="5">&emsp;&emsp;但是，在我们的实际应用中，我们往往只需要计算 Query 和 Key 之间的注意力结果，很少存在额外的真值 Value。也就是说，我们其实只需要拟合两个文本序列。在经典的注意力机制中，Q 往往来自于一个序列，K 与 V 来自于另一个序列，都通过参数矩阵计算得到，从而可以拟合这两个序列之间的关系。例如在 Transformer 的 Decoder 结构中，Q 来自于 Decoder 的输入，K 与 V 来自于 Encoder 的输出，从而拟合了编码信息与历史信息之间的关系，便于综合这两种信息实现未来的预测。</font><font size="5">&emsp;&emsp;但在 Transformer 的 Encoder 结构中，使用的是注意力机制的变种 —— 自注意力（self-attention，自注意力）机制。所谓自注意力，即是计算本身序列中每个元素对其他元素的注意力分布，即在计算过程中，Q、K、V 都由同一个输入通过不同的参数矩阵计算得到。在 Encoder 中，Q、K、V 分别是输入对参数矩阵 $W_q$、$W_k$、$W_v$ 做积得到，从而拟合输入语句中每一个 token 对其他所有 token 的关系。</font><font size="5">&emsp;&emsp;通过自注意力机制，我们可以找到一段文本中每一个 token 与其他所有 token 的相关关系大小，从而建模文本之间的依赖关系。在代码中的实现，self-attention 机制其实是通过给 Q、K、V 的输入传入同一个参数实现的。</font><h3 id="1-1-4-掩码自注意力"><a href="#1-1-4-掩码自注意力" class="headerlink" title="1.1.4 掩码自注意力"></a>1.1.4 掩码自注意力</h3><font size="5">&emsp;&emsp;**掩码自注意力（Mask Self-Attention）**是指使用注意力掩码的自注意力机制。掩码的作用是遮蔽一些特定位置的token，模型在学习的过程中就会忽略被遮蔽的token。</font><font size="5">&emsp;&emsp;使用注意力掩码的核心动机是让模型只能使用历史信息进行预测而不能看到未来信息。使用注意力机制的 Transformer 模型也是通过类似于 n-gram 的语言模型任务来学习的，也就是对一个文本序列，不断根据之前的 token 来预测下一个 token，直到将整个文本序列补全。</font><font size="5">&emsp;&emsp;例如，如果待学习的文本序列是 【BOS】I like you【EOS】，那么，模型会按如下顺序进行预测和学习：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">Step 1：输入 【BOS】，输出 I</span><br><span class="line">Step 2：输入 【BOS】I，输出 like</span><br><span class="line">Step 3：输入 【BOS】I like，输出 you</span><br><span class="line">Step 4：输入 【BOS】I like you，输出 【EOS】</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;理论上来说，只要学习的语料足够多，通过上述的过程，模型可以学会任意一种文本序列的建模方式，也就是可以对任意的文本进行补全。</font><font size="5">&emsp;&emsp;但是，我们可以发现，上述过程是一个串行的过程，也就是需要先完成 Step 1，才能做 Step 2，接下来逐步完成整个序列的补全。我们在一开始就说过，Transformer 相对于 RNN 的核心优势之一即在于其可以并行计算，具有更高的计算效率。如果对于每一个训练语料，模型都需要串行完成上述过程才能完成学习，那么很明显没有做到并行计算，计算效率很低。</font><font size="5">&emsp;&emsp;针对这个问题，Transformer 就提出了掩码自注意力的方法。掩码自注意力会生成一串掩码，来遮蔽未来信息。例如，我们待学习的文本序列仍然是 【BOS】I like you【EOS】，我们使用的注意力掩码是【MASK】，那么模型的输入为：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line">&lt;BOS&gt; 【MASK】【MASK】 【MASK】 【MASK】</span><br><span class="line">&lt;BOS&gt;    I   【MASK】 【MASK】 【MASK】</span><br><span class="line">&lt;BOS&gt;    I     like  【MASK】 【MASK】</span><br><span class="line">&lt;BOS&gt;    I     like    you   【MASK】</span><br><span class="line">&lt;BOS&gt;    I     like    you    &lt;/EOS&gt;</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;在每一行输入中，模型仍然是只看到前面的 token，预测下一个 token。但是注意，上述输入不再是串行的过程，而可以一起并行地输入到模型中，模型只需要每一个样本根据未被遮蔽的 token 来预测下一个 token 即可，从而实现了并行的语言模型。</font><font size="5">&emsp;&emsp;观察上述的掩码，我们可以发现其实则是一个和文本序列等长的上三角矩阵。我们可以简单地通过创建一个和输入同等长度的上三角矩阵作为注意力掩码，再使用掩码来遮蔽掉输入即可。也就是说，当输入维度为 （batch_size, seq_len, hidden_size）时，我们的 Mask 矩阵维度一般为 (1, seq_len, seq_len)（通过广播实现同一个 batch 中不同样本的计算）。</font><font size="5">&emsp;&emsp;在具体实现中，我们通过以下代码生成 Mask 矩阵：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">创建一个上三角矩阵，用于遮蔽未来信息。</span></span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">先通过 full 函数创建一个 1 * seq_len * seq_len 的矩阵</span></span><br><span class="line">mask = torch.full((1, args.max_seq_len, args.max_seq_len), float(&quot;-inf&quot;))</span><br><span class="line"><span class="meta prompt_"># </span><span class="language-bash">triu 函数的功能是创建一个上三角矩阵</span></span><br><span class="line">mask = torch.triu(mask, diagonal=1)</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;生成的 Mask 矩阵会是一个上三角矩阵，上三角位置的元素均为 -inf，其他位置的元素置为0。</font><font size="5">&emsp;&emsp;在注意力计算时，我们会将计算得到的注意力分数与这个掩码做和，再进行 Softmax 操作：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">此处的 scores 为计算得到的注意力分数，mask 为上文生成的掩码矩阵</span></span><br><span class="line">scores = scores + mask[:, :seqlen, :seqlen]</span><br><span class="line">scores = F.softmax(scores.float(), dim=-1).type_as(xq)</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;通过做求和，上三角区域（也就是应该被遮蔽的 token 对应的位置）的注意力分数结果都变成了 `-inf`，而下三角区域的分数不变。再做 Softmax 操作，`-inf` 的值在经过 Softmax 之后会被置为 0，从而忽略了上三角区域计算的注意力分数，从而实现了注意力遮蔽。</font><h3 id="1-1-5-多头注意力"><a href="#1-1-5-多头注意力" class="headerlink" title="1.1.5 多头注意力"></a>1.1.5 多头注意力</h3><font size="5">&emsp;&emsp;注意力机制可以实现并行化与长期依赖关系拟合，但一次注意力计算只能拟合一种相关关系，单一的注意力机制很难全面拟合语句序列里的相关关系。因此 Transformer 使用了多头注意力机制（Multi-Head Attention），即同时对一个语料进行多次注意力计算，每次注意力计算都能拟合不同的关系，将最后的多次结果拼接起来作为最后的输出，即可更全面深入地拟合语言信息。</font><font size="5">&emsp;&emsp;事实上，所谓的多头注意力机制其实就是将原始的输入序列进行多组的自注意力处理；然后再将每一组得到的自注意力结果拼接起来，再通过一个线性层进行处理，得到最终的输出。我们用公式可以表示为：</font><font size="5">&emsp;&emsp;$MultiHead(Q,K,V)=Concat(head_1,...,head_h)W^O$</font><font size="5">&emsp;&emsp;where $head_i = Attention(QW^Q_i,KW^K_i,VW^V_i)$</font><font size="5">&emsp;&emsp;其最直观的代码实现并不复杂，即 n 个头就有 n 组3个参数矩阵，每一组进行同样的注意力计算，但由于是不同的参数矩阵从而通过反向传播实现了不同的注意力结果，然后将 n 个结果拼接起来输出即可。</font><font size="5">&emsp;&emsp;但上述实现时空复杂度均较高，我们可以通过矩阵运算巧妙地实现并行的多头计算，其核心逻辑在于使用三个组合矩阵来代替了n个参数矩阵的组合，也就是矩阵内积再拼接其实等同于拼接矩阵再内积。具体实现可以参考下列代码：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> torch.nn <span class="keyword">as</span> nn</span><br><span class="line"><span class="keyword">import</span> torch</span><br><span class="line"></span><br><span class="line"><span class="string">&#x27;&#x27;&#x27;多头自注意力计算模块&#x27;&#x27;&#x27;</span></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">MultiHeadAttention</span>(nn.Module):</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, args: ModelArgs, is_causal=<span class="literal">False</span></span>):</span><br><span class="line">        <span class="comment"># 构造函数</span></span><br><span class="line">        <span class="comment"># args: 配置对象</span></span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="comment"># 隐藏层维度必须是头数的整数倍，因为后面我们会将输入拆成头数个矩阵</span></span><br><span class="line">        <span class="keyword">assert</span> args.dim % args.n_heads == <span class="number">0</span></span><br><span class="line">        <span class="comment"># 每个头的维度，等于模型维度除以头的总数</span></span><br><span class="line">        <span class="variable language_">self</span>.head_dim = args.dim // args.n_heads</span><br><span class="line">        <span class="variable language_">self</span>.n_heads = args.n_heads</span><br><span class="line">        </span><br><span class="line">        <span class="comment"># Wq,Wk,Wv 参数矩阵，每个参数矩阵为 n_embd x dim</span></span><br><span class="line">        <span class="comment"># 这里通过三个组合矩阵来代替了n个参数矩阵的组合，其逻辑在于矩阵内积再拼接其实等同于拼接矩阵再内积，</span></span><br><span class="line">        <span class="comment"># 每一个线性层其实相当于n个参数矩阵的拼接</span></span><br><span class="line">        <span class="variable language_">self</span>.wq = nn.Linear(args.n_embd, <span class="variable language_">self</span>.n_heads * <span class="variable language_">self</span>.head_dim, bias=<span class="literal">False</span>)</span><br><span class="line">        <span class="variable language_">self</span>.wk = nn.Linear(args.n_embd, <span class="variable language_">self</span>.n_heads * <span class="variable language_">self</span>.head_dim, bias=<span class="literal">False</span>)</span><br><span class="line">        <span class="variable language_">self</span>.wv = nn.Linear(args.n_embd, <span class="variable language_">self</span>.n_heads * <span class="variable language_">self</span>.head_dim, bias=<span class="literal">False</span>)</span><br><span class="line">        <span class="comment"># 输出权重矩阵，维度为 dim x dim（head_dim = dim / n_heads）</span></span><br><span class="line">        <span class="variable language_">self</span>.wo = nn.Linear(<span class="variable language_">self</span>.n_heads * <span class="variable language_">self</span>.head_dim, args.dim, bias=<span class="literal">False</span>)</span><br><span class="line">        <span class="comment"># 注意力的 dropout</span></span><br><span class="line">        <span class="variable language_">self</span>.attn_dropout = nn.Dropout(args.dropout)</span><br><span class="line">        <span class="comment"># 残差连接的 dropout</span></span><br><span class="line">        <span class="variable language_">self</span>.resid_dropout = nn.Dropout(args.dropout)</span><br><span class="line">        <span class="variable language_">self</span>.is_causal = is_causal</span><br><span class="line">        </span><br><span class="line">         <span class="comment"># 创建一个上三角矩阵，用于遮蔽未来信息</span></span><br><span class="line">        <span class="comment"># 注意，因为是多头注意力，Mask 矩阵比之前我们定义的多一个维度</span></span><br><span class="line">        <span class="keyword">if</span> is_causal:</span><br><span class="line">            mask = torch.full((<span class="number">1</span>, <span class="number">1</span>, args.max_seq_len, args.max_seq_len), <span class="built_in">float</span>(<span class="string">&quot;-inf&quot;</span>))</span><br><span class="line">            mask = torch.triu(mask, diagonal=<span class="number">1</span>)</span><br><span class="line">            <span class="comment"># 注册为模型的缓冲区</span></span><br><span class="line">            <span class="variable language_">self</span>.register_buffer(<span class="string">&quot;mask&quot;</span>, mask)</span><br><span class="line">        </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, q: torch.Tensor, k: torch.Tensor, v: torch.Tensor</span>):</span><br><span class="line">        <span class="comment"># 获取批次大小和序列长度，[batch_size, seq_len, dim]</span></span><br><span class="line">        bsz, seqlen, _ = q.shape</span><br><span class="line">        <span class="comment"># 计算查询（Q）、键（K）、值（V）,输入通过参数矩阵层，维度为 (B, T, n_embed) x (n_embed, dim) -&gt; (B, T, dim)</span></span><br><span class="line">        xq, xk, xv = <span class="variable language_">self</span>.wq(q), <span class="variable language_">self</span>.wk(k), <span class="variable language_">self</span>.wv(v)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 将 Q、K、V 拆分成多头，维度为 (B, T, n_head, dim // n_head)，然后交换维度，变成 (B, n_head, T, dim // n_head)</span></span><br><span class="line">        <span class="comment"># 因为在注意力计算中我们是取了后两个维度参与计算</span></span><br><span class="line">        <span class="comment"># 为什么要先按B*T*n_head*C//n_head展开再互换1、2维度而不是直接按注意力输入展开，是因为view的展开方式是直接把输入全部排开，</span></span><br><span class="line">        <span class="comment"># 然后按要求构造，可以发现只有上述操作能够实现我们将每个头对应部分取出来的目标</span></span><br><span class="line">        xq = xq.view(bsz, seqlen, <span class="variable language_">self</span>.n_heads, <span class="variable language_">self</span>.head_dim)</span><br><span class="line">        xk = xk.view(bsz, seqlen, <span class="variable language_">self</span>.n_heads, <span class="variable language_">self</span>.head_dim)</span><br><span class="line">        xv = xv.view(bsz, seqlen, <span class="variable language_">self</span>.n_heads, <span class="variable language_">self</span>.head_dim)</span><br><span class="line">        xq = xq.transpose(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line">        xk = xk.transpose(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line">        xv = xv.transpose(<span class="number">1</span>, <span class="number">2</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 注意力计算</span></span><br><span class="line">        <span class="comment"># 计算 QK^T / sqrt(d_k)，维度为 (B, nh, T, hs) x (B, nh, hs, T) -&gt; (B, nh, T, T)</span></span><br><span class="line">        scores = torch.matmul(xq, xk.transpose(<span class="number">2</span>, <span class="number">3</span>)) / math.sqrt(<span class="variable language_">self</span>.head_dim)</span><br><span class="line">        <span class="comment"># 掩码自注意力必须有注意力掩码</span></span><br><span class="line">        <span class="keyword">if</span> <span class="variable language_">self</span>.is_causal:</span><br><span class="line">            <span class="keyword">assert</span> <span class="built_in">hasattr</span>(<span class="variable language_">self</span>, <span class="string">&#x27;mask&#x27;</span>)</span><br><span class="line">            <span class="comment"># 这里截取到序列长度，因为有些序列可能比 max_seq_len 短</span></span><br><span class="line">            scores = scores + <span class="variable language_">self</span>.mask[:, :, :seqlen, :seqlen]</span><br><span class="line">        <span class="comment"># 计算 softmax，维度为 (B, nh, T, T)</span></span><br><span class="line">        scores = F.softmax(scores.<span class="built_in">float</span>(), dim=-<span class="number">1</span>).type_as(xq)</span><br><span class="line">        <span class="comment"># 做 Dropout</span></span><br><span class="line">        scores = <span class="variable language_">self</span>.attn_dropout(scores)</span><br><span class="line">        <span class="comment"># V * Score，维度为(B, nh, T, T) x (B, nh, T, hs) -&gt; (B, nh, T, hs)</span></span><br><span class="line">        output = torch.matmul(scores, xv)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 恢复时间维度并合并头。</span></span><br><span class="line">        <span class="comment"># 将多头的结果拼接起来, 先交换维度为 (B, T, n_head, dim // n_head)，再拼接成 (B, T, n_head * dim // n_head)</span></span><br><span class="line">        <span class="comment"># contiguous 函数用于重新开辟一块新内存存储，因为Pytorch设置先transpose再view会报错，</span></span><br><span class="line">        <span class="comment"># 因为view直接基于底层存储得到，然而transpose并不会改变底层存储，因此需要额外存储</span></span><br><span class="line">        output = output.transpose(<span class="number">1</span>, <span class="number">2</span>).contiguous().view(bsz, seqlen, -<span class="number">1</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 最终投影回残差流。</span></span><br><span class="line">        output = <span class="variable language_">self</span>.wo(output)</span><br><span class="line">        output = <span class="variable language_">self</span>.resid_dropout(output)</span><br><span class="line">        <span class="keyword">return</span> output</span><br></pre></td></tr></table></figure><h2 id="1-2-Encoder-Decoder"><a href="#1-2-Encoder-Decoder" class="headerlink" title="1.2 Encoder-Decoder"></a>1.2 Encoder-Decoder</h2><font size="5">&emsp;&emsp;在上一节，我们详细介绍了 Transformer 的核心——注意力机制。在《Attention is All You Need》一文中，作者通过仅使用注意力机制而抛弃传统的 RNN、CNN 架构搭建出 Transformer 模型，从而带来了 NLP 领域的大变革。在 Transformer 中，使用注意力机制的是其两个核心组件——Encoder（编码器）和 Decoder（解码器）。事实上，后续基于 Transformer 架构而来的预训练语言模型基本都是对 Encoder-Decoder 部分进行改进来构建新的模型架构，例如只使用 Encoder 的 BERT、只使用 Decoder 的 GPT 等。</font><font size="5">&emsp;&emsp;在本节中，我们将以上一节所介绍的 注意力机制为基础，从 Transformer 所针对的 Seq2Seq 任务出发，解析 Transformer 的 Encoder-Decoder 结构。</font><h3 id="1-2-1-Seq2Seq-模型"><a href="#1-2-1-Seq2Seq-模型" class="headerlink" title="1.2.1 Seq2Seq 模型"></a>1.2.1 Seq2Seq 模型</h3><font size="5">&emsp;&emsp;Seq2Seq，即序列到序列，是一种经典 NLP 任务。具体而言，是指模型输入的是一个自然语言序列 $input=(x_1,x_2,x_3...x_n)$ ，输出的是一个可能不等长的自然语言序列 $output=(y_1,y_2,y_3...y_m)$。事实上，Seq2Seq 是 NLP 最经典的任务，几乎所有的 NLP 任务都可以视为 Seq2Seq 任务。例如文本分类任务，可以视为输出长度为 1 的目标序列（如在上式中 $m = 1$）；词性标注任务，可以视为输出与输入序列等长的目标序列（如在上式中 $m = n$）。</font><font size="5">&emsp;&emsp;机器翻译任务即是一个经典的 Seq2Seq 任务，例如，我们的输入可能是“今天天气真好”，输出是“Today is a good day.”。Transformer 是一个经典的 Seq2Seq 模型，即模型的输入为文本序列，输出为另一个文本序列。事实上，Transformer 一开始正是应用在机器翻译任务上的。</font><font size="5">&emsp;&emsp;对于 Seq2Seq 任务，一般的思路是对自然语言序列进行编码再解码。所谓编码，就是将输入的自然语言序列通过隐藏层编码成能够表征语义的向量（或矩阵），可以简单理解为更复杂的词向量表示。而解码，就是对输入的自然语言序列编码得到的向量或矩阵通过隐藏层输出，再解码成对应的自然语言目标序列。通过编码再解码，就可以实现 Seq2Seq 任务。</font><font size="5">&emsp;&emsp;Transformer 中的 Encoder，就是用于上述的编码过程；Decoder 则用于上述的解码过程。Transformer 由 Encoder 和 Decoder 组成，每一个 Encoder（Decoder）又由 6个 Encoder（Decoder）Layer 组成。输入源序列会进入 Encoder 进行编码，到 Encoder Layer 的最顶层再将编码结果输出给 Decoder Layer 的每一层，通过 Decoder 解码后就可以得到输出目标序列了。</font><font size="5">&emsp;&emsp;接下来，我们将首先介绍 Encoder 和 Decoder 内部传统神经网络的经典结构——前馈神经网络（FNN）、层归一化（Layer Norm）和残差连接（Residual Connection），然后进一步分析 Encoder 和 Decoder 的内部结构。</font><h3 id="1-2-2-前馈神经网络"><a href="#1-2-2-前馈神经网络" class="headerlink" title="1.2.2 前馈神经网络"></a>1.2.2 前馈神经网络</h3><font size="5">&emsp;&emsp;前馈神经网络（Feed Forward Neural Network，下简称 FNN），也就是我们在上一节提过的每一层的神经元都和上下两层的每一个神经元完全连接的网络结构。每一个 Encoder Layer 都包含一个上文讲的注意力机制和一个前馈神经网络。前馈神经网络的实现是较为简单的：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">MLP</span>(nn.Module):</span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;前馈神经网络&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, dim: <span class="built_in">int</span>, hidden_dim: <span class="built_in">int</span>, dropout: <span class="built_in">float</span></span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="comment"># 定义第一层线性变换，从输入维度到隐藏维度</span></span><br><span class="line">        <span class="variable language_">self</span>.w1 = nn.Linear(dim, hidden_dim, bias=<span class="literal">False</span>)</span><br><span class="line">        <span class="comment"># 定义第二层线性变换，从隐藏维度到输入维度</span></span><br><span class="line">        <span class="variable language_">self</span>.w2 = nn.Linear(hidden_dim, dim, bias=<span class="literal">False</span>)</span><br><span class="line">        <span class="comment"># 定义dropout层，用于防止过拟合</span></span><br><span class="line">        <span class="variable language_">self</span>.dropout = nn.Dropout(dropout)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="comment"># 前向传播函数</span></span><br><span class="line">        <span class="comment"># 首先，输入x通过第一层线性变换和RELU激活函数</span></span><br><span class="line">        <span class="comment"># 最后，通过第二层线性变换和dropout层</span></span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.dropout(<span class="variable language_">self</span>.w2(F.relu(<span class="variable language_">self</span>.w1(x))))</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;注意，Transformer 的前馈神经网络是由两个线性层中间加一个 RELU 激活函数组成的，以及前馈神经网络还加入了一个 Dropout 层来防止过拟合。</font><h3 id="1-2-3-层归一化"><a href="#1-2-3-层归一化" class="headerlink" title="1.2.3 层归一化"></a>1.2.3 层归一化</h3><font size="5">&emsp;&emsp;层归一化，也就是 Layer Norm，是深度学习中经典的归一化操作。神经网络主流的归一化一般有两种，批归一化（Batch Norm）和层归一化（Layer Norm）。</font><font size="5">&emsp;&emsp;归一化核心是为了让不同层输入的取值范围或者分布能够比较一致。由于深度神经网络中每一层的输入都是上一层的输出，因此多层传递下，对网络中较高的层，之前的所有神经层的参数变化会导致其输入的分布发生较大的改变。也就是说，随着神经网络参数的更新，各层的输出分布是不相同的，且差异会随着网络深度的增大而增大。但是，需要预测的条件分布始终是相同的，从而也就造成了预测的误差。</font><font size="5">&emsp;&emsp;</font><font size="5">&emsp;&emsp;因此，在深度神经网络中，往往需要归一化操作，将每一层的输入都归一化成标准正态分布。批归一化是指在一个 mini-batch 上进行归一化，相当于对一个 batch 对样本拆分出来一部分，首先计算样本的均值：</font><font size="5">&emsp;&emsp;$\mu_j = \frac{1}{m}\sum_{i=1}^mZ^i_j$</font><font size="5">&emsp;&emsp;其中，$Z_j^i$ 是样本 i 在第 j 个维度上的值，m 就是 mini-batch 的大小。</font><font size="5">&emsp;&emsp;再计算样本的方差：</font><font size="5">&emsp;&emsp;</font><font size="5">&emsp;&emsp;$\sigma^2 = \frac{1}{m}\sum_{i=1}^m(Z^i_j-\mu_j)^2$</font><font size="5">&emsp;&emsp;最后，对每个样本的值减去均值再除以标准差来将这一个 mini-batch 的样本的分布转化为标准正态分布：</font><font size="5">&emsp;&emsp;$\tilde{Z}_j = \frac{Z_j -\mu_j}{\sqrt{\sigma^2+\epsilon}}$</font><font size="5">&emsp;&emsp;此处加上 $ϵ$ 这一极小量是为了避免分母为0。</font><font size="5">&emsp;&emsp;</font><font size="5">&emsp;&emsp;但是，批归一化存在一些缺陷，例如：</font><ul><li><font size="5">当显存有限，mini-batch 较小时，Batch Norm 取的样本的均值和方差不能反映全局的统计分布信息，从而导致效果变差；</font></li><li><font size="5">对于在时间维度展开的 RNN，不同句子的同一分布大概率不同，所以 Batch Norm 的归一化会失去意义；</font></li><li><font size="5">在训练时，Batch Norm 需要保存每个 step 的统计信息（均值和方差）。在测试时，由于变长句子的特性，测试集可能出现比训练集更长的句子，所以对于后面位置的 step，是没有训练的统计量使用的；</font><font size="5">&emsp;&emsp;</font></li><li><font size="5">应用 Batch Norm，每个 step 都需要去保存和计算 batch 统计量，耗时又耗力</font></li></ul><font size="5">&emsp;&emsp;因此，出现了在深度神经网络中更常用、效果更好的层归一化（Layer Norm）。相较于 Batch Norm 在每一层统计所有样本的均值和方差，Layer Norm 在每个样本上计算其所有层的均值和方差，从而使每个样本的分布达到稳定。Layer Norm 的归一化方式其实和 Batch Norm 是完全一样的，只是统计统计量的维度不同。</font><font size="5">&emsp;&emsp;基于上述进行归一化的公式，我们可以简单地实现一个 Layer Norm 层：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">LayerNorm</span>(nn.Module):</span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27; Layer Norm 层&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, features, eps=<span class="number">1e-6</span></span>):</span><br><span class="line">    <span class="built_in">super</span>().__init__()</span><br><span class="line">    <span class="comment"># 线性矩阵做映射</span></span><br><span class="line">    <span class="variable language_">self</span>.a_2 = nn.Parameter(torch.ones(features))</span><br><span class="line">    <span class="variable language_">self</span>.b_2 = nn.Parameter(torch.zeros(features))</span><br><span class="line">    <span class="variable language_">self</span>.eps = eps</span><br><span class="line">    </span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">    <span class="comment"># 在统计每个样本所有维度的值，求均值和方差</span></span><br><span class="line">    mean = x.mean(-<span class="number">1</span>, keepdim=<span class="literal">True</span>) <span class="comment"># mean: [bsz, max_len, 1]</span></span><br><span class="line">    std = x.std(-<span class="number">1</span>, keepdim=<span class="literal">True</span>) <span class="comment"># std: [bsz, max_len, 1]</span></span><br><span class="line">    <span class="comment"># 注意这里也在最后一个维度发生了广播</span></span><br><span class="line">    <span class="keyword">return</span> <span class="variable language_">self</span>.a_2 * (x - mean) / (std + <span class="variable language_">self</span>.eps) + <span class="variable language_">self</span>.b_2</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;注意，在我们上文实现的 Layer Norm 层中，有两个线性矩阵进行映射。</font><h3 id="1-2-4-残差连接"><a href="#1-2-4-残差连接" class="headerlink" title="1.2.4 残差连接"></a>1.2.4 残差连接</h3><font size="5">&emsp;&emsp;由于 Transformer 模型结构较复杂、层数较深，为了避免模型退化，Transformer 采用了残差连接的思想来连接每一个子层。残差连接，即下一层的输入不仅是上一层的输出，还包括上一层的输入。残差连接允许最底层信息直接传到最高层，让高层专注于残差的学习。</font><font size="5">&emsp;&emsp;例如，在 Encoder 中，在第一个子层，输入进入多头自注意力层的同时会直接传递到该层的输出，然后该层的输出会与原输入相加，再进行标准化。在第二个子层也是一样。即：</font><font size="5">&emsp;&emsp;$x=x+MultiHeadSelfAttention(LayerNorm(x))$</font><font size="5">&emsp;&emsp;$output = x + FNN(LayerNorm(x))$</font><font size="5">&emsp;&emsp;</font><font size="5">&emsp;&emsp;我们在代码实现中，通过在层的 forward 计算中加上原值来实现残差连接：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 注意力计算</span></span><br><span class="line">h = x + <span class="variable language_">self</span>.attention.forward(<span class="variable language_">self</span>.attention_norm(x))</span><br><span class="line"><span class="comment"># 经过前馈神经网络</span></span><br><span class="line">out = h + <span class="variable language_">self</span>.feed_forward.forward(<span class="variable language_">self</span>.fnn_norm(h))</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;在上文代码中，self.attention_norm 和 self.fnn_norm 都是 LayerNorm 层，self.attn 是注意力层，而 self.feed_forward 是前馈神经网络。</font><h3 id="1-2-5-Encoder"><a href="#1-2-5-Encoder" class="headerlink" title="1.2.5 Encoder"></a>1.2.5 Encoder</h3><font size="5">&emsp;&emsp;在实现上述组件之后，我们可以搭建起 Transformer 的 Encoder。Encoder 由 N 个 Encoder Layer 组成，每一个 Encoder Layer 包括一个注意力层和一个前馈神经网络。因此，我们可以首先实现一个 Encoder Layer：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">EncoderLayer</span>(nn.Module):</span><br><span class="line">  <span class="string">&#x27;&#x27;&#x27;Encoder层&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, args</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="comment"># 一个 Layer 中有两个 LayerNorm，分别在 Attention 之前和 MLP 之前</span></span><br><span class="line">        <span class="variable language_">self</span>.attention_norm = LayerNorm(args.n_embd)</span><br><span class="line">        <span class="comment"># Encoder 不需要掩码，传入 is_causal=False</span></span><br><span class="line">        <span class="variable language_">self</span>.attention = MultiHeadAttention(args, is_causal=<span class="literal">False</span>)</span><br><span class="line">        <span class="variable language_">self</span>.fnn_norm = LayerNorm(args.n_embd)</span><br><span class="line">        <span class="variable language_">self</span>.feed_forward = MLP(args.dim, args.dim, args.dropout)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="comment"># Layer Norm</span></span><br><span class="line">        norm_x = <span class="variable language_">self</span>.attention_norm(x)</span><br><span class="line">        <span class="comment"># 自注意力</span></span><br><span class="line">        h = x + <span class="variable language_">self</span>.attention.forward(norm_x, norm_x, norm_x)</span><br><span class="line">        <span class="comment"># 经过前馈神经网络</span></span><br><span class="line">        out = h + <span class="variable language_">self</span>.feed_forward.forward(<span class="variable language_">self</span>.fnn_norm(h))</span><br><span class="line">        <span class="keyword">return</span> out</span><br><span class="line"></span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;然后我们搭建一个 Encoder，由 N 个 Encoder Layer 组成，在最后会加入一个 Layer Norm 实现规范化：</font><font size="5">&emsp;&emsp;</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Encoder</span>(nn.Module):</span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;Encoder 块&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, args</span>):</span><br><span class="line">        <span class="built_in">super</span>(Encoder, <span class="variable language_">self</span>).__init__() </span><br><span class="line">        <span class="comment"># 一个 Encoder 由 N 个 Encoder Layer 组成</span></span><br><span class="line">        <span class="variable language_">self</span>.layers = nn.ModuleList([EncoderLayer(args) <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(args.n_layer)])</span><br><span class="line">        <span class="variable language_">self</span>.norm = LayerNorm(args.n_embd)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="string">&quot;分别通过 N 层 Encoder Layer&quot;</span></span><br><span class="line">        <span class="keyword">for</span> layer <span class="keyword">in</span> <span class="variable language_">self</span>.layers:</span><br><span class="line">            x = layer(x)</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.norm(x)</span><br><span class="line"></span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;通过 Encoder 的输出，就是输入编码之后的结果。</font><h3 id="1-2-6-Decoder"><a href="#1-2-6-Decoder" class="headerlink" title="1.2.6 Decoder"></a>1.2.6 Decoder</h3><font size="5">&emsp;&emsp;类似的，我们也可以先搭建 Decoder Layer，再将 N 个 Decoder Layer 组装为 Decoder。但是和 Encoder 不同的是，Decoder 由两个注意力层和一个前馈神经网络组成。第一个注意力层是一个掩码自注意力层，即使用 Mask 的注意力计算，保证每一个 token 只能使用该 token 之前的注意力分数；第二个注意力层是一个多头注意力层，该层将使用第一个注意力层的输出作为 query，使用 Encoder 的输出作为 key 和 value，来计算注意力分数。最后，再经过前馈神经网络：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">DecoderLayer</span>(nn.Module):</span><br><span class="line">  <span class="string">&#x27;&#x27;&#x27;解码层&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, args</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="comment"># 一个 Layer 中有三个 LayerNorm，分别在 Mask Attention 之前、Self Attention 之前和 MLP 之前</span></span><br><span class="line">        <span class="variable language_">self</span>.attention_norm_1 = LayerNorm(args.n_embd)</span><br><span class="line">        <span class="comment"># Decoder 的第一个部分是 Mask Attention，传入 is_causal=True</span></span><br><span class="line">        <span class="variable language_">self</span>.mask_attention = MultiHeadAttention(args, is_causal=<span class="literal">True</span>)</span><br><span class="line">        <span class="variable language_">self</span>.attention_norm_2 = LayerNorm(args.n_embd)</span><br><span class="line">        <span class="comment"># Decoder 的第二个部分是 类似于 Encoder 的 Attention，传入 is_causal=False</span></span><br><span class="line">        <span class="variable language_">self</span>.attention = MultiHeadAttention(args, is_causal=<span class="literal">False</span>)</span><br><span class="line">        <span class="variable language_">self</span>.ffn_norm = LayerNorm(args.n_embd)</span><br><span class="line">        <span class="comment"># 第三个部分是 MLP</span></span><br><span class="line">        <span class="variable language_">self</span>.feed_forward = MLP(args.dim, args.dim, args.dropout)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x, enc_out</span>):</span><br><span class="line">        <span class="comment"># Layer Norm</span></span><br><span class="line">        norm_x = <span class="variable language_">self</span>.attention_norm_1(x)</span><br><span class="line">        <span class="comment"># 掩码自注意力</span></span><br><span class="line">        x = x + <span class="variable language_">self</span>.mask_attention.forward(norm_x, norm_x, norm_x)</span><br><span class="line">        <span class="comment"># 多头注意力</span></span><br><span class="line">        norm_x = <span class="variable language_">self</span>.attention_norm_2(x)</span><br><span class="line">        h = x + <span class="variable language_">self</span>.attention.forward(norm_x, enc_out, enc_out)</span><br><span class="line">        <span class="comment"># 经过前馈神经网络</span></span><br><span class="line">        out = h + <span class="variable language_">self</span>.feed_forward.forward(<span class="variable language_">self</span>.ffn_norm(h))</span><br><span class="line">        <span class="keyword">return</span> out</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;然后同样的，我们搭建一个 Decoder 块：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Decoder</span>(nn.Module):</span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;解码器&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, args</span>):</span><br><span class="line">        <span class="built_in">super</span>(Decoder, <span class="variable language_">self</span>).__init__() </span><br><span class="line">        <span class="comment"># 一个 Decoder 由 N 个 Decoder Layer 组成</span></span><br><span class="line">        <span class="variable language_">self</span>.layers = nn.ModuleList([DecoderLayer(args) <span class="keyword">for</span> _ <span class="keyword">in</span> <span class="built_in">range</span>(args.n_layer)])</span><br><span class="line">        <span class="variable language_">self</span>.norm = LayerNorm(args.n_embd)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x, enc_out</span>):</span><br><span class="line">        <span class="string">&quot;Pass the input (and mask) through each layer in turn.&quot;</span></span><br><span class="line">        <span class="keyword">for</span> layer <span class="keyword">in</span> <span class="variable language_">self</span>.layers:</span><br><span class="line">            x = layer(x, enc_out)</span><br><span class="line">        <span class="keyword">return</span> <span class="variable language_">self</span>.norm(x)</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;完成上述 Encoder、Decoder 的搭建，就完成了 Transformer 的核心部分，接下来将 Encoder、Decoder 拼接起来再加入 Embedding 层就可以搭建出完整的 Transformer 模型啦。</font><font size="5">&emsp;&emsp;</font><h2 id="1-3-搭建一个Transformer"><a href="#1-3-搭建一个Transformer" class="headerlink" title="1.3 搭建一个Transformer"></a>1.3 搭建一个Transformer</h2><font size="5">&emsp;&emsp;在前两章，我们分别深入剖析了 Attention 机制和 Transformer 的核心——Encoder、Decoder 结构，接下来，我们就可以基于上一章实现的组件，搭建起一个完整的 Transformer 模型。</font><h3 id="1-3-1-Embedding层"><a href="#1-3-1-Embedding层" class="headerlink" title="1.3.1 Embedding层"></a>1.3.1 Embedding层</h3><font size="5">&emsp;&emsp;正如我们在第一章所讲过的，在 NLP 任务中，我们往往需要将自然语言的输入转化为机器可以处理的向量。在深度学习中，承担这个任务的组件就是 Embedding 层。</font><font size="5">&emsp;&emsp;Embedding 层其实是一个存储固定大小的词典的嵌入向量查找表。也就是说，在输入神经网络之前，我们往往会先让自然语言输入通过分词器 tokenizer，分词器的作用是把自然语言输入切分成 token 并转化成一个固定的 index。例如，如果我们将词表大小设为 4，输入“我喜欢你”，那么，分词器可以将输入转化成：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">input: 我</span><br><span class="line">output: 0</span><br><span class="line"></span><br><span class="line">input: 喜欢</span><br><span class="line">output: 1</span><br><span class="line"></span><br><span class="line">input：你</span><br><span class="line">output: 2</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;当然，在实际情况下，tokenizer 的工作会比这更复杂。例如，分词有多种不同的方式，可以切分成词、切分成子词、切分成字符等，而词表大小则往往高达数万数十万。此处我们不赘述 tokenizer 的详细情况，在后文会详细介绍大模型的 tokenizer 是如何运行和训练的。</font><font size="5">&emsp;&emsp;</font><font size="5">&emsp;&emsp;因此，Embedding 层的输入往往是一个形状为 （batch_size，seq_len，1）的矩阵，第一个维度是一次批处理的数量，第二个维度是自然语言序列的长度，第三个维度则是 token 经过 tokenizer 转化成的 index 值。例如，对上述输入，Embedding 层的输入会是：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">[[[<span class="number">0</span>],[<span class="number">1</span>],[<span class="number">2</span>]]]</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;其 batch_size 为1，seq_len 为3，转化出来的 index 如上。</font><font size="5">&emsp;&emsp;而 Embedding 内部其实是一个可训练的（Vocab_size，embedding_dim）的权重矩阵，词表里的每一个值，都对应一行维度为 embedding_dim 的向量。对于输入的值，会对应到这个词向量，然后拼接成（batch_size，seq_len，embedding_dim）的矩阵输出。</font><font size="5">&emsp;&emsp;上述实现并不复杂，我们可以直接使用 torch 中的 Embedding 层：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line"><span class="variable language_">self</span>.tok_embeddings = nn.Embedding(args.vocab_size, args.dim)</span><br></pre></td></tr></table></figure><h3 id="1-3-2-位置编码"><a href="#1-3-2-位置编码" class="headerlink" title="1.3.2 位置编码"></a>1.3.2 位置编码</h3><font size="5">&emsp;&emsp;注意力机制可以实现良好的并行计算，但同时，其注意力计算的方式也导致序列中相对位置的丢失。在 RNN、LSTM 中，输入序列会沿着语句本身的顺序被依次递归处理，因此输入序列的顺序提供了极其重要的信息，这也和自然语言的本身特性非常吻合。</font><font size="5">&emsp;&emsp;但从上文对注意力机制的分析我们可以发现，在注意力机制的计算过程中，对于序列中的每一个 token，其他各个位置对其来说都是平等的，即“我喜欢你”和“你喜欢我”在注意力机制看来是完全相同的，但无疑这是注意力机制存在的一个巨大问题。因此，为使用序列顺序信息，保留序列中的相对位置信息，Transformer 采用了位置编码机制，该机制也在之后被多种模型沿用。</font><font size="5">&emsp;&emsp;位置编码，即根据序列中 token 的相对位置对其进行编码，再将位置编码加入词向量编码中。位置编码的方式有很多，Transformer 使用了正余弦函数来进行位置编码（绝对位置编码Sinusoidal），其编码方式为：</font><font size="5">&emsp;&emsp;$PE(pos,2i)=sin(pos/10000^{2i/d_{model}})$</font><font size="5">&emsp;&emsp;$PE(pos,2i+1)=cos(pos/10000^{2i/d_{model}})$</font><font size="5">&emsp;&emsp;</font><font size="5">&emsp;&emsp;上式中，pos 为 token 在句子中的位置，2i 和 2i+1 则是指示了 token 是奇数位置还是偶数位置，从上式中我们可以看出对于奇数位置的 token 和偶数位置的 token，Transformer 采用了不同的函数进行编码。</font><font size="5">&emsp;&emsp;我们以一个简单的例子来说明位置编码的计算过程：假如我们输入的是一个长度为 4 的句子"I like to code"，我们可以得到下面的词向量矩阵 $x$，其中每一行代表的就是一个词向量， $x_0=[0.1,0.2,0.3,0.4]$ 对应的就是“I”的词向量，它的pos就是为0，以此类推，第二行代表的是“like”的词向量，它的pos就是1：</font><font size="5">&emsp;&emsp;$x=\begin{bmatrix} 0.1&0.2&0.3&0.4\\ 0.2&0.3&0.4&0.5\\ 0.3&0.4&0.5&0.6\\0.4&0.5&0.6&0.7\\ \end{bmatrix}$</font><font size="5">&emsp;&emsp;则经过位置编码后的词向量为：</font><font size="5">&emsp;&emsp;</font><p><img src="/posts/9119f4d4.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20251009130603754.png" alt="image-20251009130603754"></p><font size="5">&emsp;&emsp;这样的位置编码主要有两个好处：</font><font size="5">&emsp;&emsp;1️⃣使 PE 能够适应比训练集里面所有句子更长的句子，假设训练集里面最长的句子是有 20 个单词，突然来了一个长度为 21 的句子，则使用公式计算的方法可以计算出第 21 位的 Embedding。</font><font size="5">&emsp;&emsp;2️⃣可以让模型容易地计算出相对位置，对于固定长度的间距 $k$，$PE(pos+k)$ 可以用 $PE(pos)$ 计算得到。因为 $\sin(A+B) = \sin(A)\cos(B) + \cos(A)\sin(B)$, $\cos(A+B) = \cos(A)\cos(B) - \sin(A)\sin(B)$。</font><font size="5">&emsp;&emsp;基于上述原理，我们实现一个位置编码层：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"></span><br><span class="line"><span class="keyword">class</span> <span class="title class_">PositionalEncoding</span>(nn.Module):</span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;位置编码模块&#x27;&#x27;&#x27;</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, args</span>):</span><br><span class="line">        <span class="built_in">super</span>(PositionalEncoding, <span class="variable language_">self</span>).__init__()</span><br><span class="line">        <span class="comment"># Dropout 层</span></span><br><span class="line">        <span class="comment"># self.dropout = nn.Dropout(p=args.dropout)</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># block size 是序列的最大长度</span></span><br><span class="line">        pe = torch.zeros(args.block_size, args.n_embd)</span><br><span class="line">        position = torch.arange(<span class="number">0</span>, args.block_size).unsqueeze(<span class="number">1</span>)</span><br><span class="line">        <span class="comment"># 计算 theta</span></span><br><span class="line">        div_term = torch.exp(</span><br><span class="line">            torch.arange(<span class="number">0</span>, args.n_embd, <span class="number">2</span>) * -(math.log(<span class="number">10000.0</span>) / args.n_embd)</span><br><span class="line">        )</span><br><span class="line">        <span class="comment"># 分别计算 sin、cos 结果</span></span><br><span class="line">        pe[:, <span class="number">0</span>::<span class="number">2</span>] = torch.sin(position * div_term)</span><br><span class="line">        pe[:, <span class="number">1</span>::<span class="number">2</span>] = torch.cos(position * div_term)</span><br><span class="line">        pe = pe.unsqueeze(<span class="number">0</span>)</span><br><span class="line">        <span class="variable language_">self</span>.register_buffer(<span class="string">&quot;pe&quot;</span>, pe)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, x</span>):</span><br><span class="line">        <span class="comment"># 将位置编码加到 Embedding 结果上</span></span><br><span class="line">        x = x + <span class="variable language_">self</span>.pe[:, : x.size(<span class="number">1</span>)].requires_grad_(<span class="literal">False</span>)</span><br><span class="line">        <span class="keyword">return</span> x</span><br><span class="line"></span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;基于之前所实现过的组件，我们实现完整的 Transformer 模型：</font><figure class="highlight python"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br><span class="line">51</span><br><span class="line">52</span><br><span class="line">53</span><br><span class="line">54</span><br><span class="line">55</span><br><span class="line">56</span><br><span class="line">57</span><br><span class="line">58</span><br><span class="line">59</span><br><span class="line">60</span><br><span class="line">61</span><br><span class="line">62</span><br><span class="line">63</span><br><span class="line">64</span><br><span class="line">65</span><br><span class="line">66</span><br><span class="line">67</span><br><span class="line">68</span><br><span class="line">69</span><br><span class="line">70</span><br><span class="line">71</span><br><span class="line">72</span><br><span class="line">73</span><br><span class="line">74</span><br><span class="line">75</span><br><span class="line">76</span><br><span class="line">77</span><br><span class="line">78</span><br><span class="line">79</span><br><span class="line">80</span><br><span class="line">81</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">class</span> <span class="title class_">Transformer</span>(nn.Module):</span><br><span class="line">   <span class="string">&#x27;&#x27;&#x27;整体模型&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">__init__</span>(<span class="params">self, args</span>):</span><br><span class="line">        <span class="built_in">super</span>().__init__()</span><br><span class="line">        <span class="comment"># 必须输入词表大小和 block size</span></span><br><span class="line">        <span class="keyword">assert</span> args.vocab_size <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span></span><br><span class="line">        <span class="keyword">assert</span> args.block_size <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span></span><br><span class="line">        <span class="variable language_">self</span>.args = args</span><br><span class="line">        <span class="variable language_">self</span>.transformer = nn.ModuleDict(<span class="built_in">dict</span>(</span><br><span class="line">            wte = nn.Embedding(args.vocab_size, args.n_embd),</span><br><span class="line">            wpe = PositionalEncoding(args),</span><br><span class="line">            drop = nn.Dropout(args.dropout),</span><br><span class="line">            encoder = Encoder(args),</span><br><span class="line">            decoder = Decoder(args),</span><br><span class="line">        ))</span><br><span class="line">        <span class="comment"># 最后的线性层，输入是 n_embd，输出是词表大小</span></span><br><span class="line">        <span class="variable language_">self</span>.lm_head = nn.Linear(args.n_embd, args.vocab_size, bias=<span class="literal">False</span>)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 初始化所有的权重</span></span><br><span class="line">        <span class="variable language_">self</span>.apply(<span class="variable language_">self</span>._init_weights)</span><br><span class="line"></span><br><span class="line">        <span class="comment"># 查看所有参数的数量</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;number of parameters: %.2fM&quot;</span> % (<span class="variable language_">self</span>.get_num_params()/<span class="number">1e6</span>,))</span><br><span class="line"></span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;统计所有参数的数量&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">get_num_params</span>(<span class="params">self, non_embedding=<span class="literal">False</span></span>):</span><br><span class="line">        <span class="comment"># non_embedding: 是否统计 embedding 的参数</span></span><br><span class="line">        n_params = <span class="built_in">sum</span>(p.numel() <span class="keyword">for</span> p <span class="keyword">in</span> <span class="variable language_">self</span>.parameters())</span><br><span class="line">        <span class="comment"># 如果不统计 embedding 的参数，就减去</span></span><br><span class="line">        <span class="keyword">if</span> non_embedding:</span><br><span class="line">            n_params -= <span class="variable language_">self</span>.transformer.wte.weight.numel()</span><br><span class="line">        <span class="keyword">return</span> n_params</span><br><span class="line"></span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;初始化权重&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">_init_weights</span>(<span class="params">self, module</span>):</span><br><span class="line">        <span class="comment"># 线性层和 Embedding 层初始化为正则分布</span></span><br><span class="line">        <span class="keyword">if</span> <span class="built_in">isinstance</span>(module, nn.Linear):</span><br><span class="line">            torch.nn.init.normal_(module.weight, mean=<span class="number">0.0</span>, std=<span class="number">0.02</span>)</span><br><span class="line">            <span class="keyword">if</span> module.bias <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">                torch.nn.init.zeros_(module.bias)</span><br><span class="line">        <span class="keyword">elif</span> <span class="built_in">isinstance</span>(module, nn.Embedding):</span><br><span class="line">            torch.nn.init.normal_(module.weight, mean=<span class="number">0.0</span>, std=<span class="number">0.02</span>)</span><br><span class="line">    </span><br><span class="line">    <span class="string">&#x27;&#x27;&#x27;前向计算函数&#x27;&#x27;&#x27;</span></span><br><span class="line">    <span class="keyword">def</span> <span class="title function_">forward</span>(<span class="params">self, idx, targets=<span class="literal">None</span></span>):</span><br><span class="line">        <span class="comment"># 输入为 idx，维度为 (batch size, sequence length, 1)；targets 为目标序列，用于计算 loss</span></span><br><span class="line">        device = idx.device</span><br><span class="line">        b, t = idx.size()</span><br><span class="line">        <span class="keyword">assert</span> t &lt;= <span class="variable language_">self</span>.args.block_size, <span class="string">f&quot;不能计算该序列，该序列长度为 <span class="subst">&#123;t&#125;</span>, 最大序列长度只有 <span class="subst">&#123;self.args.block_size&#125;</span>&quot;</span></span><br><span class="line"></span><br><span class="line">        <span class="comment"># 通过 self.transformer</span></span><br><span class="line">        <span class="comment"># 首先将输入 idx 通过 Embedding 层，得到维度为 (batch size, sequence length, n_embd)</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;idx&quot;</span>,idx.size())</span><br><span class="line">        <span class="comment"># 通过 Embedding 层</span></span><br><span class="line">        tok_emb = <span class="variable language_">self</span>.transformer.wte(idx)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;tok_emb&quot;</span>,tok_emb.size())</span><br><span class="line">        <span class="comment"># 然后通过位置编码</span></span><br><span class="line">        pos_emb = <span class="variable language_">self</span>.transformer.wpe(tok_emb) </span><br><span class="line">        <span class="comment"># 再进行 Dropout</span></span><br><span class="line">        x = <span class="variable language_">self</span>.transformer.drop(pos_emb)</span><br><span class="line">        <span class="comment"># 然后通过 Encoder</span></span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;x after wpe:&quot;</span>,x.size())</span><br><span class="line">        enc_out = <span class="variable language_">self</span>.transformer.encoder(x)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;enc_out:&quot;</span>,enc_out.size())</span><br><span class="line">        <span class="comment"># 再通过 Decoder</span></span><br><span class="line">        x = <span class="variable language_">self</span>.transformer.decoder(x, enc_out)</span><br><span class="line">        <span class="built_in">print</span>(<span class="string">&quot;x after decoder:&quot;</span>,x.size())</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> targets <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">            <span class="comment"># 训练阶段，如果我们给了 targets，就计算 loss</span></span><br><span class="line">            <span class="comment"># 先通过最后的 Linear 层，得到维度为 (batch size, sequence length, vocab size)</span></span><br><span class="line">            logits = <span class="variable language_">self</span>.lm_head(x)</span><br><span class="line">            <span class="comment"># 再跟 targets 计算交叉熵</span></span><br><span class="line">            loss = F.cross_entropy(logits.view(-<span class="number">1</span>, logits.size(-<span class="number">1</span>)), targets.view(-<span class="number">1</span>), ignore_index=-<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            <span class="comment"># 推理阶段，我们只需要 logits，loss 为 None</span></span><br><span class="line">            <span class="comment"># 取 -1 是只取序列中的最后一个作为输出</span></span><br><span class="line">            logits = <span class="variable language_">self</span>.lm_head(x[:, [-<span class="number">1</span>], :]) <span class="comment"># note: using list [-1] to preserve the time dim</span></span><br><span class="line">            loss = <span class="literal">None</span></span><br><span class="line"></span><br><span class="line">        <span class="keyword">return</span> logits, loss</span><br></pre></td></tr></table></figure><font size="5">&emsp;&emsp;经过 tokenizer 映射后的输出先经过 Embedding 层和 Positional Embedding 层编码，然后进入上一节讲过的 N 个 Encoder 和 N 个 Decoder（在 Transformer 原模型中，N 取为6），最后经过一个线性层和一个 Softmax 层就得到了最终输出。</font>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>哈基鹏的大模型之路（二）</title>
      <link href="/posts/ec11bb5g.html"/>
      <url>/posts/ec11bb5g.html</url>
      
        <content type="html"><![CDATA[<h1 id="💥从零打造专属领域大模型💥"><a href="#💥从零打造专属领域大模型💥" class="headerlink" title="💥从零打造专属领域大模型💥"></a>💥从零打造专属领域大模型💥</h1><h1 id="1-LLaMA-Factory-安装"><a href="#1-LLaMA-Factory-安装" class="headerlink" title="1.LLaMA Factory 安装"></a>1.LLaMA Factory 安装</h1><font size="5">在部署好虚拟环境后，运行以下指令以安装LLaMA-Factory及其依赖：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line">git clone --depth 1 https://github.com/hiyouga/LLaMA-Factory.git</span><br><span class="line">cd LLaMA-Factory</span><br><span class="line">pip install -e &quot;.[torch,metrics]&quot;</span><br></pre></td></tr></table></figure><font size="5">安装后，可以通过使用一下命令，判断是否安装成功：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llamafactory-cli version</span><br></pre></td></tr></table></figure><font size="5">如果出现如下页面，就表明安装成功：</font><p><img src="/posts/ec11bb5g.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20250926140901535.png" alt="image-20250926140901535"></p><font size="5">LLaMA-Factory 支持通过WebUI 零代码微调大语言模型。在完成安装后，通过以下指令进入WebUI：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">llamafactory-cli webui</span><br></pre></td></tr></table></figure><font size="5">启动后就会自动帮你跳转到可视化网址：http://127.0.0.1:7860</font><p><img src="/posts/ec11bb5g.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20250926141342354.png" alt="image-20250926141342354"></p><p><img src="/posts/ec11bb5g.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20250926142413939.png" alt="image-20250926142413939"></p><h1 id="2-选择预训练模型"><a href="#2-选择预训练模型" class="headerlink" title="2.选择预训练模型"></a>2.选择预训练模型</h1><font size="5">第一步，选择用于微调的基础模型：（这里以Qwen2.5-1.5B-Instruct为例）</font><p><img src="/posts/ec11bb5g.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20250926142731977.png" alt="image-20250926142731977"></p><font size="5">在微调前，选择一个合适的基础模型非常重要，下面我们再具体介绍一下模型名称中其他参数代表的含义：</font><div class="table-container"><table><thead><tr><th>分类</th><th>标识</th><th>含义</th><th>示例（模型名称）</th></tr></thead><tbody><tr><td>功能与任务类型</td><td><code>-Base</code></td><td>基础模型，未经过特定任务微调，提供原始能力（用于二次开发）。</td><td>Qwen3-14B-Base</td></tr><tr><td></td><td><code>-Chat</code></td><td>对话优化模型，支持交互式聊天、问答，针对对话生活任务微调。</td><td>DeepSeek-LLM-7B-Chat</td></tr><tr><td></td><td><code>-Instruct</code></td><td>指令微调模型，擅长遵循具体任务指令（推理、生成、翻译等）。</td><td>Qwen3-0.6B-Instruct</td></tr><tr><td></td><td><code>-Distill</code></td><td>知识蒸馏模型，通过蒸馏技术压缩，模型更小，推理更高效。</td><td>DeepSeek-R1-1.5B-Distill</td></tr><tr><td></td><td><code>-Math</code></td><td>专注数学推理任务，优化数值计算、公式解析、逻辑证明等能力。</td><td>DeepSeek-Math-7B-Instruct</td></tr><tr><td></td><td><code>-Coder</code></td><td>针对代码生成、编程任务优化、支持代码补全、漏洞检测、算法实现等。</td><td>DeepSeek-Coder-V2-16B</td></tr><tr><td>多模态</td><td><code>-VL</code></td><td>视觉-语言多模态（Vision-Language），支持图文联合输入输出。</td><td>Kimi-VL-A3B</td></tr><tr><td></td><td><code>-Video</code></td><td>视频多模态模型，结合视频帧与文本进行交互。</td><td>LLaVA-NeXT-Video-7B-Chat</td></tr><tr><td></td><td><code>-Audio</code></td><td>支持音频输入输出，涉及语音识别（ASR）或语音生成（TTS）。</td><td>Qwen2-Audio-7B</td></tr><tr><td>技术特性与优化</td><td><code>-Int8/-Int4</code></td><td>权重量化为8位/4位，降低显存占用，提升推理速度（适合低资源设备）。</td><td>Qwen2-VL-2B-Instruct-GPTQ-Int8</td></tr><tr><td></td><td><code>-AWQ/-GPTQ</code></td><td>特定量化技术（自适应权重/GPTQ量化），优化低精度下的模型性能。</td><td>Qwen2.5-VL-72B-Instruct-AWQ</td></tr><tr><td></td><td><code>-MoE</code></td><td>混合专家模型（Mixture of Experts），包含多个专用模块处理复杂任务。</td><td>DeepSeek-MoE-16B-Chat</td></tr><tr><td></td><td><code>-RL</code></td><td>使用强化学习（Reinforcement Learing）优化，提升对话质量或任务响应。</td><td>MiMo-7B-Instruct-RL</td></tr><tr><td>版本与变体标识</td><td><code>-v0.1/-v0.2</code></td><td>模型版本号，标识开发阶段（alpha/beta/正式版）。</td><td>Mistral-7B-v0.1</td></tr><tr><td></td><td><code>-Pure</code></td><td>纯净版模型，去除领域数据或保留原始能力，避免预训练编差</td><td>Index-1.9B-Base</td></tr><tr><td></td><td><code>-Character</code></td><td>角色对话模型，专注角色扮演或特定人设（如虚拟助手，动漫角色）</td><td>Index-1.9B-Character-Chat</td></tr><tr><td></td><td><code>-Long-Chat</code></td><td>支持长上下文对话（通常&gt;4k tokens）,处理超长输入输出</td><td>Orion-14B-Long-Chat</td></tr><tr><td>领域与应用标识</td><td><code>-RAG</code></td><td>检索增强生成模型，结合外部知识库检索与生成能力</td><td>Orion-14B-RAG-Chat</td></tr><tr><td></td><td><code>-Chinese</code></td><td>中文优化版本、支持中文分词、方言、拼音纠错等本土化能力。</td><td>Llama-3-70B-Chinese-Chat</td></tr><tr><td></td><td><code>-MT</code></td><td>机器翻译专用模型、支持多语言翻译任务（如中英、英日互译）</td><td>BLOOMZ-7B1-MT</td></tr></tbody></table></div><font size="5">选取模型后，模型路径的位置会自动关联Hugging Face 上的模型标识，如果你没有在本地下载过此模型，LLaMA Factory 将自动从Hugging Face 上拉取此模型。</font><font size="5">选择好模型后，点击下方Chat Tab，然后直接点击加载模型；然后就可以和模型进行对话</font><p><img src="/posts/ec11bb5g.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20250928102132703.png" alt="image-20250928102132703"></p><h1 id="3-微调大模型"><a href="#3-微调大模型" class="headerlink" title="3.微调大模型"></a>3.微调大模型</h1><font size="5">LLaMA Factory的微调方法分别为：full、freeze、lora</font><font size="5">🦒**全参数微调（Full Parameter Fine-tuning）**：更新预训练模型的所有参数</font><font size="5">其特点：1.适应能力强；2.需要较大的计算资源；3.可能导致过拟合；4.存储成本高；5.训练时间长</font><font size="5">🦆**冻结微调（Freeze Fine-tuning）**：冻结部分层，只更新部分参数</font><font size="5">其特点：1.减少计算资源；2.保留底层特征提取能力；3.只训练任务相关参数；4.训练更快；5.平衡性能与资源消耗</font><font size="5">🐟**低秩矩阵微调（Lora）**：通过低秩适配矩阵更新参数</font><font size="5">其特点：1.参数量极大减少；2.存储高效；3.训练速度快；4.适合资源受限的场景；5.可组合多个适配器</font><h1 id="4-模型量化与蒸馏"><a href="#4-模型量化与蒸馏" class="headerlink" title="4.模型量化与蒸馏"></a>4.模型量化与蒸馏</h1><font size="5">模型的**量化**还有**蒸馏**，其实都是属于模型压缩的常见方法。对于一些大参数模型，比如前段时间非常火的 `DeepSeek-R1` 满血版具有 6710 亿个参数、最新的 `Qwen3` 满血版有 2350 亿个参数，它们都是各自公司下的旗舰版模型，具备着最先进的能力。但是由于参数量巨大，普通的小公司或者个人想要在本地部署模型需要的成本是非常高的，所以我们一般会选择使用量化、蒸馏这些手段对模型进行压缩，从而在损失一定精度的情况下减小模型的部署成本。</font><font size="5">模型量化是一种通过降低权重和激活值的精度来减小模型尺寸、加快推理速度，同时尽量保持模型准确率的技术。简单来说，就是用更少的数字位数来表示模型中的数据</font><font size="5">大模型本质上是由很多参数构成的，比如 `DeepSeek-R1` 满血版具有 6710 亿个参数。大模型通常是基于深度学习的神经网络架构，由大量的神经元和连接组成。这些参数主要包括神经元之间的连接权重以及偏置项等。在模型的训练过程中，这些参数会通过反向传播算法等优化方法不断调整和更新，以最小化损失函数，从而使模型能够学习到数据中的规律和特征。而在计算机中，这些参数是以数字的形式存储和计算的，通常使用浮点数（如单精度浮点数 float32 或双精度浮点数 float64）来表示，以在精度和计算效率之间取得平衡</font><font size="5">为了表示这些参数，我们需要在计算机上开辟一些内存空间，开辟多大的内存空间就取决于这些参数存储的精度。如果参数的精度越低，所需的内存空间就越小，反之参数精度越高，所需内存空间就越大。比如我们把原本存储了 7 位小数的一个参数：3.1415926，转换成一个精度更低的值：3.14 来进行存储，很明显我们丢掉的精度仅仅为 0.0015926 ，但是我们却大大节省了存储空间，模型量化的本质其实就是把模型的参数从高精度转换成低精度的过程。我们在 Hugging Face 上可以看到模型参数的存储精度，比如 Qwen3-235B 的参数使用的是 BF16</font><font size="5">BF 是属于一种模型参数的精度单位，常用的精度单位有：</font><ul><li><font size="5">**FP**：浮点（Floating Point），如 FP16、FP32，可以理解为科学计数法表示的数字，包含符号位、指数部分和尾数部分；**优点：可表示范围广，精度高**、**缺点：占用空间较大**</font></li><li><font size="5">**BF**：脑浮点（Brain Floating Point），如 BF16，专为深度学习设计的浮点数格式，相比传统浮点数，在保持数值范围的同时减少精度，平衡了精度和范围的需求，**兼顾精度与效率**。</font></li><li><font size="5">**INT**:整数（Integer），如 INT8、INT4，用整数表示权重和激活值，压缩模型体积并加速推理。**优点：结构简单，占用空间小、缺点：表示范围有限，精度丢失较多。**</font></li></ul><h1 id="5-加速方式"><a href="#5-加速方式" class="headerlink" title="5.加速方式"></a>5.加速方式</h1><p><img src="/posts/ec11bb5g.htm/Users\yingteng\AppData\Roaming\Typora\typora-user-images\image-20250928104408961.png" alt="image-20250928104408961"></p><font size="5">**Auto**：默认为FlashAttention。</font><font size="5">**FlashAttention**：能够加快注意力机制的运算速度，同时减少对内存的使用</font><font size="5">**Unsloth**：该框架支持Llama，Mistral，Phi-3，Gemma，Yi，DeepSeek，Qwen等大语言模型并支持 4-bit 和 16-bit 的QLoRA/LoRA 微调，该框架在提高运算速度的同时还减少了显存占用。默认采用动态的4位量化。</font><font size="5">**Liger Kernel**：是一个大语言模型训练的性能优化框架，可有效地提高吞吐量并减少内存占用。</font><h1 id="6-微调数据集准备"><a href="#6-微调数据集准备" class="headerlink" title="6.微调数据集准备"></a>6.微调数据集准备</h1><font size="5">数据集在微调任务中起着至关重要的作用。要想得到好的微调效果，数据集的质量要远远大于其他参数的设置。如果数据集太小、多样性不足、数据噪声太大、样本偏差严重等问题都会导致微调任务失败。</font><blockquote><ul><li><font size="5">**数据量大小**：数据量不足时，模型无法学习到足够的特征规律，如果调的训练轮数很大，非常容易出现过拟合（仅记住少量样本的噪声信息），导致泛化能力差，在新数据上表现不佳。当然不同的微调任务对数据量的要求不同，如果是邻域知识注入类任务，（7B模型）至少要1000条数据起步，数据集的数据量的扩大也要随着模型参数的变大而增加。也要注意，若数据质量差，海量数据反而放大噪声影响。优先保证单条数据的有效性，再考虑扩充数量。</font></li><li><font size="5">**噪声数据多**：如文本数据中存在大量错别字、格式混乱、图像数据模糊、标注错误等，会误导模型学习错误的映射关系。所以数据集一定要进行清洗，文本数据重点筛除乱码、重复内容、与任务无关的段落（比如微调法律模型时混入娱乐新闻）；标注数据需检查标签-内容一致性（如情感分类中”好评“文本误标为”差评“）。</font></li><li><font size="5">**样本偏差严重**：数据分布与实际应用场景差异大（如训练数据中90%是正面评论，而实际测试数据正负比例均衡），导致模型在真实场景下失效。</font></li><li><font size="5">**任务相关性不足：**要模型学会一个领域的知识，不是说这个邻域的所有数据都建议用于训练，比如你要微调一个金融问答模型时，就不要混入大量的财经新闻，因为新闻侧重事件描述，而问答侧重问题解答，数据形式需贴近实际应用场景</font></li><li><font size="5">**数据多样性不足**：样本覆盖的场景、特征维度单一（如文本数据仅包含短句子，缺乏长句或复杂句式；图像数据仅包含晴天场景，没有雨天、夜晚等），模型无法适应真实应用中的多样性需求。此外，若指令类型单一（如仅包含[解释型]指令），缺乏【推理型】【操作型】指令，会导致模型在实际应用中能力受限。</font></li></ul></blockquote><font size="5">在一次微调任务中，大概80%的时间应该花在数据集的准备和处理上，因为微调的工具、流程和参数的调整都是有经验可循的。从数据采集时需要覆盖多维度场景，到清洗时剔除噪声与偏差样本，再到标注时确保一致性与准确性，每个环节都影响最终效果。此外，还需合理划分训练/验证/测试集，通过数据增强扩充样本多样性，让模型在微调中真正学习到关键特征。</font><font size="5">本次任务进行指令监督微调（SFT），在LLaMA Factory中主要支持Alpaca、ShareGPT两种格式：</font><font size="5">**AIpaca 格式的指令微调数据集：**</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">格式要求</span></span><br><span class="line">[</span><br><span class="line"> &#123;</span><br><span class="line">  &quot;instruction&quot;:&quot;人类指令（必填）&quot;</span><br><span class="line">  &quot;input&quot;:&quot;人类输入（选填）&quot;</span><br><span class="line">  &quot;output&quot;:&quot;模型回答（必填）&quot;</span><br><span class="line">  &quot;system&quot;:&quot;系统提示词（选填）&quot;</span><br><span class="line">  &quot;history&quot;:[</span><br><span class="line">     [&quot;第一轮指令（选填）&quot;，&quot;第一轮回答（选填）&quot;],</span><br><span class="line">     [&quot;第二轮指令（选填）&quot;，&quot;第二轮回答（选填）&quot;]</span><br><span class="line">  ]</span><br><span class="line"> &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">实际案例</span></span><br><span class="line">[</span><br><span class="line"> &#123;</span><br><span class="line">  &quot;instruction&quot;:&quot;将这句英文翻译成中文&quot;</span><br><span class="line">  &quot;input&quot;:&quot;Hello，how are you？&quot;</span><br><span class="line">  &quot;output&quot;:&quot;你好，你好吗？&quot;</span><br><span class="line">  &quot;system&quot;:&quot;翻译任务，请确保翻译准确、自然&quot;</span><br><span class="line">  &quot;history&quot;:[</span><br><span class="line">     [&quot;将这句英文翻译成中文&quot;，&quot;你好，你好吗？&quot;],</span><br><span class="line">     [&quot;将这句中文翻译成英文&quot;，&quot;Hello，how are you？&quot;]</span><br><span class="line">  ]</span><br><span class="line"> &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><font size="5">**ShareGPT 格式的指令微调数据集：**</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">格式要求</span></span><br><span class="line">[</span><br><span class="line"> &#123;</span><br><span class="line">   &quot;conversations&quot;:[</span><br><span class="line">    &#123;</span><br><span class="line">      &quot;from&quot;:&quot;human&quot;,</span><br><span class="line">      &quot;value&quot;:&quot;人类指令&quot;，</span><br><span class="line">    &#125;，</span><br><span class="line">    &#123;</span><br><span class="line">      &quot;from&quot;:&quot;function_call&quot;,</span><br><span class="line">      &quot;value&quot;:&quot;工具参数&quot;，</span><br><span class="line">    &#125;，</span><br><span class="line">    &#123;</span><br><span class="line">      &quot;from&quot;:&quot;observation&quot;,</span><br><span class="line">      &quot;value&quot;:&quot;工具结果&quot;，</span><br><span class="line">    &#125;，</span><br><span class="line">    &#123;</span><br><span class="line">      &quot;from&quot;:&quot;gpt&quot;,</span><br><span class="line">      &quot;value&quot;:&quot;模型回答&quot;，</span><br><span class="line">    &#125;，</span><br><span class="line">   ]，</span><br><span class="line">   &quot;system&quot;:&quot;系统提示词（选填）&quot;，</span><br><span class="line">   &quot;tools&quot;:&quot;工具描述（选填）&quot;</span><br><span class="line"> &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="meta prompt_"># </span><span class="language-bash">实际案例</span></span><br><span class="line">[</span><br><span class="line"> &#123;</span><br><span class="line">   &quot;conversations&quot;:[</span><br><span class="line">    &#123;</span><br><span class="line">      &quot;from&quot;:&quot;human&quot;,</span><br><span class="line">      &quot;value&quot;:&quot;帮我计算一下 2+2&quot;，</span><br><span class="line">    &#125;，</span><br><span class="line">    &#123;</span><br><span class="line">      &quot;from&quot;:&quot;function_call&quot;,</span><br><span class="line">      &quot;value&quot;:&quot;calculator:1|operation=addition|a=2|b=2&quot;，</span><br><span class="line">    &#125;，</span><br><span class="line">    &#123;</span><br><span class="line">      &quot;from&quot;:&quot;observation&quot;,</span><br><span class="line">      &quot;value&quot;:&quot;calculator:1|result=4&quot;，</span><br><span class="line">    &#125;，</span><br><span class="line">    &#123;</span><br><span class="line">      &quot;from&quot;:&quot;gpt&quot;,</span><br><span class="line">      &quot;value&quot;:&quot;2+2的结果是4&quot;，</span><br><span class="line">    &#125;，</span><br><span class="line">   ]，</span><br><span class="line">   &quot;system&quot;:&quot;使用计算器工具完成数学计算任务&quot;，</span><br><span class="line">   &quot;tools&quot;:&quot;calculator:执行基本数学运算&quot;</span><br><span class="line"> &#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><h1 id="7-微调参数设置"><a href="#7-微调参数设置" class="headerlink" title="7.微调参数设置"></a>7.微调参数设置</h1><font size="5">🦊**学习率（Learning rate）：**决定了模型在每次更新时参数调整的幅度。通常在（0，1）之间，数值越大，表明模型每次调整参数的幅度就越大。小数据尽量不要用大学习率。最常用的学习率：**5e-5**（适用于大多数预训练模型的微调）； **4e-5**（更保守的选择，适合重要任务或需要更稳定性的场景）；**1e-5**（全参数微调时使用更小的学习率，防止破坏原始知识）。</font><font size="5">🦸‍♂️**训练轮数（Epochs）：**模型完整地遍历整个训练数据集的次数，1epoch表示模型完整地遍历一次整个训练数据集。**欠拟合**（太少轮数，模型没有充分学习，就像只复习了一遍教材，考试时可能表现不佳）；**理想状态**（3-6轮数，模型学习充分且泛化能力好，就像合理复习后既掌握知识又能应对新题目）；**过拟合**（模型记住训练数据但泛化差，就像死记硬背教材但不会做变式题）；一般情况下，数据集越小需要越多的epochs；数据集越大需要越少的epochs；loss一般控制在0.5-1.5之间。</font> <font size="5">🏋️‍♀️**批量大小（Batch size）：**是指在模型训练过程中，每次更新模型参数时所使用的样本数量。两个关键参数：实际批量大小=每个设备批量大小 x 梯度累积步数，通过这两个参数灵活控制。我们可以减小批量大小，而增加梯度累积步数的方式（4或8）来减少显存的使用。一般建议大batch_size 搭配大学习率，小batch_size 搭配小学习率。小模型/小数据集：从batch_size=1或2开始</font><font size="5">🦉**截断长度（Cutoff Length）：**决定了模型处理文本时能接收的最大token数量（token是文本分块后的单位，如词语、子词）。它直接影响模型对上下文信息的捕获能力，同时制约计算资源的消耗。截断大小每增长一倍，模型处理每个样本的显存消耗近似线性增长。在实际训练过程中，不能为了省显存而把截断长度设定的过小，比如你的训练数据平均大小为4096个Tokens，假设把截断长度设定为2048个tokens，那有接近一半的训练数据都将是被截断的、不完整的，这会大大影响训练效果。在显存条件允许的情况下，最好的截断长度当然是设定为数据集中单条数据的最大tokens数。但现实可能不允许，我们可以去计算数据集Tokens长度的值，再根据显存的大小来逐步调整，这也意味着有1%，5%的训练数据是不完整的，可以在训练前将这部分数据集剔除出去，这样可以减小对训练效果的影响。</font><font size="5">🤖**LoRA参数设置：LoRA秩**决定模型微调时的“表达能力”，8-16开始尝试</font><font size="5">**验证集比例：**从原始训练数据中划分出用于模型性能评估的子集比例，不参与模型训练，仅用于在训练过程中实时监控模型的泛化能力避免过拟合。</font>]]></content>
      
      
      
    </entry>
    
    
    
    <entry>
      <title>哈基鹏的大模型之路（一）</title>
      <link href="/posts/ec11bb5e.html"/>
      <url>/posts/ec11bb5e.html</url>
      
        <content type="html"><![CDATA[<h1 id="1-文本模型微调"><a href="#1-文本模型微调" class="headerlink" title="1.文本模型微调"></a>1.文本模型微调</h1><h2 id="1-1-监督微调"><a href="#1-1-监督微调" class="headerlink" title="1.1 监督微调"></a>1.1 监督微调</h2><font size="5">监督微调（Supervised Fine-Tuning, SFT），其通过标注过的数据直接教模型做事，适合有明确目标的任务。最简单的，就是直接告诉模型输入对应的输出是什么。</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">&#123;&quot;input&quot;:&quot;Hello&quot;, &quot;output&quot;:&quot;你好&quot;&#125;</span><br></pre></td></tr></table></figure><h3 id="1-1-1-指令微调"><a href="#1-1-1-指令微调" class="headerlink" title="1.1.1 指令微调"></a>1.1.1 指令微调</h3><font size="5">当我们希望模型具备多种语言理解的能力，这时候只靠两个字段是不够的。比如**在同一个Input下，我们希望模型有不同的输出**，从而完成不同的任务，就可以引入一个指令的概念，比如这个数据集</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line">[</span><br><span class="line">  &#123;</span><br><span class="line">   &quot;instruction&quot;:&quot;将这句英文翻译成法语&quot;</span><br><span class="line">   &quot;input&quot;:&quot;Hello， how are you?&quot;, </span><br><span class="line">   &quot;output&quot;:&quot;Bonjour, comment ça va?&quot;</span><br><span class="line"> &#125;,</span><br><span class="line">  ...</span><br><span class="line">]</span><br></pre></td></tr></table></figure><font size="5">在上述的例子中，我们告诉模型明确的指令：将英文翻译为法语，再将Input以及output告诉模型，模型就能准确理解要做什么，这就是指令微调。</font><font size="5">指令微调常见的业务场景：</font><font size="5">⛏**智能教育：**实现作业辅导、规划个性化学习路径、辅助语言学习。</font><font size="5">🖋**智能办公：**可处理文档、邮件，进行日程管理。</font><font size="5">🗡**智能翻译：**应用于专业领域翻译、特定场景翻译及多语言交互</font><font size="5">🔫**智能翻译：**让模型根据分析需求指令，对数据进行准确解读和洞察。</font><font size="5">**如何构建高质量指令微调的数据集？**(埋个坑)</font><h3 id="1-1-2-对话微调"><a href="#1-1-2-对话微调" class="headerlink" title="1.1.2 对话微调"></a>1.1.2 对话微调</h3><font size="5">多轮微调（Dialogue Tuning）是通过多轮对话数据训练模型生成连贯、符合语境的回复，强调对话历史的上下文理解和回复的自然流畅性。其核心在于教会模型处理对话中的逻辑关系、情感表达和角色身份，对话微调的数据集通常包含对话的上下文以及对应的回复。</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line">[</span><br><span class="line">  &#123;</span><br><span class="line">    &quot;dialogue&quot;: [</span><br><span class="line">      &#123;&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: &quot;今天天气怎么样？&quot;&#125;,</span><br><span class="line">      &#123;&quot;role&quot;: &quot;assistant&quot;, &quot;content&quot;: &quot;北京今日多云转晴，气温22℃，适合户外活动。&quot;&#125;,</span><br><span class="line">      &#123;&quot;role&quot;: &quot;user&quot;, &quot;content&quot;: &quot;那适合去长城吗？&quot;&#125;,</span><br><span class="line">      &#123;&quot;role&quot;: &quot;assistant&quot;, &quot;content&quot;: &quot;长城景区海拔较高，建议携带外套，注意防晒。&quot;&#125;</span><br><span class="line">    ]</span><br><span class="line">  &#125;,</span><br><span class="line">  ...</span><br><span class="line">]</span><br></pre></td></tr></table></figure><font size="5">对话微调数据集的核心特点：包含多轮对话上下文、标注角色身份，注重回复连贯性与逻辑性。通过这样的数据，模型可以学习到在不同对话场景下如何生成合适的回复，从而提高对话的连贯性。</font><font size="5">对话微调常见的业务场景：</font><font size="5">🏍**智能客服系统：**提升客服机器人在处理用户咨询时的对话能力，能够更准确地理解用户意图并提供解决方案。</font><font size="5">🌆**聊天机器人：**让聊天机器人更自然地与用户进行多轮对话，提高用户体验。</font><font size="5">🚒**语音助手：**优化语音助手在语音交互中的对话表现，使其更符合用户的期望。</font><h3 id="1-1-3-领域适配"><a href="#1-1-3-领域适配" class="headerlink" title="1.1.3 领域适配"></a>1.1.3 领域适配</h3><font size="5">预训练模型虽然基于海量数据具备了通用的文字理解和生成能力，但是由于预训练的数据集在特定领域可能并不会覆盖的那么全面，这些模型在某些特定领域或任务上的表现可能并不理想，这时候就需要进行特定领域的适配微调。领域适配（Domain Adaptation）是指将模型在特定领域的数据上进行微调，使其更好地适应特定领域的任务和需求。特定领域的数据集通常包含该领域的专业术语、特定格式和相关任务的标注。例如在医疗领域，数据集可能包含病例文本、医学术语以及对应的诊断结果等信息。</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">[</span><br><span class="line">  &#123;</span><br><span class="line">    &quot;instruction&quot;: &quot;分析患者的症状描述&quot;,</span><br><span class="line">    &quot;input&quot;: &quot;55岁男性，持续性胸骨后疼痛3小时，含服硝酸甘油无效&quot;,</span><br><span class="line">    &quot;output&quot;: &quot;可能诊断：急性心肌梗死（STEMI），建议立即行心电图检查及心肌酶谱检测&quot;,</span><br><span class="line">    &quot;domain&quot;: &quot;医疗&quot;</span><br><span class="line">  &#125;,</span><br><span class="line">  &#123;</span><br><span class="line">    &quot;instruction&quot;: &quot;解释法律条款&quot;,</span><br><span class="line">    &quot;input&quot;: &quot;《民法典》第1032条&quot;,</span><br><span class="line">    &quot;output&quot;: &quot;该条款规定自然人享有隐私权，任何组织或个人不得以刺探、侵扰、泄露、公开等方式侵害他人隐私权&quot;,</span><br><span class="line">    &quot;domain&quot;: &quot;法律&quot;</span><br><span class="line">  &#125;,</span><br><span class="line">  ...</span><br><span class="line">]</span><br></pre></td></tr></table></figure><font size="5">领域适配数据集的核心特点：领域术语标注（如医学本体库、法律术语库）、复杂规则建模（如药物相互作用、合同条款逻辑）、场景化数据增强（如模拟问诊对话、合同审查流程）；</font><font size="5">领域适配常见的业务场景：</font><font size="5">🚑**医疗领域适配：**用于病历分析、疾病诊断辅助、医疗文献检索等。</font><font size="5">⚖**法律领域适配：**辅助法律文件分析、案例检索、合同审查等。</font><font size="5">💰**金融领域适配：**用于风险评估、市场分析报告生成、金融产品推荐等。</font><font size="5">将领域文献构造成数据集，一般就两个途径：</font><ul><li><font size="5">第一个就是靠纯人工整理，或者与领域专家合作标注数据（可以借助一些三方的标注平台），将数据处理为模型可接受的格式，可能涉及术语标准化等工作。</font></li><li><font size="5">第二个就是借助 AI 辅助生成，注意这里 AI 起到的作用只是辅助和提效，并不能完全自动化，就算大家真的能够借助某些工具全自动将领域文献转化成数据集，我也不建议大家直接用，在不经过人工二次确认的情况下，质量肯定难以保障，最终模型微调的效果也不会很好。</font></li></ul><h3 id="1-1-4-文本分类"><a href="#1-1-4-文本分类" class="headerlink" title="1.1.4 文本分类"></a>1.1.4 文本分类</h3><font size="5">文本分类（Text Classification），它是自然语言处理中的一个经典任务，目的就是通过标注数据训练模型对文本进行类别预测或标签分配。这类任务需要模型理解文本语义与类别特征的关系，适用于需要结构化输出的场景。</font><font size="5">怎么进行文本分类呢？我们需要使用标注了类别的文本数据集对模型进行训练，让模型学习文本特征与类别的映射关系。文本分类数据集的关键在于构建符合业务需求的分类标签，例如从评论中区分出好评和差评，从新闻中区分出客集新闻和金融新闻：</font><figure class="highlight shell"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">[</span><br><span class="line">  &#123;&quot;text&quot;: &quot;这款手机续航长达48小时，拍照效果惊艳&quot;, &quot;label&quot;: &quot;positive&quot;&#125;,</span><br><span class="line">  &#123;&quot;text&quot;: &quot;系统频繁卡顿，客服响应速度慢&quot;, &quot;label&quot;: &quot;negative&quot;&#125;,</span><br><span class="line">  &#123;&quot;text&quot;: &quot;量子计算机突破新型纠错码技术&quot;, &quot;label&quot;: &quot;science_news&quot;&#125;,</span><br><span class="line">  &#123;&quot;text&quot;: &quot;央行宣布下调存款准备金率0.5个百分点&quot;, &quot;label&quot;: &quot;finance_news&quot;&#125;</span><br><span class="line">]</span><br></pre></td></tr></table></figure><font size="5">文本分类微调的典型业务场景：</font><font size="5">🌲**情感分析**：商品评论情感极性识别（正面/负面/中性）</font><font size="5">🌷**内容审核**：检测违规内容（涉政/暴力/广告</font><font size="5">🌵**新闻分类**：自动归类至财经/科技/体育等栏目</font><font size="5">🍀**意图识别**：用户query分类（咨询/投诉/比价）</font><h1 id="2-MGA增强数据集"><a href="#2-MGA增强数据集" class="headerlink" title="2.MGA增强数据集"></a>2.MGA增强数据集</h1><font size="5">当前，大模型的训练高度依赖训练数据的规模与质量，但现实往往面临着两大矛盾：</font><font size="5">**1.数据稀缺性**：高质量语料（如学术文献、专业文本）总量有限，公开数据集（如C4，RefinedWeb）经过严格过滤后仅保留不到10%的原始内容，难以支撑模型的持续扩展和训练。</font><font size="5">**2.重复退化问题：**在传统深度学习中，重复训练是可以继续提升模型性能的，但LLM训练中，过度重复会导致模型泛化能力下降，优化稳定性变差，尤其是参数规模超千亿的模型。</font><font size="5">字节跳动 Seed 团队最近发表了一篇论文：《Reformulation for Pretraining Data Augmentation》</font><font size="5">其中提出了一种新的Massive Genre-Audience（MGA）方法，通过轻量级框架将现有语料系统重构为多样化变体，核心思路是：**基于不同“体裁（Genre）”和“受众（Audience）”生成内容变体，在保留核心知识的同时创造语义丰富的新数据。**</font><font size="5">“`Massive Genre-Audience`”（大规模类型-受众）是论文中提出的 `MGA（Massive Genre-Audience Reformulation）` 方法的核心概念，其含义可从以下两方面具体理解：</font><p><img src="https://docs.easy-dataset.com/~gitbook/image?url=https%3A%2F%2F544849060-files.gitbook.io%2F%7E%2Ffiles%2Fv0%2Fb%2Fgitbook-x-prod.appspot.com%2Fo%2Fspaces%252FDgZmZsLiavwvoXVnDBQ5%252Fuploads%252FXxcouZ418EJizaVhBI0O%252Fimage.png%3Falt%3Dmedia%26token%3D4c6d6b70-d2fe-41d8-9550-703c7fa980c4&amp;width=768&amp;dpr=4&amp;quality=100&amp;sign=fb71f9cd&amp;sv=2" alt="img"></p><font size="5">**“Massive”的含义**</font><ul><li><font size="5">**大规模的多样性生成**：指该方法通过系统设计，能够生成海量的内容变体。例如，论文中提到每次推理会生成 5 对“类型-受众”组合，使原始文档扩展为 5 个新文档，实现 3.9 倍的 Token 数扩展。</font></li><li><font size="5">**覆盖广泛的场景**：强调其适用于大规模语料库的扩展，解决数据稀缺和重复问题，支持模型在数十亿参数规模下的高效训练。</font></li></ul><font size="5">**“Genre-Audience”的含义**</font><font size="5">**Genre（类型）**： 指内容的“知识表达框架”，通过多个维度定义，包括：</font><ul><li><font size="5">**沟通目的**（如教育、分析、叙事）；</font></li><li><font size="5">**内容结构**（如分步教程、学术论文、对话体）；</font></li><li><font size="5">**语言风格**（如严谨学术风、通俗故事风）；</font></li><li><font size="5">**知识深度**（如初学者入门、专业研究者深度分析）。</font></li></ul><font size="5">例如，将同一篇科普文章重构为“学术论文”或“儿童故事”，会采用不同的结构和语言风格，但保留核心知识。</font><font size="5">**Audience（受众）**： 指内容的目标读者群体，结合以下特征：</font><ul><li><font size="5">**人口统计学因素**（年龄、职业、教育背景，如“12-15岁中学生”“医学专业研究生”）；</font></li><li><font size="5">**知识背景与动机**（如“对化学感兴趣的初学者”“需要教学素材的中学教师”）。 例如，针对“办公室工作人员”的急救指南会侧重实用性和通俗表达，而针对“医学生”的版本则会包含更多专业术语和深度理论。</font></li></ul><font size="5"></font><font size="5"></font><font size="5"></font><font size="5"></font><font size="5"></font><font size="5"></font><font size="5"></font><font size="5"></font><font size="5"></font><font size="5"></font>]]></content>
      
      
      
    </entry>
    
    
  
  
</search>
